---
phase: 08-npm-publish-hardening
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - test/helpers.ts
  - test/init.test.ts
autonomous: true
requirements:
  - CLI-01
  - BUILD-03
  - BUILD-04
must_haves:
  truths:
    - "D-04: The blocking pre-publish smoke uses a locally generated npm tarball"
    - "D-05: The smoke runs in a fresh temporary directory and validates scaffold/assets"
    - "D-06: codewiki@latest is a post-publish/manual check, not the release-candidate gate"
  artifacts:
    - path: "test/helpers.ts"
      provides: "Reusable helper for invoking a local package tarball through npx"
      contains: "runPackedCli"
    - path: "test/init.test.ts"
      provides: "Local packed tarball smoke coverage"
      contains: "npm pack --json"
---

<objective>
Prove the local release-candidate tarball works through `npx` in a fresh project and can locate all bundled scaffold and adapter assets.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-npm-publish-hardening/08-CONTEXT.md
@.planning/phases/08-npm-publish-hardening/08-RESEARCH.md
@.planning/phases/08-npm-publish-hardening/08-PATTERNS.md
@test/helpers.ts
@test/init.test.ts
@package.json
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add packed-package CLI helpers</name>
  <files>test/helpers.ts</files>
  <read_first>
    - test/helpers.ts
    - test/init.test.ts
    - package.json
  </read_first>
  <action>
Extend `test/helpers.ts` with helpers that match the existing `runCli` and `mustRun` style:

- `export function runPackedCli(cwd: string, tarballPath: string, args: string[]): RunResult`
  - run `spawnSync("npx", ["--yes", tarballPath, ...args], { cwd, encoding: "utf8" })`
  - return `{ status: result.status, stdout: result.stdout, stderr: result.stderr }`
- `export function mustRunPackedCli(cwd: string, tarballPath: string, args: string[]): RunResult`
  - call `runPackedCli`
  - throw an error on non-zero status using the same stdout/stderr formatting as `mustRun`

Do not change existing helper names or behavior.
  </action>
  <acceptance_criteria>
    - `test/helpers.ts` contains `runPackedCli`
    - `test/helpers.ts` contains `mustRunPackedCli`
    - `test/helpers.ts` contains `spawnSync("npx", ["--yes", tarballPath, ...args]`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "runPackedCli|mustRunPackedCli|spawnSync\\(\"npx\", \\[\"--yes\", tarballPath, \\.\\.\\.args\\]" test/helpers.ts</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Add a local tarball npx smoke test</name>
  <files>test/init.test.ts</files>
  <read_first>
    - test/init.test.ts
    - test/helpers.ts
    - package.json
  </read_first>
  <action>
Add a compiled Node `test()` in `test/init.test.ts` named exactly:

`local npm tarball works through npx in a fresh project`

The test must:

1. Run `spawnSync("npm", ["pack", "--json"], { cwd: process.cwd(), encoding: "utf8" })`.
2. Assert status 0 and parse the first JSON entry's `filename`.
3. Build an absolute tarball path with `path.join(process.cwd(), filename)`.
4. Create a fresh temp project.
5. Run `mustRunPackedCli(cwd, tarballPath, ["init", "--name", "packed-smoke", "--tool", "claude-code,codex,copilot,opencode"])`.
6. Assert stdout contains `CodeWiki initialized for packed-smoke.`, `claude-code adapter:`, `codex adapter:`, `copilot adapter:`, and `opencode adapter:`.
7. Assert concrete files exist:
   - `.codewiki/config.yml`
   - `.codewiki/hooks/pre-wiki-context.sh`
   - `wiki/index.md`
   - `.claude/skills/codewiki-ingest/SKILL.md`
   - `.agents/skills/codewiki-ingest/SKILL.md`
   - `.codex/hooks.json`
   - `.github/hooks/codewiki-hooks.json`
   - `.opencode/plugins/codewiki.ts`
8. Delete the generated tarball at the end of the test with `unlinkSync(tarballPath)` in a `finally` block if it exists.

Import any missing Node APIs from `node:fs` or `node:child_process`. Keep the test focused on package self-containment; do not duplicate every existing adapter assertion.
  </action>
  <acceptance_criteria>
    - `test/init.test.ts` contains `local npm tarball works through npx in a fresh project`
    - `test/init.test.ts` contains `npm", ["pack", "--json"]`
    - `test/init.test.ts` contains `mustRunPackedCli`
    - `test/init.test.ts` contains `claude-code,codex,copilot,opencode`
    - `test/init.test.ts` contains `.opencode/plugins/codewiki.ts`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "local npm tarball works through npx in a fresh project|\\[\"pack\", \"--json\"\\]|mustRunPackedCli|claude-code,codex,copilot,opencode|\\.opencode/plugins/codewiki\\.ts" test/init.test.ts && npm run build && node --test dist/test/init.test.js</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local source tree -> generated tarball | The smoke must execute the packaged artifact, not the working tree directly |
| npm/npx execution -> fresh project | The package must resolve its `dist/templates/**` assets after npm extraction |
| temporary test output -> repository | Generated `.tgz` files must be cleaned up so tests do not dirty the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | R | pre-publish smoke | mitigate | Use local `npm pack --json` tarball instead of registry `codewiki@latest` |
| T-08-02-02 | T | asset resolution | mitigate | Assert concrete installed files from scaffold and every adapter surface |
| T-08-02-03 | D | generated tarball cleanup | mitigate | Remove the generated tarball in a `finally` block |
</threat_model>

<verification>
- `rg -n "runPackedCli|mustRunPackedCli" test/helpers.ts`
- `npm run build && node --test dist/test/init.test.js`
- `npm test`
</verification>

<success_criteria>
- A local `.tgz` candidate can run `init` through `npx --yes`.
- The smoke creates a wiki scaffold and representative Claude, Codex, Copilot, and OpenCode adapter files.
- The test cleans up the generated tarball after running.
</success_criteria>

<output>
After completion, create `.planning/phases/08-npm-publish-hardening/08-02-SUMMARY.md`.
</output>

