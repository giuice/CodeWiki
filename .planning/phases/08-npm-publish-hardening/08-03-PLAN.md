---
phase: 08-npm-publish-hardening
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - README.md
autonomous: true
requirements:
  - CLI-01
  - BUILD-03
  - BUILD-04
must_haves:
  truths:
    - "D-12: README covers user-facing install/use and publish-hardening details"
    - "D-13: README includes local tarball smoke and post-publish codewiki@latest check"
    - "D-14: README stays bounded and does not become a long internal release manual"
    - "Phase 8 success criterion 4: README documents the four supported tools, hook strategies, and `--tool`, `--force`, `--name` flags"
  artifacts:
    - path: "README.md"
      provides: "User and maintainer publish verification guidance"
      contains: "npx codewiki@latest init"
---

<objective>
Update README documentation so users and maintainers understand the supported Node version, runtime dependency contract, local release-candidate verification, post-publish smoke, and common publish troubleshooting.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-npm-publish-hardening/08-CONTEXT.md
@.planning/phases/08-npm-publish-hardening/08-RESEARCH.md
@README.md
@package.json
</context>

<tasks>

<task type="execute">
  <name>Task 1: Document runtime and release-candidate verification</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - package.json
    - .planning/phases/08-npm-publish-hardening/08-CONTEXT.md
  </read_first>
  <action>
In the README `Development` section, add a bounded publish verification subsection that includes these exact commands or strings:

- `node >=20.11.0`
- `npm run build`
- `npm test`
- `npm pack --dry-run --json`
- `npm pack --json`
- `npx --yes ./codewiki-<version>.tgz init --name packed-smoke --tool claude-code,codex,copilot,opencode`
- `npx codewiki@latest init --name latest-smoke`

State that the local tarball smoke is the blocking pre-publish check and that `codewiki@latest` is a post-publish check because it targets the already-published registry package.

Also verify the README still documents the four supported tools (`claude-code`, `codex`, `copilot`, `opencode`), their hook/integration strategies, and the `--tool`, `--force`, and `--name` flags. If any of those are missing or stale, update the relevant existing README usage/tool sections with concise current text.
  </action>
  <acceptance_criteria>
    - `README.md` contains `node >=20.11.0`
    - `README.md` contains `npm pack --dry-run --json`
    - `README.md` contains `npx --yes ./codewiki-<version>.tgz init --name packed-smoke --tool claude-code,codex,copilot,opencode`
    - `README.md` contains `npx codewiki@latest init --name latest-smoke`
    - `README.md` contains `post-publish`
    - `README.md` contains `claude-code`, `codex`, `copilot`, `opencode`, `--tool`, `--force`, and `--name`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "node >=20\\.11\\.0|npm pack --dry-run --json|npx --yes ./codewiki-<version>\\.tgz init --name packed-smoke --tool claude-code,codex,copilot,opencode|npx codewiki@latest init --name latest-smoke|post-publish|claude-code|codex|copilot|opencode|--tool|--force|--name" README.md</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Document dependency and troubleshooting constraints</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - package.json
  </read_first>
  <action>
Update the existing runtime dependency paragraph so it says:

- The published package targets `node >=20.11.0`.
- Runtime dependencies are intentionally zero.
- TypeScript, Vitest, and `@types/node` are dev-only.
- If the local tarball smoke cannot find templates, check that `npm run build` copied `src/templates/**` to `dist/templates/**` and that `npm pack --dry-run --json` lists the expected `dist/templates/...` files.
- If `npx codewiki@latest init` fails after publish while the local tarball passed, treat it as a registry/publish verification issue rather than a source-tree test failure.

Keep this as a short troubleshooting note in README, not a long release manual.
  </action>
  <acceptance_criteria>
    - `README.md` contains `Runtime dependencies are intentionally zero`
    - `README.md` contains `src/templates/**`
    - `README.md` contains `dist/templates/**`
    - `README.md` contains `registry`
    - `README.md` contains `troubleshooting`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Runtime dependencies are intentionally zero|src/templates/\\*\\*|dist/templates/\\*\\*|registry|troubleshooting" README.md</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README -> user execution | Commands must distinguish local release candidate from published registry package |
| README -> maintainer release process | Troubleshooting must point to build, pack manifest, and registry checks without changing scope |
| dependency statement -> package contract | Documentation must match the package's zero-runtime-dependency state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | R | release docs | mitigate | Document local tarball smoke as pre-publish and `codewiki@latest` as post-publish |
| T-08-03-02 | I | runtime contract | mitigate | State `node >=20.11.0` and zero runtime dependencies explicitly |
| T-08-03-03 | D | troubleshooting | mitigate | Provide concise checks for missing templates and registry-specific failure |
</threat_model>

<verification>
- `rg -n "node >=20\\.11\\.0|npm pack --dry-run --json|npx codewiki@latest init --name latest-smoke|Runtime dependencies are intentionally zero|troubleshooting" README.md`
- `npm test`
</verification>

<success_criteria>
- README documents the Node engine contract and zero-runtime-dependency package shape.
- README documents local tarball verification as the blocking pre-publish gate.
- README documents `npx codewiki@latest init` as a post-publish check.
- README includes concise troubleshooting for missing template assets and registry smoke failures.
</success_criteria>

<output>
After completion, create `.planning/phases/08-npm-publish-hardening/08-03-SUMMARY.md`.
</output>
