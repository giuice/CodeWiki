---
phase: 08-npm-publish-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - test/pack.test.ts
autonomous: true
requirements:
  - BUILD-03
  - BUILD-04
must_haves:
  truths:
    - "D-01/D-02: Pack verification covers all required runtime template assets needed by init"
    - "D-03: Pack verification uses npm pack --dry-run --json as the machine-readable source"
    - "D-07: package.json declares engines.node >=20.11.0"
    - "D-08/D-10: Runtime dependencies stay at zero unless a concrete implementation need appears"
    - "D-09/D-11: picocolors is allowed only as a narrow, deliberate exception; this plan does not add it because publish hardening does not require colored output"
  artifacts:
    - path: "package.json"
      provides: "npm engine and runtime dependency contract"
      contains: "\"node\": \">=20.11.0\""
    - path: "test/pack.test.ts"
      provides: "Full runtime template tarball manifest coverage"
      contains: "REQUIRED_TEMPLATE_FILES"
---

<objective>
Lock the npm package contract by adding the Node engine field and expanding pack manifest tests so missing runtime templates fail before publish.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-npm-publish-hardening/08-CONTEXT.md
@.planning/phases/08-npm-publish-hardening/08-RESEARCH.md
@.planning/phases/08-npm-publish-hardening/08-PATTERNS.md
@package.json
@test/pack.test.ts
@src/templates/
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add the Node engine contract and preserve zero runtime dependencies</name>
  <files>package.json</files>
  <read_first>
    - package.json
    - .planning/phases/08-npm-publish-hardening/08-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
Add this exact top-level field to `package.json`:

`"engines": { "node": ">=20.11.0" }`

Keep `dependencies` absent. Do not add `commander`, `picocolors`, or any other runtime dependency. `picocolors` is allowed by Phase 8 context only as a narrow, deliberate exception, but this task has no colored-output requirement and must preserve the zero-runtime-dependency package. Preserve the existing `bin`, `files`, `scripts`, `keywords`, `license`, and `devDependencies` values.
  </action>
  <acceptance_criteria>
    - `package.json` contains `"engines"`
    - `package.json` contains `"node": ">=20.11.0"`
    - `node -e "const p=require('./package.json'); if (p.engines.node !== '>=20.11.0') process.exit(1); if ('dependencies' in p) process.exit(2)"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if (p.engines.node !== '>=20.11.0') process.exit(1); if ('dependencies' in p) process.exit(2)"</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Replace representative pack assertions with a complete runtime template manifest</name>
  <files>test/pack.test.ts</files>
  <read_first>
    - test/pack.test.ts
    - src/templates/skills/codewiki-ingest/SKILL.md
    - src/templates/hooks/pre-wiki-context.sh
    - src/templates/codex/hooks.json
    - src/templates/copilot/hooks/codewiki-hooks.json
    - src/templates/opencode/plugins/codewiki.ts
    - src/templates/claude/agents/codewiki-wiki-updater.md
  </read_first>
  <action>
Update `test/pack.test.ts` so it parses `npm pack --dry-run --json` and builds a `Set<string>` from the first package entry's `files[].path`.

Add a constant named `REQUIRED_TEMPLATE_FILES` containing every runtime template asset required by `init`:

- `dist/templates/skills/codewiki-absorb/SKILL.md`
- `dist/templates/skills/codewiki-breakdown/SKILL.md`
- `dist/templates/skills/codewiki-ingest/SKILL.md`
- `dist/templates/skills/codewiki-lint/SKILL.md`
- `dist/templates/skills/codewiki-prd/SKILL.md`
- `dist/templates/skills/codewiki-process/SKILL.md`
- `dist/templates/skills/codewiki-query/SKILL.md`
- `dist/templates/skills/codewiki-tasks/SKILL.md`
- `dist/templates/hooks/pre-wiki-context.sh`
- `dist/templates/hooks/post-verify.sh`
- `dist/templates/hooks/session-end.sh`
- `dist/templates/claude/agents/codewiki-verifier.md`
- `dist/templates/claude/agents/codewiki-wiki-updater.md`
- `dist/templates/claude/instructions.md`
- `dist/templates/codex/hooks.json`
- `dist/templates/codex/config.toml`
- `dist/templates/codex/hooks/user-prompt-submit.sh`
- `dist/templates/codex/hooks/pre-tool-use.sh`
- `dist/templates/codex/hooks/post-tool-use.sh`
- `dist/templates/codex/hooks/stop.sh`
- `dist/templates/codex/agents/codewiki-verifier.toml`
- `dist/templates/codex/agents/codewiki-wiki-updater.toml`
- `dist/templates/codex/instructions.md`
- `dist/templates/copilot/hooks/codewiki-hooks.json`
- `dist/templates/copilot/hooks/pre-tool-use.sh`
- `dist/templates/copilot/hooks/post-tool-use.sh`
- `dist/templates/copilot/hooks/agent-stop.sh`
- `dist/templates/copilot/instructions.md`
- `dist/templates/opencode/plugins/codewiki.ts`
- `dist/templates/opencode/agents/codewiki-verifier.md`
- `dist/templates/opencode/agents/codewiki-wiki-updater.md`
- `dist/templates/opencode/instructions.md`

For each required path, assert `files.has(requiredPath) === true` with the message `tarball must include ${requiredPath}`.
  </action>
  <acceptance_criteria>
    - `test/pack.test.ts` contains `REQUIRED_TEMPLATE_FILES`
    - `test/pack.test.ts` contains `dist/templates/skills/codewiki-tasks/SKILL.md`
    - `test/pack.test.ts` contains `dist/templates/codex/hooks/user-prompt-submit.sh`
    - `test/pack.test.ts` contains `dist/templates/copilot/hooks/agent-stop.sh`
    - `test/pack.test.ts` contains `dist/templates/opencode/plugins/codewiki.ts`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "REQUIRED_TEMPLATE_FILES|dist/templates/skills/codewiki-tasks/SKILL.md|dist/templates/codex/hooks/user-prompt-submit.sh|dist/templates/copilot/hooks/agent-stop.sh|dist/templates/opencode/plugins/codewiki.ts" test/pack.test.ts && npm run build && node --test dist/test/pack.test.js</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm metadata -> user runtime | `engines.node` communicates the supported Node version before `init` runs |
| source templates -> packaged tarball | `src/templates/**` must survive build and npm file allowlisting as `dist/templates/**` |
| package manifest -> dependency install | Runtime dependencies must not creep into the installer package accidentally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | D | Node runtime | mitigate | Add exact `engines.node` contract and verify it from parsed package JSON |
| T-08-01-02 | T | package dependencies | mitigate | Assert the `dependencies` key remains absent in `package.json` |
| T-08-01-03 | R | tarball manifest | mitigate | Parse `npm pack --dry-run --json` and assert every required runtime template path |
</threat_model>

<verification>
- `node -e "const p=require('./package.json'); if (p.engines.node !== '>=20.11.0') process.exit(1); if ('dependencies' in p) process.exit(2)"`
- `npm run build && node --test dist/test/pack.test.js`
- `npm test`
</verification>

<success_criteria>
- `BUILD-03` is covered by package metadata and a parsed JSON assertion.
- `BUILD-04` remains satisfied by an absent runtime `dependencies` field.
- Pack tests fail if any runtime template asset required by `init` is missing from the tarball.
</success_criteria>

<output>
After completion, create `.planning/phases/08-npm-publish-hardening/08-01-SUMMARY.md`.
</output>
