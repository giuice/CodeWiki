---
phase: 06-opencode-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/opencode/commands/codewiki/ingest.md
  - src/templates/opencode/commands/codewiki/query.md
  - src/templates/opencode/commands/codewiki/lint.md
  - src/templates/opencode/commands/codewiki/absorb.md
  - src/templates/opencode/commands/codewiki/breakdown.md
  - src/templates/opencode/commands/codewiki/prd.md
  - src/templates/opencode/commands/codewiki/tasks.md
  - src/templates/opencode/commands/codewiki/process.md
  - src/templates/opencode/agents/codewiki-wiki-updater.md
  - src/templates/opencode/agents/codewiki-verifier.md
  - src/templates/opencode/instructions.md
autonomous: true
requirements:
  - OC-01
  - OC-02
  - OC-04
must_haves:
  truths:
    - "OpenCode-specific CodeWiki command templates exist for the full eight-command CodeWiki surface"
    - "OpenCode-specific agent templates and AGENTS.md instructions exist without claiming unsupported pre-tool behavior"
    - "npm run build copies the new OpenCode assets into dist/templates so the adapter can install them"
  artifacts:
    - path: "src/templates/opencode/commands/codewiki/ingest.md"
      provides: "OpenCode ingest command template"
    - path: "src/templates/opencode/agents/codewiki-wiki-updater.md"
      provides: "OpenCode wiki-updater subagent definition"
    - path: "src/templates/opencode/instructions.md"
      provides: "OpenCode AGENTS.md instruction fragment"
  key_links:
    - from: "src/templates/opencode/commands/codewiki/ingest.md"
      to: "dist/templates/opencode/commands/codewiki/ingest.md"
      via: "postbuild template copy"
      pattern: "dist/templates/opencode/commands/codewiki/ingest.md"
    - from: "src/templates/opencode/instructions.md"
      to: "AGENTS.md"
      via: "OpenCode adapter marker merge"
      pattern: "CodeWiki"
---

<objective>
Create the OpenCode-specific template surface that Phase 6 depends on: eight command files, two agent files, and the AGENTS.md instruction fragment.

Purpose: OpenCode cannot reuse Claude's markdown frontmatter verbatim. This plan creates the tool-native assets first so the installer work in Plan 06-02 can stay focused on adapter behavior instead of prompt/file authoring.

Output: `src/templates/opencode/commands/codewiki/*.md`, `src/templates/opencode/agents/*.md`, and `src/templates/opencode/instructions.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@src/templates/claude/commands/codewiki/ingest.md
@src/templates/claude/commands/codewiki/prd.md
@src/templates/claude/agents/codewiki-wiki-updater.md
@src/templates/claude/agents/codewiki-verifier.md
@src/templates/claude/instructions.md
</context>

<interfaces>
<!-- Reuse the established CodeWiki prompt bodies, but convert the frontmatter and instruction wording to OpenCode's conventions. -->

From `src/templates/claude/commands/codewiki/ingest.md`:
`description: Digest a raw source document into wiki pages`
`allowed-tools: [Read, Write, Edit, Glob, Grep, Bash]`
`argument-hint: <source-file-path>`

Preserve the ingest body and `$ARGUMENTS` behavior, but strip Claude-only frontmatter fields for OpenCode.

From `src/templates/claude/agents/codewiki-wiki-updater.md`:
`description: Proposes wiki updates based on recent code changes`
`allowed-tools: [Read, Glob, Grep, Bash, Edit, Write]`

Translate the role and rules into an OpenCode subagent definition with explicit OpenCode frontmatter.

From `.planning/research/FEATURES.md`:
```text
OpenCode command path: .opencode/commands/<name>.md
OpenCode agent path: .opencode/agents/<name>.md
Instruction file: AGENTS.md
```
Phase 6 must still honor `OC-01` and install into `.opencode/commands/codewiki/` even though the older research examples were flat.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the eight OpenCode command templates</name>
  <files>
src/templates/opencode/commands/codewiki/ingest.md
src/templates/opencode/commands/codewiki/query.md
src/templates/opencode/commands/codewiki/lint.md
src/templates/opencode/commands/codewiki/absorb.md
src/templates/opencode/commands/codewiki/breakdown.md
src/templates/opencode/commands/codewiki/prd.md
src/templates/opencode/commands/codewiki/tasks.md
src/templates/opencode/commands/codewiki/process.md
  </files>
  <action>
Create OpenCode-native versions of the eight CodeWiki commands: `ingest`, `query`, `lint`, `absorb`, `breakdown`, `prd`, `tasks`, and `process`.

Implementation rules:
- Use the existing Claude command bodies as the source of truth for the workflow content so behavior stays aligned across tools.
- Rewrite the frontmatter to the OpenCode-compatible surface: keep `description`, preserve `$ARGUMENTS` placeholders and prompt bodies, and remove Claude-only fields such as `allowed-tools` and `argument-hint`.
- Place the files under `src/templates/opencode/commands/codewiki/` per `OC-01` and the roadmap. Do not flatten them to `src/templates/opencode/commands/` in this phase even though older research examples used a flat directory.
- Include `absorb.md` and `breakdown.md` so the OpenCode adapter matches the full product command surface already established in `PROJECT.md`.
  </action>
  <verify>
    <automated>npm run build && rg -n '^description:' src/templates/opencode/commands/codewiki/*.md && test -f dist/templates/opencode/commands/codewiki/ingest.md && test -f dist/templates/opencode/commands/codewiki/query.md && test -f dist/templates/opencode/commands/codewiki/lint.md && test -f dist/templates/opencode/commands/codewiki/absorb.md && test -f dist/templates/opencode/commands/codewiki/breakdown.md && test -f dist/templates/opencode/commands/codewiki/prd.md && test -f dist/templates/opencode/commands/codewiki/tasks.md && test -f dist/templates/opencode/commands/codewiki/process.md</automated>
  </verify>
  <done>
    - All eight OpenCode command files exist under `src/templates/opencode/commands/codewiki/`
    - Each file starts with a `description:` frontmatter entry
    - `npm run build` copies the eight files into `dist/templates/opencode/commands/codewiki/`
    - `absorb.md` and `breakdown.md` are included in the OpenCode command set
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the OpenCode agents and AGENTS.md instruction fragment</name>
  <files>
src/templates/opencode/agents/codewiki-wiki-updater.md
src/templates/opencode/agents/codewiki-verifier.md
src/templates/opencode/instructions.md
  </files>
  <action>
Create the OpenCode-specific agent and instruction assets that the adapter will install.

Implementation rules:
- Create `codewiki-wiki-updater.md` and `codewiki-verifier.md` as OpenCode subagent definitions. Translate the established Claude agent behavior, but use OpenCode frontmatter with a required `description` and `mode: subagent`.
- Keep the wiki-updater capable of proposing and applying approved wiki edits; keep the verifier read-only by denying write/edit/bash-equivalent capabilities in the agent frontmatter.
- Create `src/templates/opencode/instructions.md` as the marker-managed content for `AGENTS.md`. It must describe the CodeWiki wiki location, the eight OpenCode commands, the two agents, and the OpenCode limitation that wiki context is provided by static instructions plus the session-completed hook rather than pre-tool injection.
- Do not mention unsupported `PreToolUse`/`PostToolUse` behavior anywhere in the instruction fragment.
  </action>
  <verify>
    <automated>npm run build && rg -n '^description:|^mode:' src/templates/opencode/agents/*.md && rg -n 'CodeWiki|/codewiki-ingest|AGENTS.md|session_completed' src/templates/opencode/instructions.md && test -f dist/templates/opencode/agents/codewiki-wiki-updater.md && test -f dist/templates/opencode/agents/codewiki-verifier.md && test -f dist/templates/opencode/instructions.md</automated>
  </verify>
  <done>
    - Both OpenCode agent files exist under `src/templates/opencode/agents/`
    - `src/templates/opencode/instructions.md` exists and is specific to `AGENTS.md`
    - The verifier agent is configured as read-only
    - `npm run build` copies the agent files and instruction fragment into `dist/templates/opencode/`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template markdown → OpenCode runtime | Prompt frontmatter and body control how OpenCode exposes commands and agents |
| instruction fragment → AGENTS.md merge | Installed instructions become persistent project rules for future sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Elevation of Privilege | `src/templates/opencode/agents/codewiki-verifier.md` | mitigate | Keep the verifier agent explicitly read-only in OpenCode frontmatter so verification cannot mutate the wiki by mistake |
| T-06-01-02 | Tampering | `src/templates/opencode/instructions.md` | mitigate | Document only the supported OpenCode integration surface (static `AGENTS.md` guidance + `session_completed` follow-up) and avoid any claim of unsupported pre-tool context injection |
</threat_model>

<verification>
Run:

```bash
npm run build && rg -n '^description:' src/templates/opencode/commands/codewiki/*.md && rg -n '^description:|^mode:' src/templates/opencode/agents/*.md
```

Expected: build succeeds, the eight command files expose `description:` frontmatter, the two agent files expose OpenCode agent frontmatter, and all assets are present in `dist/templates/opencode/`.
</verification>

<success_criteria>
1. Eight OpenCode command templates exist under `src/templates/opencode/commands/codewiki/`
2. Two OpenCode agent templates and one OpenCode instruction fragment exist under `src/templates/opencode/`
3. The new assets are copied into `dist/templates/opencode/` by `npm run build`
4. No file in this plan claims unsupported OpenCode pre-tool hook behavior
</success_criteria>

<output>
After completion, create `.planning/phases/06-opencode-adapter/06-01-SUMMARY.md`
</output>
