---
phase: 06-opencode-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/opencode/plugins/codewiki.ts
  - src/templates/opencode/agents/codewiki-wiki-updater.md
  - src/templates/opencode/agents/codewiki-verifier.md
  - src/templates/opencode/instructions.md
autonomous: true
requirements:
  - OC-02
  - OC-03
  - OC-04

must_haves:
  truths:
    - "OpenCode-owned template assets exist for the plugin dispatcher, the two agent manifests, and the concise `AGENTS.md` marker block"
    - "The plugin template is a thin dispatcher that references only the documented OpenCode events `tool.execute.before`, `file.edited`, and `session.idle`"
    - "The OpenCode instruction template stays concise and focuses on approval boundaries, shared hooks, and important project paths instead of cloning Claude instructions"
    - "The two OpenCode agent templates preserve the existing updater/verifier role pair while adapting their frontmatter and wording to OpenCode's markdown-agent surface"
  artifacts:
    - path: "src/templates/opencode/plugins/codewiki.ts"
      provides: "Project-local OpenCode plugin template that bridges host events to shared CodeWiki hooks"
      contains: "tool.execute.before"
    - path: "src/templates/opencode/agents/codewiki-wiki-updater.md"
      provides: "OpenCode updater agent manifest preserving approval-gated wiki writes"
      contains: "approval"
    - path: "src/templates/opencode/agents/codewiki-verifier.md"
      provides: "OpenCode verifier agent manifest preserving read-only contradiction/reference checks"
      contains: "read-only"
    - path: "src/templates/opencode/instructions.md"
      provides: "Concise `AGENTS.md` marker block for OpenCode installs"
      contains: "wiki/_backlinks.json"
  key_links:
    - from: "src/templates/opencode/plugins/codewiki.ts"
      to: "src/templates/hooks/pre-wiki-context.sh"
      via: "dispatches the documented pre-edit event to the shared hook"
      pattern: "pre-wiki-context\\.sh"
    - from: "src/templates/opencode/plugins/codewiki.ts"
      to: "src/templates/hooks/post-verify.sh"
      via: "dispatches file-edited events to the shared change-context hook"
      pattern: "post-verify\\.sh"
    - from: "src/templates/opencode/plugins/codewiki.ts"
      to: "src/templates/hooks/session-end.sh"
      via: "dispatches the assistant-idle event to the shared session-summary hook"
      pattern: "session-end\\.sh"
---

<objective>
Create the OpenCode-owned template assets that Phase 6 needs before any installer wiring happens.

Purpose: isolate the new OpenCode plugin, markdown-agent, and `AGENTS.md` assets from the later adapter/test work so Phase 6 stays atomic and reviewable.
Output: four new source templates under `src/templates/opencode/` that the later OpenCode adapter can copy into real projects.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-opencode-adapter/06-CONTEXT.md
@.planning/phases/06-opencode-adapter/06-RESEARCH.md
@.planning/phases/06-opencode-adapter/06-VALIDATION.md
@docs/codewiki-project-v2.md
@docs/implementation-plan-v2.md
@src/templates/claude/agents/codewiki-wiki-updater.md
@src/templates/claude/agents/codewiki-verifier.md
@src/templates/hooks/pre-wiki-context.sh
@src/templates/hooks/post-verify.sh
@src/templates/hooks/session-end.sh

<interfaces>
From current OpenCode docs checked during research:

- project rules live in `AGENTS.md`
- project plugins load from `.opencode/plugins/`
- OpenCode discovers skills from `.agents/skills/<name>/SKILL.md`
- plugin event names include `tool.execute.before`, `file.edited`, and `session.idle`
- plugin examples use the OpenCode `$` helper to invoke local shell commands

From `src/templates/claude/agents/*.md`:

- the updater role is approval-gated and may propose/apply wiki changes only after explicit approval
- the verifier role is read-only and focuses on contradictions, broken references, and missing index coverage

The OpenCode templates must preserve those responsibilities without copying Claude-specific wording or frontmatter literally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the thin OpenCode plugin template</name>
  <files>src/templates/opencode/plugins/codewiki.ts</files>
  <read_first>
    - src/templates/hooks/pre-wiki-context.sh
    - src/templates/hooks/post-verify.sh
    - src/templates/hooks/session-end.sh
    - .planning/phases/06-opencode-adapter/06-RESEARCH.md
  </read_first>
  <action>
Create `src/templates/opencode/plugins/codewiki.ts` as the minimal OpenCode plugin dispatcher per the locked Phase 6 decisions.

Implementation requirements:
- export a project-local plugin factory compatible with OpenCode's plugin docs
- reference only the three documented events: `tool.execute.before`, `file.edited`, `session.idle`
- resolve the shared shell hooks under `.codewiki/hooks/`
- keep behavior intentionally thin: event-to-hook bridge only, no wiki logic and no product heuristics
- include a brief comment or wording that treats `session.idle` as assistant-idle / turn-end, not teardown
- when practical, pass event payloads to stdin so the shared hooks can stay reusable

Do not create adapter wiring here. This file is a source template only.
  </action>
  <verify>
    <automated>test -f src/templates/opencode/plugins/codewiki.ts && rg -n "tool\\.execute\\.before|file\\.edited|session\\.idle|pre-wiki-context\\.sh|post-verify\\.sh|session-end\\.sh" src/templates/opencode/plugins/codewiki.ts</automated>
  </verify>
  <done>The repository contains a thin OpenCode plugin source template referencing only the documented event bridge and shared hook targets.</done>
</task>

<task type="auto">
  <name>Task 2: Create OpenCode-specific updater and verifier agent templates</name>
  <files>src/templates/opencode/agents/codewiki-wiki-updater.md, src/templates/opencode/agents/codewiki-verifier.md</files>
  <read_first>
    - src/templates/claude/agents/codewiki-wiki-updater.md
    - src/templates/claude/agents/codewiki-verifier.md
    - .planning/phases/06-opencode-adapter/06-CONTEXT.md
  </read_first>
  <action>
Create the two OpenCode markdown-agent templates:

- `src/templates/opencode/agents/codewiki-wiki-updater.md`
- `src/templates/opencode/agents/codewiki-verifier.md`

Requirements:
- preserve the same role pair and responsibilities as the existing Claude versions
- adapt the frontmatter and wording for OpenCode's markdown-agent surface
- keep the updater approval-gated for wiki writes
- keep the verifier read-only and focused on contradictions, broken references, and missing index updates
- keep the files concise enough to feel native to OpenCode rather than like transplanted Claude command text

Do not invent additional agent roles, workflow stages, or tool-specific business logic.
  </action>
  <verify>
    <automated>test -f src/templates/opencode/agents/codewiki-wiki-updater.md && test -f src/templates/opencode/agents/codewiki-verifier.md && rg -n "approval|wiki/index\\.md|contradiction|broken ref|read-only|mode:" src/templates/opencode/agents/codewiki-wiki-updater.md src/templates/opencode/agents/codewiki-verifier.md</automated>
  </verify>
  <done>The OpenCode agent templates preserve the updater/verifier behavior contract while using host-appropriate markdown-agent framing.</done>
</task>

<task type="auto">
  <name>Task 3: Create the concise OpenCode AGENTS marker template</name>
  <files>src/templates/opencode/instructions.md</files>
  <read_first>
    - .planning/phases/06-opencode-adapter/06-CONTEXT.md
    - docs/codewiki-project-v2.md
    - src/templates/claude/instructions.md
  </read_first>
  <action>
Create `src/templates/opencode/instructions.md` as the concise CodeWiki marker block that will be merged into `AGENTS.md` for OpenCode installs.

The content must cover only:
- available CodeWiki skills at a high level
- the human approval boundary for `wiki/` edits
- the expected pre/post/session hook behavior in OpenCode terms
- important project paths: `wiki/`, `raw/`, `.codewiki/config.yml`, `wiki/_backlinks.json`

Do not mirror the Claude instruction block section-for-section, and do not describe OpenCode as "instruction-only" because Phase 6 is implementing a real plugin + agent adapter.
  </action>
  <verify>
    <automated>test -f src/templates/opencode/instructions.md && rg -n "wiki/|raw/|\\.codewiki/config\\.yml|wiki/_backlinks\\.json|approval" src/templates/opencode/instructions.md</automated>
  </verify>
  <done>The OpenCode instruction source is concise, practical, and ready to merge into project `AGENTS.md` files via marker comments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenCode plugin runtime -> shared CodeWiki hooks | The plugin may only dispatch host events to the shared shell hooks, not add hidden business logic |
| OpenCode agents -> project wiki | The updater must remain approval-gated; the verifier must remain read-only |
| OpenCode instructions -> project AGENTS.md | The marker block must stay concise and avoid overriding unrelated project rules outside the managed section |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T | `src/templates/opencode/plugins/codewiki.ts` | mitigate | Keep the plugin template to documented event names plus hook dispatch only; no product logic and no extra persistence behavior |
| T-06-01-02 | E | updater agent template | mitigate | Preserve explicit approval language for all wiki writes and keep responsibilities aligned with the existing updater role |
| T-06-01-03 | T | `src/templates/opencode/instructions.md` | mitigate | Keep the AGENTS block narrow and practical so the later marker merge changes only CodeWiki-managed content |
</threat_model>

<verification>
- `test -f src/templates/opencode/plugins/codewiki.ts`
- `rg -n "tool\\.execute\\.before|file\\.edited|session\\.idle|pre-wiki-context\\.sh|post-verify\\.sh|session-end\\.sh" src/templates/opencode/plugins/codewiki.ts`
- `test -f src/templates/opencode/agents/codewiki-wiki-updater.md`
- `test -f src/templates/opencode/agents/codewiki-verifier.md`
- `rg -n "approval|wiki/index\\.md|contradiction|broken ref|read-only|mode:" src/templates/opencode/agents/codewiki-wiki-updater.md src/templates/opencode/agents/codewiki-verifier.md`
- `test -f src/templates/opencode/instructions.md`
- `rg -n "wiki/|raw/|\\.codewiki/config\\.yml|wiki/_backlinks\\.json|approval" src/templates/opencode/instructions.md`
</verification>

<success_criteria>
- The repository contains one OpenCode plugin source template, two OpenCode agent templates, and one concise OpenCode instruction template.
- The plugin template references exactly the documented OpenCode event bridge and the shared CodeWiki hook targets.
- The agent templates preserve updater/verifier responsibilities without reverting to Claude-specific command/instruction framing.
- The instruction template is concise and ready for `AGENTS.md` marker merging in the installer.
</success_criteria>

<output>
After completion, create `.planning/phases/06-opencode-adapter/06-01-SUMMARY.md`
</output>
