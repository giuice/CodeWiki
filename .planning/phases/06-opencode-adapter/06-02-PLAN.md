---
phase: 06-opencode-adapter
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - src/lib/adapters/opencode.ts
  - src/lib/adapters/index.ts
  - src/commands/init.ts
  - test/init.test.ts
  - src/templates/__tests__/opencode-adapter.test.ts
autonomous: true
requirements:
  - OC-01
  - OC-02
  - OC-03
  - OC-04

must_haves:
  truths:
    - "Running `init --tool opencode` installs the existing shared `.agents/skills` tree plus the OpenCode-owned plugin, agent, and `AGENTS.md` surfaces"
    - "The OpenCode adapter reuses existing copy/merge/report helpers and does not duplicate the shared skill installer"
    - "Re-running the OpenCode install does not create duplicate marker sections in `AGENTS.md` and does not leave OpenCode mislabeled as a pending integration"
    - "Regression coverage proves explicit OpenCode bootstrap works even when no prior OpenCode config exists"
  artifacts:
    - path: "src/lib/adapters/opencode.ts"
      provides: "Real OpenCode adapter installing `.opencode/plugins`, `.opencode/agents`, and `AGENTS.md`"
      exports: ["OpenCodeAdapter"]
    - path: "src/lib/adapters/index.ts"
      provides: "Adapter resolution that runs shared-skills plus the real OpenCode adapter for OpenCode selections"
      contains: "opencode"
    - path: "src/commands/init.ts"
      provides: "Updated pending-integration reporting so OpenCode is no longer described as shared-skills-only"
      contains: "SHARED_SKILL_ONLY_TOOLS"
    - path: "test/init.test.ts"
      provides: "Executable install/rerun coverage for explicit OpenCode bootstrap"
      contains: ".opencode/plugins/codewiki.ts"
    - path: "src/templates/__tests__/opencode-adapter.test.ts"
      provides: "Template-level assertions for the OpenCode plugin, instructions, and agent assets"
      contains: "tool.execute.before"
  key_links:
    - from: "src/lib/adapters/opencode.ts"
      to: "src/templates/opencode/plugins/codewiki.ts"
      via: "copies the project-local plugin template into `.opencode/plugins/`"
      pattern: "plugins/codewiki\\.ts"
    - from: "src/lib/adapters/opencode.ts"
      to: "src/templates/opencode/instructions.md"
      via: "merges the concise CodeWiki marker block into `AGENTS.md`"
      pattern: "AGENTS\\.md"
    - from: "test/init.test.ts"
      to: "src/commands/init.ts"
      via: "explicit OpenCode CLI install proves adapter resolution and pending-report cleanup end-to-end"
      pattern: "opencode"
---

<objective>
Implement the real OpenCode adapter and the regression coverage that proves it works.

Purpose: convert OpenCode from a "shared skills only" pending integration into a real installed surface while keeping the already-shipped `.agents/skills` behavior intact.
Output: one real OpenCode adapter, updated adapter/init routing, and regression coverage for explicit bootstrap plus rerun idempotency.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-opencode-adapter/06-CONTEXT.md
@.planning/phases/06-opencode-adapter/06-RESEARCH.md
@.planning/phases/06-opencode-adapter/06-VALIDATION.md
@.planning/phases/06-opencode-adapter/06-01-PLAN.md
@docs/codewiki-project-v2.md
@docs/implementation-plan-v2.md
@src/commands/init.ts
@src/lib/adapters/types.ts
@src/lib/adapters/base.ts
@src/lib/adapters/claude.ts
@src/lib/adapters/index.ts
@src/lib/adapters/shared-skills.ts
@src/lib/merge.ts
@src/templates/opencode/plugins/codewiki.ts
@src/templates/opencode/agents/codewiki-wiki-updater.md
@src/templates/opencode/agents/codewiki-verifier.md
@src/templates/opencode/instructions.md
@test/helpers.ts
@test/init.test.ts

<interfaces>
From `src/commands/init.ts`:
```typescript
let tools = requestedTools ?? await detectTools(root);
...
const { adapters, unsupported } = await resolveAdapters(tools);
...
const hasSharedSkillsAdapter = adapters.some((adapter) => adapter.tool === "shared-skills");
const pendingIntegrationEntries = hasSharedSkillsAdapter ? getPendingIntegrationEntries(tools) : [];
```
Explicit `--tool opencode` already bypasses auto-detection. The remaining CLI work is making the report accurate once OpenCode is implemented.

From `src/lib/adapters/claude.ts`:
```typescript
await ensureDir(options.root, CLAUDE_AGENTS_DIR)
...
report.push(await this.mergeInstructions(options))
```
Reuse the same copy/merge/report pattern for OpenCode rather than inventing a one-off installer.

From `src/lib/adapters/shared-skills.ts`:
```typescript
return copyTemplateDir(
  path.join(options.templateDir, "skills"),
  ensureInsideRoot(options.root, AGENT_SKILLS_DIR),
  options.force,
  options.root
);
```
OpenCode must continue relying on this shared skill tree; the real OpenCode adapter should add only the plugin, agent, and `AGENTS.md` surfaces.

From `src/lib/merge.ts`:
```typescript
export function mergeMarkerSection(existing: string, newContent: string, force: boolean): string
```
Use the same marker merge approach for `AGENTS.md` that Claude already uses for `CLAUDE.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the real OpenCode adapter and resolve it alongside shared-skills</name>
  <files>src/lib/adapters/opencode.ts, src/lib/adapters/index.ts</files>
  <read_first>
    - src/lib/adapters/claude.ts
    - src/lib/adapters/shared-skills.ts
    - src/lib/adapters/base.ts
    - src/lib/merge.ts
    - src/templates/opencode/plugins/codewiki.ts
    - src/templates/opencode/instructions.md
  </read_first>
  <action>
Create `src/lib/adapters/opencode.ts` and update `src/lib/adapters/index.ts` so OpenCode selections install both:

- the existing shared-skills adapter for `.agents/skills`
- the new OpenCode adapter for:
  - `.opencode/plugins/codewiki.ts`
  - `.opencode/agents/codewiki-wiki-updater.md`
  - `.opencode/agents/codewiki-verifier.md`
  - `AGENTS.md` marker section

Implementation requirements:
- reuse `copyTemplateDir`, `ensureDir`, `ensureInsideRoot`, `exists`, `readTextIfExists`, and `mergeMarkerSection`
- keep the OpenCode adapter focused on only the surfaces OpenCode owns
- do not duplicate hook installation; the shared scaffold already owns `.codewiki/hooks/`
- do not introduce `.opencode/skills/`
- keep shared-skills deduplicated across mixed non-Claude selections
  </action>
  <verify>
    <automated>test -f src/lib/adapters/opencode.ts && rg -n "OpenCodeAdapter|AGENTS\\.md|\\.opencode/plugins|\\.opencode/agents|mergeMarkerSection" src/lib/adapters/opencode.ts src/lib/adapters/index.ts</automated>
  </verify>
  <done>The codebase has a real OpenCode adapter layered on top of the existing shared-skills install surface.</done>
</task>

<task type="auto">
  <name>Task 2: Remove OpenCode from the pending-integration report once the adapter is real</name>
  <files>src/commands/init.ts</files>
  <read_first>
    - src/commands/init.ts
    - test/init.test.ts
    - .planning/phases/06-opencode-adapter/06-RESEARCH.md
  </read_first>
  <action>
Update `src/commands/init.ts` so OpenCode is no longer treated as a "shared skills installed; hooks and instructions remain pending" tool after the real OpenCode adapter exists.

Requirements:
- preserve the existing pending messaging for tools that truly remain partial
- keep explicit `--tool opencode` authoritative
- do not broaden auto-detection in this phase
- keep the sectioned report format stable for already-shipped Claude and shared-skills behavior
  </action>
  <verify>
    <automated>rg -n "SHARED_SKILL_ONLY_TOOLS|getPendingIntegrationEntries|opencode" src/commands/init.ts</automated>
  </verify>
  <done>The CLI report matches reality: OpenCode is implemented, while only the still-unfinished tools remain listed as pending.</done>
</task>

<task type="auto">
  <name>Task 3: Add OpenCode regression coverage for template content, explicit bootstrap, and rerun idempotency</name>
  <files>test/init.test.ts, src/templates/__tests__/opencode-adapter.test.ts</files>
  <read_first>
    - test/init.test.ts
    - test/helpers.ts
    - src/templates/__tests__/agents.test.ts
    - src/templates/opencode/plugins/codewiki.ts
    - src/templates/opencode/instructions.md
  </read_first>
  <action>
Extend the test surface with:

1. `test/init.test.ts`
   - add an explicit `init --tool opencode` temp-project test with no prior OpenCode config
   - assert `.agents/skills/codewiki-<name>/SKILL.md` exists
   - assert `.opencode/plugins/codewiki.ts` exists
   - assert both `.opencode/agents/` files exist
   - assert `AGENTS.md` contains a single CodeWiki marker block
   - rerun the install and confirm the marker block is still singular and the report does not say OpenCode is pending

2. `src/templates/__tests__/opencode-adapter.test.ts`
   - assert plugin template contains `tool.execute.before`, `file.edited`, `session.idle`, and the shared hook filenames
   - assert instructions template stays concise and references `wiki/`, `raw/`, `.codewiki/config.yml`, and `wiki/_backlinks.json`
   - assert the OpenCode agent templates preserve updater/verifier responsibilities

Use existing test patterns; do not add new frameworks.
  </action>
  <verify>
    <automated>npm run build && node --test dist/test/init.test.js && npx vitest run src/templates/__tests__/opencode-adapter.test.ts</automated>
  </verify>
  <done>The repo has executable coverage for OpenCode explicit bootstrap, install outputs, rerun idempotency, and template-level contract drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI tool selection -> installed OpenCode surface | Explicit `--tool opencode` must install the full Phase 6 surface without relying on prior repo markers |
| OpenCode adapter -> project AGENTS.md | Marker-section merging must preserve unrelated project rules while inserting CodeWiki guidance exactly once |
| shared-skills installer -> OpenCode adapter | The OpenCode adapter must complement, not replace or duplicate, the already-shipped `.agents/skills` surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | T | `src/lib/adapters/opencode.ts` | mitigate | Reuse the existing copy/merge/report helpers and keep the adapter limited to plugin, agents, and `AGENTS.md` surfaces |
| T-06-02-02 | T | `src/commands/init.ts` pending-report logic | mitigate | Remove OpenCode from the shared-skills-only pending set once the real adapter lands and lock the behavior in integration tests |
| T-06-02-03 | R | `AGENTS.md` marker merge | mitigate | Use `mergeMarkerSection()` and assert exactly one CodeWiki marker block after repeated installs |
| T-06-02-04 | D | explicit bootstrap path | mitigate | Add a temp-project test with `--tool opencode` and no prior `opencode.json` or `.opencode/` files |
</threat_model>

<verification>
- `test -f src/lib/adapters/opencode.ts`
- `rg -n "OpenCodeAdapter|AGENTS\\.md|\\.opencode/plugins|\\.opencode/agents|mergeMarkerSection" src/lib/adapters/opencode.ts src/lib/adapters/index.ts`
- `rg -n "SHARED_SKILL_ONLY_TOOLS|getPendingIntegrationEntries|opencode" src/commands/init.ts`
- `npm run build && node --test dist/test/init.test.js && npx vitest run src/templates/__tests__/opencode-adapter.test.ts`
- `npm test`
</verification>

<success_criteria>
- `init --tool opencode` installs the shared `.agents/skills` tree plus `.opencode/plugins/codewiki.ts`, `.opencode/agents/`, and a CodeWiki marker section in `AGENTS.md`.
- OpenCode is resolved as a real adapter instead of staying in the "pending integrations" report.
- Re-running the install does not duplicate marker sections or regress the report output.
- Automated coverage exists for both install outputs and template content.
</success_criteria>

<output>
After completion, create `.planning/phases/06-opencode-adapter/06-02-SUMMARY.md`
</output>
