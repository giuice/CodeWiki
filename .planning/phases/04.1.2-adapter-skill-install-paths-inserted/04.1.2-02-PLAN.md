---
phase: 04.1.2-adapter-skill-install-paths-inserted
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/adapters/types.ts
  - src/lib/adapters/index.ts
  - src/lib/adapters/shared-skills.ts
autonomous: true
requirements:
  - SM-03
must_haves:
  truths:
    - "Selecting codex, copilot, or opencode installs the eight canonical skill directories to `.agents/skills/codewiki-<name>/SKILL.md`"
    - "The shared non-Claude skill install reuses `src/templates/skills` rather than any legacy command directory or generated content"
    - "Mixed non-Claude selections install the shared `.agents/skills` tree once, not once per selected tool"
  artifacts:
    - path: "src/lib/adapters/shared-skills.ts"
      provides: "Reusable installer for the shared `.agents/skills` tree"
      contains: ".agents/skills"
    - path: "src/lib/adapters/index.ts"
      provides: "Adapter resolution that adds the shared skills installer for non-Claude selections"
      contains: "shared-skills"
    - path: "src/lib/adapters/types.ts"
      provides: "Adapter contract that can label the shared skill installer"
      contains: "shared-skills"
  key_links:
    - from: "src/lib/adapters/shared-skills.ts"
      to: "src/templates/skills/codewiki-ingest/SKILL.md"
      via: "copyTemplateDir reads from templateDir/skills into `.agents/skills`"
      pattern: "path.join\\(options.templateDir, \"skills\"\\)"
    - from: "src/lib/adapters/index.ts"
      to: "src/lib/adapters/shared-skills.ts"
      via: "resolveAdapters adds the shared installer when non-Claude tools are selected"
      pattern: "shared-skills"
---

<objective>
Add the shared `.agents/skills` installer that serves Codex, Copilot, and OpenCode selections.

Purpose: Deliver the non-Claude half of SM-03 using the canonical skill source tree from Phase 04.1.1, without pretending the later tool-specific hook/instruction adapters are already implemented.
Output: A shared-skills adapter plus the minimal adapter-contract update needed to install `.agents/skills/codewiki-<name>/SKILL.md` once for any non-Claude selection set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-01-SUMMARY.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-04-SUMMARY.md
@docs/codewiki-project-v2.md
@docs/skills-migration-handoff.md
@docs/skills/wiki.md
@src/lib/adapters/types.ts
@src/lib/adapters/index.ts
@src/lib/adapters/base.ts
@src/core/types.ts
@src/templates/skills/codewiki-ingest/SKILL.md
@src/templates/skills/codewiki-process/SKILL.md

<interfaces>
From src/core/types.ts:
```typescript
export const SUPPORTED_TOOLS = ["claude-code", "codex", "copilot", "opencode"] as const;
export type SupportedTool = (typeof SUPPORTED_TOOLS)[number];
```

From src/lib/adapters/types.ts:
```typescript
export interface AdapterInstallOptions {
  root: string;
  projectName: string;
  force: boolean;
  templateDir: string;
}

export interface ToolAdapter {
  tool: SupportedTool;
  install(options: AdapterInstallOptions): Promise<ReportEntry[]>;
}
```

From src/lib/adapters/index.ts:
```typescript
export async function resolveAdapters(
  tools: SupportedTool[]
): Promise<{ adapters: ToolAdapter[]; unsupported: SupportedTool[] }> {
  const adapters: ToolAdapter[] = [];
  const unsupported: SupportedTool[] = [];

  for (const tool of tools) {
    const factory = adapterFactories[tool];
    if (!factory) {
      unsupported.push(tool);
      continue;
    }

    adapters.push(await factory());
  }

  return { adapters, unsupported };
}
```
Use the existing `copyTemplateDir()` helper; do not invent a second directory-copy implementation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the adapter contract for a shared skill-install section</name>
  <files>src/lib/adapters/types.ts</files>
  <read_first>
    - src/lib/adapters/types.ts
    - src/core/types.ts
    - src/commands/init.ts
  </read_first>
  <action>
Make the minimal contract change needed for a reusable shared-skills installer per D-02 and D-05.

Recommended shape: allow `ToolAdapter.tool` to represent a shared install section (for example `SupportedTool | "shared-skills"`) so the existing init/report pipeline can render a dedicated section for `.agents/skills` without pretending it is one of the tool-specific future adapters.

Keep `AdapterInstallOptions` unchanged. Do not move tool-selection logic into `core/types.ts`, and do not broaden the supported CLI flag values.
  </action>
  <verify>
    <automated>grep -q 'shared-skills' src/lib/adapters/types.ts</automated>
  </verify>
  <done>The adapter contract can describe a shared skill-install section without changing the supported tool flags.</done>
</task>

<task type="auto">
  <name>Task 2: Create the shared `.agents/skills` installer and wire it into adapter resolution</name>
  <files>src/lib/adapters/shared-skills.ts, src/lib/adapters/index.ts</files>
  <read_first>
    - src/lib/adapters/index.ts
    - src/lib/adapters/base.ts
    - src/templates/skills/codewiki-ingest/SKILL.md
    - src/templates/skills/codewiki-process/SKILL.md
  </read_first>
  <action>
Create `src/lib/adapters/shared-skills.ts` and update `src/lib/adapters/index.ts` so any selection containing `codex`, `copilot`, or `opencode` gets one shared installer per D-02, D-04, and D-05.

Implementation requirements:
- Copy from `path.join(options.templateDir, "skills")`
- Target `ensureInsideRoot(options.root, ".agents/skills")`
- Preserve the per-skill directory layout exactly (`codewiki-<name>/SKILL.md`)
- Deduplicate the shared installer across mixed non-Claude selections (`codex,copilot,opencode` still installs the tree once)
- Stop routing non-Claude selections through the legacy `unsupported` path when the shared skill surface is now implemented

Do not install hooks, agent definitions, or instruction files from this adapter. Those remain future tool-specific work.
  </action>
  <verify>
    <automated>repo_root="$PWD" && rtk npm run build && tmpdir="$(mktemp -d)" && cd "$tmpdir" && rtk proxy node "$repo_root/dist/bin/codewiki.js" init --tool codex,copilot >/tmp/codewiki-04.1.2-02.out && test "$(find .agents/skills -mindepth 1 -maxdepth 1 -type d | wc -l)" -eq 8 && test -f .agents/skills/codewiki-ingest/SKILL.md && test -f .agents/skills/codewiki-process/SKILL.md && test ! -e .claude/skills</automated>
  </verify>
  <done>Non-Claude selections install one shared `.agents/skills` tree with all eight CodeWiki skills and no redundant Claude tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected non-Claude tools -> shared agent-skill tree | One CLI selection set must map to exactly one `.agents/skills` install tree |
| canonical skill templates -> `.agents/skills` | The installer must copy only the canonical per-skill directories, not stale command templates or generated content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.2-03 | T | shared skill source selection | mitigate | Read only `templateDir/skills` and preserve the per-skill directory layout from Phase 04.1.1 |
| T-04.1.2-04 | D | mixed non-Claude selections | mitigate | Deduplicate the shared installer in `resolveAdapters()` so `codex,copilot,opencode` installs one `.agents/skills` tree instead of repeatedly writing the same files |
</threat_model>

<verification>
- `grep -q 'shared-skills' src/lib/adapters/types.ts`
- `repo_root="$PWD" && rtk npm run build && tmpdir="$(mktemp -d)" && cd "$tmpdir" && rtk proxy node "$repo_root/dist/bin/codewiki.js" init --tool codex,copilot >/tmp/codewiki-04.1.2-02.out && test "$(find .agents/skills -mindepth 1 -maxdepth 1 -type d | wc -l)" -eq 8`
- `test -f .agents/skills/codewiki-ingest/SKILL.md`
- `test -f .agents/skills/codewiki-process/SKILL.md`
- `test ! -e .claude/skills`
</verification>

<success_criteria>
Running `init --tool codex`, `init --tool copilot`, `init --tool opencode`, or any mixed non-Claude selection installs the shared `.agents/skills` tree once from the canonical skill templates.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.2-adapter-skill-install-paths-inserted/04.1.2-02-SUMMARY.md`
</output>
