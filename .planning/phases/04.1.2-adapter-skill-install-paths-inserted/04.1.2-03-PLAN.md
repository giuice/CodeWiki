---
phase: 04.1.2-adapter-skill-install-paths-inserted
plan: 03
type: execute
wave: 2
depends_on:
  - 04.1.2-01
  - 04.1.2-02
files_modified:
  - src/commands/init.ts
autonomous: true
requirements:
  - SM-03
must_haves:
  truths:
    - "Combined selections such as `claude-code,codex` run both the Claude skill install and the shared `.agents/skills` install in one `init` execution"
    - "The init report explicitly says shared skills were installed while non-Claude hook/instruction integrations remain pending"
    - "Tools that now receive the shared skill tree are no longer reported as blanket 'unsupported' selections"
  artifacts:
    - path: "src/commands/init.ts"
      provides: "Init orchestration and reporting for combined Claude + shared-skill installs"
      contains: "Tool-specific integrations pending"
  key_links:
    - from: "src/commands/init.ts"
      to: "src/lib/adapters/index.ts"
      via: "resolveAdapters drives the Claude and shared-skills installer sections"
      pattern: "resolveAdapters"
    - from: "src/commands/init.ts"
      to: ".agents/skills/codewiki-ingest/SKILL.md"
      via: "combined selections retain the shared-skills adapter section instead of falling back to unsupported-tool messaging"
      pattern: "Tool-specific integrations pending"
---

<objective>
Update `init` so the combined-selection flow reports the new partial support accurately.

Purpose: Finish SM-03 by making `claude-code,codex`-style selections install both skill trees in one run while telling the user that shared skills are in place and the later tool-specific integrations are still pending.
Output: `src/commands/init.ts` renders Claude + shared-skills sections together and replaces the blanket unsupported messaging for non-Claude tools with an explicit pending-integration section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@docs/codewiki-project-v2.md
@docs/skills-migration-handoff.md
@src/commands/init.ts
@src/lib/adapters/index.ts
@src/lib/reporter.ts

<interfaces>
From src/lib/reporter.ts:
```typescript
export interface ReportSection {
  title: string;
  entries: ReportEntry[];
}

export function formatSectionedReport(projectName: string, sections: ReportSection[]): string;
```

Current init report-building flow:
```typescript
const { adapters, unsupported } = await resolveAdapters(tools);
const sections: ReportSection[] = [{ title: "Wiki scaffold", entries: scaffoldEntries }];

for (const adapter of adapters) {
  const adapterEntries = await adapter.install({ root, projectName, force, templateDir });
  sections.push({ title: `${adapter.tool} adapter`, entries: adapterEntries });
}

if (unsupported.length > 0) {
  sections.push({
    title: "Unsupported (not yet implemented)",
    entries: unsupported.map((tool) => ({
      action: "skipped" as const,
      path: tool,
      reason: "adapter not implemented"
    }))
  });
}
```
This is the user-facing boundary to update in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace blanket unsupported messaging with explicit partial-support reporting for non-Claude selections</name>
  <files>src/commands/init.ts</files>
  <read_first>
    - src/commands/init.ts
    - src/lib/adapters/index.ts
    - src/lib/reporter.ts
    - docs/skills-migration-handoff.md
  </read_first>
  <action>
Update the report assembly in `initCommand()` per D-02 and D-04 so non-Claude selections that now receive shared skills are reported accurately.

Required behavior:
- Keep the existing `Wiki scaffold`, `claude-code adapter`, and `shared-skills adapter` sections
- Add a follow-on section titled `Tool-specific integrations pending` when `codex`, `copilot`, or `opencode` were selected
- Each pending entry should explain that shared skills were installed but hooks and instruction-file wiring remain future phase work
- Reserve `Unsupported (not yet implemented)` for truly unhandled selections only; tools covered by the shared-skills adapter must not appear there anymore

Do not change argument parsing, TTY fallback, or scaffold order.
  </action>
  <verify>
    <automated>grep -q 'Tool-specific integrations pending' src/commands/init.ts</automated>
  </verify>
  <done>`init` has an explicit pending-integration report path for non-Claude tools that now receive shared skills.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve combined-selection execution order and verify dual-tree installs in one run</name>
  <files>src/commands/init.ts</files>
  <read_first>
    - src/commands/init.ts
    - src/lib/adapters/index.ts
  </read_first>
  <action>
Make sure combined selections such as `claude-code,codex` still execute in one pass after Plans 01 and 02 land:

- Claude install section runs and writes `.claude/skills`
- Shared-skills section runs and writes `.agents/skills`
- Pending-integration messaging appears after the real install sections
- `claude-code`-only behavior remains unchanged (no pending section)

Keep the output ordering stable so the user sees installed surfaces first and pending surfaces second.
  </action>
  <verify>
    <automated>repo_root="$PWD" && rtk npm run build && tmpdir="$(mktemp -d)" && cd "$tmpdir" && output="$(rtk proxy node "$repo_root/dist/bin/codewiki.js" init --tool claude-code,codex)" && printf '%s\n' "$output" | grep -q 'claude-code adapter:' && printf '%s\n' "$output" | grep -q 'shared-skills adapter:' && printf '%s\n' "$output" | grep -q 'Tool-specific integrations pending:' && printf '%s\n' "$output" | grep -q 'codex (shared skills installed; hooks and instructions remain pending)' && test -f .claude/skills/codewiki-ingest/SKILL.md && test -f .agents/skills/codewiki-ingest/SKILL.md</automated>
  </verify>
  <done>Combined selections install both skill trees in one run and the report clearly separates installed surfaces from still-pending tool-specific integrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter-resolution state -> user-facing report | The CLI report is the user's only immediate signal about which install surfaces are real versus still pending |
| mixed tool selection -> combined install flow | `init` must orchestrate both skill trees without suppressing one or mislabeling the result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.2-05 | I | init report messaging | mitigate | Replace the blanket unsupported section for non-Claude tools with explicit partial-support messaging that matches the new shared-skills behavior |
| T-04.1.2-06 | D | combined selection orchestration | mitigate | Verify `claude-code,codex` installs both `.claude/skills` and `.agents/skills` in one invocation and preserves section order |
</threat_model>

<verification>
- `grep -q 'Tool-specific integrations pending' src/commands/init.ts`
- `repo_root="$PWD" && rtk npm run build && tmpdir="$(mktemp -d)" && cd "$tmpdir" && output="$(rtk proxy node "$repo_root/dist/bin/codewiki.js" init --tool claude-code,codex)" && printf '%s\n' "$output" | grep -q 'claude-code adapter:'`
- `printf '%s\n' "$output" | grep -q 'shared-skills adapter:'`
- `printf '%s\n' "$output" | grep -q 'Tool-specific integrations pending:'`
- `printf '%s\n' "$output" | grep -q 'codex (shared skills installed; hooks and instructions remain pending)'`
- `test -f .claude/skills/codewiki-ingest/SKILL.md`
- `test -f .agents/skills/codewiki-ingest/SKILL.md`
</verification>

<success_criteria>
Running `init --tool claude-code,codex` installs both skill trees in one invocation and reports that Codex's shared skills are installed while its later hook/instruction integrations are still pending future phases.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.2-adapter-skill-install-paths-inserted/04.1.2-03-SUMMARY.md`
</output>
