---
phase: 04.1.2-adapter-skill-install-paths-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/adapters/claude.ts
autonomous: true
requirements:
  - SM-02
must_haves:
  truths:
    - "`claude-code` installs the eight canonical skill directories to `.claude/skills/codewiki-<name>/SKILL.md`"
    - "Claude installs continue to create `.claude/agents`, `.codewiki/hooks`, `.claude/settings.json`, and `CLAUDE.md` exactly as before"
    - "Claude-only installs do not create `.agents/skills` as a side effect of the Claude adapter"
  artifacts:
    - path: "src/lib/adapters/claude.ts"
      provides: "Claude adapter wired to canonical skill sources and the Claude-only skill tree"
      contains: ".claude/skills"
  key_links:
    - from: "src/lib/adapters/claude.ts"
      to: "src/templates/skills/codewiki-ingest/SKILL.md"
      via: "copyAssetDirectory reads from templateDir/skills into `.claude/skills`"
      pattern: "path.join\\(options.templateDir, \"skills\"\\)"
    - from: "src/lib/adapters/claude.ts"
      to: ".claude/settings.json"
      via: "mergeSettings remains the Claude hook/config write surface"
      pattern: "mergeSettings"
---

<objective>
Repoint the Claude adapter from legacy command files to the canonical skill tree under `.claude/skills`.

Purpose: Deliver SM-02 by making `claude-code` install the eight canonical `SKILL.md` directories without disturbing the existing Claude agent, hook, settings, or instruction behavior established in Phase 4.
Output: `src/lib/adapters/claude.ts` copies from `dist/templates/skills` into `.claude/skills/codewiki-<name>/SKILL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04-claude-code-adapter-init-command/04-02-SUMMARY.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-01-SUMMARY.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-04-SUMMARY.md
@docs/codewiki-project-v2.md
@docs/skills-migration-handoff.md
@docs/skills/wiki.md
@src/lib/adapters/claude.ts
@src/lib/adapters/base.ts
@src/templates/skills/codewiki-ingest/SKILL.md
@src/templates/skills/codewiki-process/SKILL.md

<interfaces>
From src/lib/adapters/types.ts:
```typescript
export interface AdapterInstallOptions {
  root: string;
  projectName: string;
  force: boolean;
  templateDir: string;
}

export interface ToolAdapter {
  tool: SupportedTool;
  install(options: AdapterInstallOptions): Promise<ReportEntry[]>;
}
```

From src/lib/adapters/base.ts:
```typescript
export async function copyTemplateDir(
  srcDir: string,
  destDir: string,
  force: boolean,
  displayRoot: string
): Promise<ReportEntry[]>;

export async function chmodExecutable(filePath: string): Promise<void>;
```

Current Claude adapter install order:
```typescript
await Promise.all([
  ensureDir(options.root, ".claude/commands/codewiki"),
  ensureDir(options.root, ".claude/agents"),
  ensureDir(options.root, ".codewiki/hooks")
]);

report.push(
  ...(await this.copyAssetDirectory(
    path.join(options.templateDir, "claude", "commands", "codewiki"),
    ensureInsideRoot(options.root, ".claude/commands/codewiki"),
    options
  ))
);
```
Only the skill source/target paths should move in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint Claude skill copying to the canonical skill source and target tree</name>
  <files>src/lib/adapters/claude.ts</files>
  <read_first>
    - src/lib/adapters/claude.ts
    - src/lib/adapters/base.ts
    - src/templates/skills/codewiki-ingest/SKILL.md
    - src/templates/skills/codewiki-process/SKILL.md
  </read_first>
  <action>
Update `ClaudeCodeAdapter.install()` so the skill-copy section uses the canonical Phase 04.1.1 source tree and Claude's skill destination per D-01, D-02, and D-03:

- Create `.claude/skills` instead of `.claude/commands/codewiki`
- Copy from `path.join(options.templateDir, "skills")`
- Target `ensureInsideRoot(options.root, ".claude/skills")`
- Preserve the directory-per-skill layout so the installed result is `.claude/skills/codewiki-<name>/SKILL.md`

Do not read from `claude/commands/codewiki` anymore, and do not generate or transform skill content in code. This plan consumes the canonical skill directories created in Phase 04.1.1.
  </action>
  <verify>
    <automated>repo_root="$PWD" && rtk npm run build && tmpdir="$(mktemp -d)" && cd "$tmpdir" && rtk proxy node "$repo_root/dist/bin/codewiki.js" init --tool claude-code >/tmp/codewiki-04.1.2-01.out && test "$(find .claude/skills -mindepth 1 -maxdepth 1 -type d | wc -l)" -eq 8 && test -f .claude/skills/codewiki-ingest/SKILL.md && test -f .claude/skills/codewiki-process/SKILL.md</automated>
  </verify>
  <done>The Claude adapter installs eight skill directories under `.claude/skills/` from the canonical skill templates.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve Claude-specific non-skill assets and single-tree behavior</name>
  <files>src/lib/adapters/claude.ts</files>
  <read_first>
    - src/lib/adapters/claude.ts
    - .planning/phases/04-claude-code-adapter-init-command/04-02-SUMMARY.md
  </read_first>
  <action>
Keep the rest of `ClaudeCodeAdapter` behavior unchanged per D-04:

- Continue installing `.claude/agents` from `dist/templates/claude/agents`
- Continue installing shared hook scripts to `.codewiki/hooks` and keep the `session-end.sh` reason text
- Keep `.claude/settings.json` merging and `CLAUDE.md` marker merging unchanged
- Do not add any `.agents/skills` writes in the Claude adapter; Claude-only installs must remain a single-tree install per D-02

If refactoring is needed while changing the skill path, keep it minimal and localized to `src/lib/adapters/claude.ts`.
  </action>
  <verify>
    <automated>repo_root="$PWD" && rtk npm run build && tmpdir="$(mktemp -d)" && cd "$tmpdir" && rtk proxy node "$repo_root/dist/bin/codewiki.js" init --tool claude-code >/tmp/codewiki-04.1.2-01.out && test -f .claude/agents/codewiki-verifier.md && test -f .codewiki/hooks/session-end.sh && test -f .claude/settings.json && test -f CLAUDE.md && test ! -e .agents/skills</automated>
  </verify>
  <done>Claude-only installs still include agents, hooks, settings, and instructions, and they do not create a redundant `.agents/skills` tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical skill templates -> Claude project tree | Installer copies LLM-authored skill files into a live project and must target only the intended Claude skill directory |
| Claude adapter -> Claude config files | The skill-path migration must not disturb existing hook wiring or instruction merge behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.2-01 | T | `src/lib/adapters/claude.ts` skill path migration | mitigate | Replace only the command source/target paths with `templateDir/skills` and `.claude/skills`; reuse existing `ensureInsideRoot` and `copyTemplateDir` helpers |
| T-04.1.2-02 | T | Claude config/hook surfaces | mitigate | Leave `mergeSettings()`, `mergeInstructions()`, hook copy, and agent copy logic unchanged while verifying their outputs still exist after `init --tool claude-code` |
</threat_model>

<verification>
- `repo_root="$PWD" && rtk npm run build && tmpdir="$(mktemp -d)" && cd "$tmpdir" && rtk proxy node "$repo_root/dist/bin/codewiki.js" init --tool claude-code >/tmp/codewiki-04.1.2-01.out && test "$(find .claude/skills -mindepth 1 -maxdepth 1 -type d | wc -l)" -eq 8`
- `test -f .claude/skills/codewiki-ingest/SKILL.md`
- `test -f .claude/agents/codewiki-verifier.md`
- `test -f .codewiki/hooks/session-end.sh`
- `test -f .claude/settings.json`
- `test -f CLAUDE.md`
- `test ! -e .agents/skills`
</verification>

<success_criteria>
Running `init --tool claude-code` produces the Claude skill tree under `.claude/skills/` for all eight CodeWiki skills while preserving the existing Claude agents, hooks, settings merge, and instruction merge behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.2-adapter-skill-install-paths-inserted/04.1.2-01-SUMMARY.md`
</output>
