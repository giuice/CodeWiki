---
phase: 04.1.2-adapter-skill-install-paths-inserted
verified: 2026-04-12T08:15:32-03:00
status: passed
score: 7/7
overrides_applied: 0
---

# Phase 04.1.2: Adapter Skill Install Paths Verification Report

**Phase Goal:** Installer code copies the shared skill templates into the correct Claude and non-Claude skill trees without changing hook or agent behavior.
**Verified:** 2026-04-12
**Status:** PASSED
**Re-verification:** No — initial verification after execution

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Claude installs now copy the eight canonical skills into `.claude/skills/codewiki-<name>/SKILL.md` | VERIFIED | `src/lib/adapters/claude.ts` now copies from `templateDir/skills` into `.claude/skills`; plan 04.1.2-01 verification confirmed 8 installed skill directories |
| 2 | Claude-only installs still create agents, hooks, `.claude/settings.json`, and `CLAUDE.md` without creating `.agents/skills` | VERIFIED | `src/lib/adapters/claude.ts` preserves the existing agent/hook/settings/instruction merge paths; plan 04.1.2-01 verification confirmed all expected Claude surfaces and no `.agents/skills` |
| 3 | Codex, Copilot, and OpenCode selections install one shared `.agents/skills` tree from the canonical skill templates | VERIFIED | `src/lib/adapters/shared-skills.ts` copies `templateDir/skills` into `.agents/skills`; plan 04.1.2-02 verification confirmed 8 installed skill directories and no `.claude/skills` for non-Claude selections |
| 4 | Mixed non-Claude selections no longer fall back to blanket unsupported reporting | VERIFIED | `src/lib/adapters/index.ts` routes `codex`, `copilot`, and `opencode` through a deduplicated shared-skills adapter; `test/init.test.ts` verifies the updated combined-selection reporting |
| 5 | Combined selections such as `claude-code,codex` install both skill trees in a single `init` run | VERIFIED | `src/commands/init.ts` executes both adapters returned by `resolveAdapters()`; plan 04.1.2-03 verification confirmed both `.claude/skills/codewiki-ingest/SKILL.md` and `.agents/skills/codewiki-ingest/SKILL.md` |
| 6 | `init` now reports installed surfaces first and tool-specific integration debt separately | VERIFIED | `src/commands/init.ts` adds a `Tool-specific integrations pending` section only when the shared-skills adapter runs; plan 04.1.2-03 verification confirmed section order `Wiki scaffold -> claude-code adapter -> shared-skills adapter -> Tool-specific integrations pending` |
| 7 | The automated validation gate passes with the skill-path migration in place | VERIFIED | `npm run build` and `npm test` both passed after aligning the installer regression coverage to the new skill surface |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/adapters/claude.ts` | Claude adapter installs skills to `.claude/skills` | VERIFIED | Copies canonical skills, preserves Claude-only non-skill writes |
| `src/lib/adapters/types.ts` | Adapter contract supports a shared-skills section | VERIFIED | Exports `AdapterSection = SupportedTool | "shared-skills"` |
| `src/lib/adapters/index.ts` | Resolver adds one shared-skills adapter for non-Claude selections | VERIFIED | Deduplicates Codex/Copilot/OpenCode shared installs |
| `src/lib/adapters/shared-skills.ts` | Shared `.agents/skills` installer | VERIFIED | Copies the canonical skill tree into `.agents/skills` |
| `src/commands/init.ts` | Combined reporting for Claude + shared skills + pending integrations | VERIFIED | Reports installed adapter sections first, then pending non-Claude integration work |
| `src/commands/__tests__/init.test.ts` | TTY init coverage updated to `.claude/skills` | VERIFIED | Checks the Claude-only prompt path against `SKILL.md` output |
| `test/init.test.ts` | Built CLI integration coverage updated to the skill install surface | VERIFIED | Verifies `.claude/skills`, `.agents/skills`, combined-selection messaging, and force replacement |

### Requirement Traceability

| Requirement | Status | Evidence |
|-------------|--------|----------|
| SM-02 | VERIFIED | `src/lib/adapters/claude.ts` installs canonical skills into `.claude/skills` and retains the existing Claude agents, hooks, settings, and instructions |
| SM-03 | VERIFIED | `src/lib/adapters/shared-skills.ts`, `src/lib/adapters/index.ts`, and `src/commands/init.ts` install `.agents/skills` for non-Claude selections and report the partial-support surface accurately |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/lib/adapters/claude.ts` | `dist/templates/skills` | `copyTemplateDir` | WIRED | Claude adapter reads the shared canonical skill template tree |
| `src/lib/adapters/shared-skills.ts` | `dist/templates/skills` | `copyTemplateDir` | WIRED | Shared non-Claude installer reads the same canonical skill tree |
| `src/lib/adapters/index.ts` | `src/lib/adapters/shared-skills.ts` | `createSharedSkillsAdapter()` | WIRED | Non-Claude selections resolve to a deduplicated shared install section |
| `src/commands/init.ts` | `src/lib/adapters/index.ts` | `resolveAdapters()` | WIRED | `init` orchestrates Claude and shared-skills installs together |
| `src/commands/init.ts` | installer report output | `Tool-specific integrations pending` section | WIRED | Pending work is separated from real installs and not misreported as unsupported |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript build passes | `npm run build` | exit 0 | PASS |
| Full test suite passes | `npm test` | 84 tests passed | PASS |
| Claude-only install creates `.claude/skills` and no `.agents/skills` | `node dist/bin/codewiki.js init --tool claude-code` | 8 Claude skill directories plus agents/hooks/settings/instructions | PASS |
| Non-Claude install creates `.agents/skills` only | `node dist/bin/codewiki.js init --tool codex,copilot` | 8 shared skill directories and no `.claude/skills` | PASS |
| Combined install creates both trees and ordered reporting | `node dist/bin/codewiki.js init --tool claude-code,codex` | Both skill trees created; pending section follows install sections | PASS |

### Anti-Patterns Found

None that block Phase 04.1.2 completion.

### Human Verification Required

None.

### Gaps Summary

No gaps. Phase 04.1.2 goal, must-haves, and requirement IDs are satisfied.

---
_Verified: 2026-04-12_
_Verifier: manual execution against plan must-haves + full automated validation suite_
