---
phase: 04.1.2-adapter-skill-install-paths-inserted
plan: 02
subsystem: installer
tags: [skills, adapters, codex, copilot, opencode]
requires:
  - phase: 04.1.1-skill-template-source-inserted
    provides: canonical skill templates under src/templates/skills
provides:
  - Shared installer for .agents/skills
  - Adapter contract support for a shared-skills report section
  - Deduplicated non-Claude adapter resolution for mixed selections
affects: [04.1.2-03, 04.1.3-skills-regression-coverage, future-non-claude-adapters]
tech-stack:
  added: []
  patterns:
    - Non-Claude shared install surfaces are represented as a dedicated shared-skills adapter section
    - Mixed non-Claude selections install one shared .agents/skills tree on the first matching tool
key-files:
  created:
    - src/lib/adapters/shared-skills.ts
  modified:
    - src/lib/adapters/types.ts
    - src/lib/adapters/index.ts
key-decisions:
  - Represent the shared non-Claude install surface as `shared-skills` instead of pretending Codex, Copilot, or OpenCode already have full adapters.
  - Deduplicate the shared installer inside `resolveAdapters()` so mixed non-Claude selections write `.agents/skills` only once.
patterns-established:
  - Shared installer sections can coexist with tool-specific adapters in the existing reporting pipeline.
  - Canonical skill templates should be copied into .agents/skills directly from the shared skill tree.
requirements-completed: [SM-03]
duration: 3 min
completed: 2026-04-12
---

# Phase 04.1.2 Plan 02: Shared Non-Claude Skill Installer Summary

**Codex, Copilot, and OpenCode selections now install one shared `.agents/skills` tree from the canonical skill templates instead of being reported as unsupported.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-12T08:00:17-03:00
- **Completed:** 2026-04-12T08:03:08-03:00
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Expanded the adapter contract so the install report can surface a dedicated `shared-skills` section.
- Added a reusable shared-skills adapter that copies the canonical skill directories into `.agents/skills`.
- Wired mixed non-Claude selections through one deduplicated shared installer and removed the old unsupported fallback for those tool choices.

## Task Commits

Each task was committed atomically:

1. **Task 1: Expand the adapter contract for a shared skill-install section** - `e94d8b5` (feat)
2. **Task 2: Create the shared `.agents/skills` installer and wire it into adapter resolution** - `13a487f` (feat)

## Files Created/Modified
- `src/lib/adapters/types.ts` - Adds the `AdapterSection` union so the reporter can label `shared-skills` explicitly.
- `src/lib/adapters/shared-skills.ts` - Copies the canonical skill directory tree into `.agents/skills` for non-Claude selections.
- `src/lib/adapters/index.ts` - Deduplicates shared non-Claude installs and stops routing those selections to the unsupported path.

## Decisions Made
- Used a dedicated `shared-skills` adapter label so the install report stays honest about what is implemented now versus what remains future tool-specific work.
- Added the shared installer once at adapter-resolution time, which preserves mixed-selection installs without redundant file copies.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Combined selections can now execute both the Claude adapter and the shared non-Claude skills installer in one run.
- The remaining Plan 03 work is limited to user-facing reporting so shared-skill installs are described accurately.

## Self-Check: PASSED

---
*Phase: 04.1.2-adapter-skill-install-paths-inserted*
*Completed: 2026-04-12*
