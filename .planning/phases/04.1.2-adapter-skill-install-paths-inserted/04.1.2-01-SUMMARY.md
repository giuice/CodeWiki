---
phase: 04.1.2-adapter-skill-install-paths-inserted
plan: 01
subsystem: installer
tags: [claude, skills, installer, adapters]
requires:
  - phase: 04.1.1-skill-template-source-inserted
    provides: canonical skill templates under src/templates/skills
  - phase: 04-claude-code-adapter-init-command
    provides: Claude agent, hook, settings, and instruction merge behavior
provides:
  - Claude adapter install path rewired to .claude/skills
  - Claude-only install surfaces kept isolated from the shared .agents/skills work
  - Canonical shared skill templates used as the Claude source of truth
affects: [04.1.2-02, 04.1.2-03, 04.1.3-skills-regression-coverage]
tech-stack:
  added: []
  patterns:
    - Claude installs read canonical skill directories from dist/templates/skills
    - Claude-only adapter surfaces stay explicit so later non-Claude work does not leak into Claude installs
key-files:
  created: []
  modified:
    - src/lib/adapters/claude.ts
key-decisions:
  - Repoint the Claude adapter to the shared skill template tree instead of the legacy claude/commands/codewiki source.
  - Keep Claude-only writes explicit in constants so agents, hooks, settings, and instructions remain unchanged while .agents/skills stays out of the Claude path.
patterns-established:
  - Claude skill installs should target .claude/skills while reusing the shared canonical skill source tree.
  - Path constants are sufficient to make Claude-only install boundaries obvious without changing behavior.
requirements-completed: [SM-02]
duration: 1 min
completed: 2026-04-12
---

# Phase 04.1.2 Plan 01: Claude Skill Install Path Summary

**Claude installs now copy the eight canonical skill directories into `.claude/skills` while preserving agents, hooks, settings, and instruction merges exactly as before.**

## Performance

- **Duration:** 1 min
- **Started:** 2026-04-12T07:55:36-03:00
- **Completed:** 2026-04-12T07:56:47-03:00
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Repointed the Claude adapter from the legacy command asset tree to the canonical shared skill template tree.
- Verified that `init --tool claude-code` now installs eight `codewiki-*` skill directories under `.claude/skills`.
- Preserved the existing Claude-only agent, hook, settings, and `CLAUDE.md` merge behavior without creating `.agents/skills`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Repoint Claude skill copying to the canonical skill source and target tree** - `c472335` (feat)
2. **Task 2: Preserve Claude-specific non-skill assets and single-tree behavior** - `91b84b5` (refactor)

## Files Created/Modified
- `src/lib/adapters/claude.ts` - Repoints Claude installs to `.claude/skills` and makes the preserved Claude-only write surfaces explicit.

## Decisions Made
- Read the canonical skill directories from `dist/templates/skills` so Claude and later non-Claude installs share one source of truth.
- Kept the rest of the Claude adapter behavior isolated behind explicit path constants instead of broadening the adapter into shared-skill responsibilities.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Claude installs now land on the correct `.claude/skills` surface for the remaining Phase 04.1.2 work.
- The shared non-Claude installer can now target `.agents/skills` without touching Claude-specific behavior.

## Self-Check: PASSED

---
*Phase: 04.1.2-adapter-skill-install-paths-inserted*
*Completed: 2026-04-12*
