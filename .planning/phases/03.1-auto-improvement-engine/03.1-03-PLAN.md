---
phase: 03.1-auto-improvement-engine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/claude/commands/codewiki/ingest.md
  - src/templates/claude/commands/codewiki/lint.md
  - src/templates/claude/commands/codewiki/query.md
autonomous: true
requirements: [ABS-03]
must_haves:
  truths:
    - "ingest.md references wiki/_backlinks.json in its process"
    - "lint.md references wiki/_backlinks.json in its process"
    - "query.md references wiki/_backlinks.json in its process"
  artifacts:
    - path: "src/templates/claude/commands/codewiki/ingest.md"
      provides: "Updated ingest with backlinks reference"
      contains: "_backlinks.json"
    - path: "src/templates/claude/commands/codewiki/lint.md"
      provides: "Updated lint with backlinks reference"
      contains: "_backlinks.json"
    - path: "src/templates/claude/commands/codewiki/query.md"
      provides: "Updated query with backlinks reference"
      contains: "_backlinks.json"
  key_links:
    - from: "ingest.md"
      to: "wiki/_backlinks.json"
      via: "step to update backlinks after ingestion"
      pattern: "_backlinks\\.json"
---

<objective>
Update existing ingest, lint, and query prompts to reference wiki/_backlinks.json so the backlink index stays current across all wiki operations.

Purpose: Without these updates, only absorb and breakdown would know about backlinks. All wiki-modifying commands must maintain the backlink index, and query should use it for relevance ranking.
Output: Three updated prompt files with backlink integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-auto-improvement-engine/03.1-RESEARCH.md

@src/templates/claude/commands/codewiki/ingest.md
@src/templates/claude/commands/codewiki/lint.md
@src/templates/claude/commands/codewiki/query.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add backlinks references to ingest.md</name>
  <files>src/templates/claude/commands/codewiki/ingest.md</files>
  <read_first>
    src/templates/claude/commands/codewiki/ingest.md
  </read_first>
  <action>
Add two backlinks integrations to ingest.md:

**1. In Step 2 (Load current wiki state),** add after the line about reading templates:
```
- Read `wiki/_backlinks.json` to understand which pages are most referenced and interconnected.
```

**2. In Step 6 (Write approved updates),** add after the line about updating wiki/index.md:
```
- Update `wiki/_backlinks.json` by scanning all modified and new pages for `[[wikilink]]` references. Add new backlink entries; do not remove existing ones unless a link was actually deleted.
```

Do not change any other content. Preserve all existing steps and formatting.
  </action>
  <verify>
    <automated>grep -c "_backlinks.json" src/templates/claude/commands/codewiki/ingest.md | grep -q "2" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - ingest.md contains exactly 2 references to `_backlinks.json`
    - One reference is in the "Load current wiki state" section
    - One reference is in the "Write approved updates" section
    - All original content is preserved (no deletions)
  </acceptance_criteria>
  <done>ingest.md reads backlinks during state loading and updates backlinks after approved writes</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add backlinks references to lint.md and query.md</name>
  <files>src/templates/claude/commands/codewiki/lint.md, src/templates/claude/commands/codewiki/query.md</files>
  <read_first>
    src/templates/claude/commands/codewiki/lint.md
    src/templates/claude/commands/codewiki/query.md
  </read_first>
  <action>
**1. Update lint.md:**

In Step 1 (Load the wiki catalog), add after the Glob line:
```
- Read `wiki/_backlinks.json` to identify high-importance pages (many backlinks) vs orphaned pages (zero backlinks).
```

In Step 3 (Detect orphaned pages), add after the existing orphan detection:
```
- Cross-reference with `wiki/_backlinks.json`: pages with zero backlinks AND not in index.md are strong orphan candidates.
```

**2. Update query.md:**

In Step 2 (Read the wiki index first), add after the existing index reading:
```
- Read `wiki/_backlinks.json` to identify high-importance pages. Pages with many backlinks are more likely to contain authoritative answers.
```

In Step 3 (Search for relevant pages), add after the Grep/Glob instructions:
```
- Use `wiki/_backlinks.json` to prioritize pages with higher backlink counts when multiple pages match the query.
```

Do not change any other content in either file.
  </action>
  <verify>
    <automated>grep -q "_backlinks.json" src/templates/claude/commands/codewiki/lint.md && grep -q "_backlinks.json" src/templates/claude/commands/codewiki/query.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - lint.md contains at least 2 references to `_backlinks.json`
    - query.md contains at least 2 references to `_backlinks.json`
    - All original content in both files is preserved
    - lint.md backlinks reference appears in "Load the wiki catalog" and "Detect orphaned pages" steps
    - query.md backlinks reference appears in "Read the wiki index" and "Search for relevant pages" steps
  </acceptance_criteria>
  <done>lint.md uses backlinks for orphan detection and importance ranking; query.md uses backlinks for relevance prioritization</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan only modifies prompt text — no runtime code, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-05 | I (Info Disclosure) | _backlinks.json | accept | Backlinks index contains only page paths — no sensitive data |
</threat_model>

<verification>
- All three files (ingest.md, lint.md, query.md) contain `_backlinks.json` references
- No existing content was removed from any file
- Each file has contextually appropriate backlink usage (read vs update vs rank)
</verification>

<success_criteria>
All three existing prompts reference wiki/_backlinks.json for reading, updating, or ranking — completing the backlink integration across all wiki operations.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-auto-improvement-engine/03.1-03-SUMMARY.md`
</output>
