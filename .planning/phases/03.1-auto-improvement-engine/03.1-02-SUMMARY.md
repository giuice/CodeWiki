---
phase: 03.1-auto-improvement-engine
plan: "02"
subsystem: infra
tags: [hooks, scaffold, backlinks, shell, wiki]
requires:
  - phase: 02-shared-infrastructure
    provides: scaffold-and-template-copy
  - phase: 03-prompt-templates-and-hook-scripts
    provides: hook-script-foundation
provides:
  - scaffolded backlinks index file
  - session-end hook for lightweight session capture
  - structured post-verify change context for wiki updates
affects: [phase-4-installer, hook-wiring, auto-improvement-loop]
tech-stack:
  added: []
  patterns: [structured-hook-delimiters, lightweight-session-capture, scaffolded-backlink-state]
key-files:
  created:
    - src/templates/hooks/session-end.sh
    - src/templates/__tests__/session-end.test.ts
  modified:
    - src/templates/scaffold.ts
    - src/lib/__tests__/scaffold.test.ts
    - src/templates/hooks/post-verify.sh
    - src/templates/__tests__/hooks.test.ts
key-decisions:
  - Initialize `wiki/_backlinks.json` as an empty JSON object during scaffold so prompt logic can assume the file exists.
  - Keep both hooks read-only and delimiter-based so the AI runtime can consume structured context without risking repo writes.
patterns-established:
  - Hook templates use explicit start/end delimiters for machine-parsable context blocks.
  - Session-end collection prefers a cheap git diff summary over heavy runtime analysis.
requirements-completed: [ABS-03, ABS-04, ABS-05]
duration: 1m
completed: 2026-04-08
---

# Phase 03.1 Plan 02: Hooks and Scaffold Summary

**Backlinks now scaffold with every wiki and the hook layer emits structured session and post-verify context for the auto-improvement loop**

## Performance

- **Duration:** 1 min
- **Started:** 2026-04-08T13:35:03-03:00
- **Completed:** 2026-04-08T13:35:44-03:00
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Added `_backlinks.json` to the scaffold output so every new CodeWiki install starts with backlink state.
- Added `session-end.sh` as a lightweight hook that summarizes recent git changes with stable delimiters.
- Upgraded `post-verify.sh` from passive reminder output to structured `CODEWIKI_CHANGE_CONTEXT` output that can drive a wiki-updater workflow.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add _backlinks.json to scaffold and create session-end.sh** - `a2b8e17` (feat)
2. **Task 2: Update post-verify.sh to output structured change context** - `eea2519` (feat)

## Files Created/Modified

- `src/templates/scaffold.ts` - Adds `wiki/_backlinks.json` to the scaffolded file set.
- `src/lib/__tests__/scaffold.test.ts` - Verifies new projects receive the backlinks file.
- `src/templates/hooks/session-end.sh` - Emits a diff-stat summary of recent session changes and exits safely under all conditions.
- `src/templates/__tests__/session-end.test.ts` - Verifies markers, exit behavior, and shellcheck coverage for the new session hook.
- `src/templates/hooks/post-verify.sh` - Emits structured wiki-entity context with an explicit `/codewiki-absorb` follow-up action.
- `src/templates/__tests__/hooks.test.ts` - Updates hook assertions for the new structured post-verify contract.

## Decisions Made

- Kept the hook payloads as plain structured text instead of JSON to stay POSIX-friendly and avoid adding parsing complexity to the templates.
- Used `HEAD~1`, unstaged, and staged diff fallbacks in the session hook so it remains useful across clean, dirty, and partially staged sessions.

## Deviations from Plan

### Execution Notes

- Copilot runtime compatibility required inline execution instead of spawned executor agents, but the task boundaries and outputs stayed identical to the plan.

## Issues Encountered

None.

## User Setup Required

None - these are bundled template assets only.

## Next Phase Readiness

- Adapter work can now wire session-end and post-verify hooks against concrete, tested template files.
- Existing prompts still needed backlink awareness so every wiki command uses the same index consistently.

---
*Phase: 03.1-auto-improvement-engine*
*Completed: 2026-04-08*