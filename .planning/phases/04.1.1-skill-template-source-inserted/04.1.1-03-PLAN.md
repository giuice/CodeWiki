---
phase: 04.1.1-skill-template-source-inserted
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/skills/codewiki-breakdown/SKILL.md
  - src/templates/skills/codewiki-prd/SKILL.md
autonomous: true
requirements:
  - SM-01
must_haves:
  truths:
    - "The canonical breakdown skill exists at src/templates/skills/codewiki-breakdown/SKILL.md with skill metadata and preserved backlink-ranked gap-finding behavior"
    - "The canonical PRD skill exists at src/templates/skills/codewiki-prd/SKILL.md with skill metadata and preserved mentorship/fast-mode PRD workflow"
    - "The migration preserves current Task usage and approval/questioning behavior instead of redesigning the prompt contracts"
  artifacts:
    - path: "src/templates/skills/codewiki-breakdown/SKILL.md"
      provides: "Canonical breakdown skill source"
      contains: "top 5-10 candidates"
    - path: "src/templates/skills/codewiki-prd/SKILL.md"
      provides: "Canonical PRD skill source"
      contains: "Use Task to spawn parallel research agents"
  key_links:
    - from: "src/templates/skills/codewiki-breakdown/SKILL.md"
      to: "src/templates/claude/commands/codewiki/breakdown.md"
      via: "frontmatter upgrade while preserving the breakdown prompt body"
      pattern: "top 5-10 candidates"
    - from: "src/templates/skills/codewiki-prd/SKILL.md"
      to: "src/templates/claude/commands/codewiki/prd.md"
      via: "frontmatter upgrade while preserving the PRD prompt body"
      pattern: "Use Task to spawn parallel research agents"
---

<objective>
Create the new canonical skill-source files for `codewiki-breakdown` and `codewiki-prd`.

Purpose: Migrate the gap-finding and PRD-generation workflows into skill-native template locations while keeping the current prompt contracts intact.
Output: Two new `SKILL.md` files under `src/templates/skills/` for breakdown and PRD.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-CONTEXT.md
@docs/codewiki-project-v2.md
@docs/skills/wiki.md
@src/templates/claude/commands/codewiki/breakdown.md
@src/templates/claude/commands/codewiki/prd.md

<interfaces>
Target skill metadata for this pair:
- breakdown: name = codewiki-breakdown; description = Find undocumented entities referenced in wiki and propose new pages; argument-hint = ""; allowed-tools = [Read, Glob, Grep, Bash]
- prd: name = codewiki-prd; description = Generate a Product Requirements Document from a feature idea; argument-hint = <feature-description>; allowed-tools = [Read, Write, Edit, Glob, Grep, Bash, Task]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical breakdown skill source</name>
  <files>src/templates/skills/codewiki-breakdown/SKILL.md</files>
  <read_first>
    - src/templates/claude/commands/codewiki/breakdown.md
    - docs/skills/wiki.md
  </read_first>
  <action>
Create `src/templates/skills/codewiki-breakdown/SKILL.md`.

Use this frontmatter exactly:
- `name: codewiki-breakdown`
- `description: Find undocumented entities referenced in wiki and propose new pages`
- `argument-hint: ""`
- `allowed-tools: [Read, Glob, Grep, Bash]`

Then migrate the existing breakdown prompt body from `src/templates/claude/commands/codewiki/breakdown.md` into the new file. Preserve the backlink-loading logic, the ranking workflow, the top-5-to-10 candidate batch proposal, the approval gate, and the guardrails.

Do not add new arguments or any write behavior beyond the existing approved-page creation flow already described in the prompt body.
  </action>
  <verify>
    <automated>test -f src/templates/skills/codewiki-breakdown/SKILL.md && grep -q '^name: codewiki-breakdown$' src/templates/skills/codewiki-breakdown/SKILL.md && grep -q '^argument-hint: ""$' src/templates/skills/codewiki-breakdown/SKILL.md && grep -q 'top 5-10 candidates' src/templates/skills/codewiki-breakdown/SKILL.md</automated>
  </verify>
  <done>`src/templates/skills/codewiki-breakdown/SKILL.md` exists with skill metadata and the preserved breakdown workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical PRD skill source</name>
  <files>src/templates/skills/codewiki-prd/SKILL.md</files>
  <read_first>
    - src/templates/claude/commands/codewiki/prd.md
    - docs/skills/wiki.md
  </read_first>
  <action>
Create `src/templates/skills/codewiki-prd/SKILL.md`.

Use this frontmatter exactly:
- `name: codewiki-prd`
- `description: Generate a Product Requirements Document from a feature idea`
- `argument-hint: <feature-description>`
- `allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Task]`

Then migrate the existing PRD prompt body from `src/templates/claude/commands/codewiki/prd.md` into the new file. Preserve the mentorship-vs-fast mode split, the clarifying-question flow, the `Task`-based parallel research step, the PRD structure, and the save path contract.

Do not redesign the prompt into a shorter or more generic skill in this sub-phase.
  </action>
  <verify>
    <automated>test -f src/templates/skills/codewiki-prd/SKILL.md && grep -q '^name: codewiki-prd$' src/templates/skills/codewiki-prd/SKILL.md && grep -q '^argument-hint: <feature-description>$' src/templates/skills/codewiki-prd/SKILL.md && grep -q 'Use `Task` to spawn parallel research agents' src/templates/skills/codewiki-prd/SKILL.md</automated>
  </verify>
  <done>`src/templates/skills/codewiki-prd/SKILL.md` exists with skill metadata and the preserved PRD workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy command template -> canonical skill file | Prompt content is copied into a new source-of-truth file and must not lose required approval or questioning steps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.1-05 | T | breakdown skill migration | mitigate | Preserve the current backlink-ranked proposal flow and approval gate rather than shortening it into a generic skill |
| T-04.1.1-06 | T | PRD skill migration | mitigate | Preserve the existing `Task` research step and mentorship/fast-mode behavior |
</threat_model>

<verification>
- `test -f src/templates/skills/codewiki-breakdown/SKILL.md`
- `test -f src/templates/skills/codewiki-prd/SKILL.md`
- `grep -q '^name: codewiki-breakdown$' src/templates/skills/codewiki-breakdown/SKILL.md`
- `grep -q '^name: codewiki-prd$' src/templates/skills/codewiki-prd/SKILL.md`
- `grep -q 'top 5-10 candidates' src/templates/skills/codewiki-breakdown/SKILL.md`
- `grep -q 'Use `Task` to spawn parallel research agents' src/templates/skills/codewiki-prd/SKILL.md`
</verification>

<success_criteria>
The breakdown and PRD skills exist under `src/templates/skills/` with required skill metadata and no behavioral regression from the command templates.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-03-SUMMARY.md`
</output>
