---
phase: 04.1.1-skill-template-source-inserted
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/skills/codewiki-tasks/SKILL.md
  - src/templates/skills/codewiki-process/SKILL.md
autonomous: true
requirements:
  - SM-01
must_haves:
  truths:
    - "The canonical tasks skill exists at src/templates/skills/codewiki-tasks/SKILL.md with skill metadata and preserved PRD-to-task-list behavior"
    - "The canonical process skill exists at src/templates/skills/codewiki-process/SKILL.md with skill metadata and preserved one-sub-task-at-a-time execution behavior"
    - "The migration preserves the existing mentorship gate and fast-mode behavior instead of simplifying the workflow"
  artifacts:
    - path: "src/templates/skills/codewiki-tasks/SKILL.md"
      provides: "Canonical tasks skill source"
      contains: "Respond with Go to proceed"
    - path: "src/templates/skills/codewiki-process/SKILL.md"
      provides: "Canonical process skill source"
      contains: "execute one sub-task at a time"
  key_links:
    - from: "src/templates/skills/codewiki-tasks/SKILL.md"
      to: "src/templates/claude/commands/codewiki/tasks.md"
      via: "frontmatter upgrade while preserving the tasks prompt body"
      pattern: "Respond with Go to proceed"
    - from: "src/templates/skills/codewiki-process/SKILL.md"
      to: "src/templates/claude/commands/codewiki/process.md"
      via: "frontmatter upgrade while preserving the process prompt body"
      pattern: "one sub-task at a time"
---

<objective>
Create the new canonical skill-source files for `codewiki-tasks` and `codewiki-process`.

Purpose: Finish the skill-source migration with the execution-oriented prompts while preserving the existing mentorship gate, fast-mode behavior, and task-processing cadence.
Output: Two new `SKILL.md` files under `src/templates/skills/` for tasks and process.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-CONTEXT.md
@docs/codewiki-project-v2.md
@docs/skills/wiki.md
@src/templates/claude/commands/codewiki/tasks.md
@src/templates/claude/commands/codewiki/process.md

<interfaces>
Target skill metadata for this pair:
- tasks: name = codewiki-tasks; description = Generate implementation tasks from a PRD; argument-hint = <prd-file-path>; allowed-tools = [Read, Write, Edit, Glob, Grep, Bash, Task]
- process: name = codewiki-process; description = Execute tasks from a task list one sub-task at a time; argument-hint = <task-file-path>; allowed-tools = [Read, Write, Edit, Glob, Grep, Bash, Task]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical tasks skill source</name>
  <files>src/templates/skills/codewiki-tasks/SKILL.md</files>
  <read_first>
    - src/templates/claude/commands/codewiki/tasks.md
    - docs/skills/wiki.md
  </read_first>
  <action>
Create `src/templates/skills/codewiki-tasks/SKILL.md`.

Use this frontmatter exactly:
- `name: codewiki-tasks`
- `description: Generate implementation tasks from a PRD`
- `argument-hint: <prd-file-path>`
- `allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Task]`

Then migrate the existing tasks prompt body from `src/templates/claude/commands/codewiki/tasks.md` into the new file. Preserve the PRD resolution flow, the mentorship-vs-fast mode split, the parent-task gate that waits for `Go`, the `Task` two-agent split, and the save path contract.

Do not shorten or flatten the mentorship workflow in this sub-phase.
  </action>
  <verify>
    <automated>test -f src/templates/skills/codewiki-tasks/SKILL.md && grep -q '^name: codewiki-tasks$' src/templates/skills/codewiki-tasks/SKILL.md && grep -q '^argument-hint: <prd-file-path>$' src/templates/skills/codewiki-tasks/SKILL.md && grep -q "Respond with 'Go' to proceed" src/templates/skills/codewiki-tasks/SKILL.md</automated>
  </verify>
  <done>`src/templates/skills/codewiki-tasks/SKILL.md` exists with skill metadata and the preserved tasks workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical process skill source</name>
  <files>src/templates/skills/codewiki-process/SKILL.md</files>
  <read_first>
    - src/templates/claude/commands/codewiki/process.md
    - docs/skills/wiki.md
  </read_first>
  <action>
Create `src/templates/skills/codewiki-process/SKILL.md`.

Use this frontmatter exactly:
- `name: codewiki-process`
- `description: Execute tasks from a task list one sub-task at a time`
- `argument-hint: <task-file-path>`
- `allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Task]`

Then migrate the existing process prompt body from `src/templates/claude/commands/codewiki/process.md` into the new file. Preserve the task-list resolution flow, the mentorship-vs-fast mode split, the one-sub-task execution cadence, the task-list update behavior, and the explicit boundary against silently skipping blockers.

Do not redesign this into a generic executor skill in this sub-phase.
  </action>
  <verify>
    <automated>test -f src/templates/skills/codewiki-process/SKILL.md && grep -q '^name: codewiki-process$' src/templates/skills/codewiki-process/SKILL.md && grep -q '^argument-hint: <task-file-path>$' src/templates/skills/codewiki-process/SKILL.md && grep -q 'one sub-task at a time' src/templates/skills/codewiki-process/SKILL.md</automated>
  </verify>
  <done>`src/templates/skills/codewiki-process/SKILL.md` exists with skill metadata and the preserved process workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy command template -> canonical skill file | Prompt content is copied into a new source-of-truth file and must not lose execution control points |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.1-07 | T | tasks skill migration | mitigate | Preserve the `Go` checkpoint and current task-generation structure rather than simplifying it |
| T-04.1.1-08 | T | process skill migration | mitigate | Preserve the one-sub-task cadence and blocker-reporting rules from the current prompt |
</threat_model>

<verification>
- `test -f src/templates/skills/codewiki-tasks/SKILL.md`
- `test -f src/templates/skills/codewiki-process/SKILL.md`
- `grep -q '^name: codewiki-tasks$' src/templates/skills/codewiki-tasks/SKILL.md`
- `grep -q '^name: codewiki-process$' src/templates/skills/codewiki-process/SKILL.md`
- `grep -q "Respond with 'Go' to proceed" src/templates/skills/codewiki-tasks/SKILL.md`
- `grep -q 'one sub-task at a time' src/templates/skills/codewiki-process/SKILL.md`
</verification>

<success_criteria>
The tasks and process skills exist under `src/templates/skills/` with required skill metadata and no behavioral regression from the command templates.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-04-SUMMARY.md`
</output>
