---
phase: 04.1.1-skill-template-source-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/skills/codewiki-ingest/SKILL.md
  - src/templates/skills/codewiki-query/SKILL.md
autonomous: true
requirements:
  - SM-01
must_haves:
  truths:
    - "The canonical ingest skill exists at src/templates/skills/codewiki-ingest/SKILL.md with skill frontmatter and the current ingest workflow preserved"
    - "The canonical query skill exists at src/templates/skills/codewiki-query/SKILL.md with skill frontmatter and the current read-only query workflow preserved"
    - "Neither migration invents new behavior beyond the skill metadata required for discovery"
  artifacts:
    - path: "src/templates/skills/codewiki-ingest/SKILL.md"
      provides: "Canonical ingest skill source"
      contains: "Wait for user approval before writing any file"
    - path: "src/templates/skills/codewiki-query/SKILL.md"
      provides: "Canonical query skill source"
      contains: "Do not edit files from this command"
  key_links:
    - from: "src/templates/skills/codewiki-ingest/SKILL.md"
      to: "src/templates/claude/commands/codewiki/ingest.md"
      via: "frontmatter upgrade while preserving the ingest prompt body"
      pattern: "Wait for user approval before writing any file"
    - from: "src/templates/skills/codewiki-query/SKILL.md"
      to: "src/templates/claude/commands/codewiki/query.md"
      via: "frontmatter upgrade while preserving the query prompt body"
      pattern: "Read wiki/index.md before anything else"
---

<objective>
Create the new canonical skill-source files for `codewiki-ingest` and `codewiki-query`.

Purpose: Start the skill-source migration with the two wiki-reading entrypoints while keeping behavior identical to the shipped command templates.
Output: Two new `SKILL.md` files under `src/templates/skills/` for ingest and query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-CONTEXT.md
@docs/codewiki-project-v2.md
@docs/skills/wiki.md
@src/templates/claude/commands/codewiki/ingest.md
@src/templates/claude/commands/codewiki/query.md

<interfaces>
Target skill metadata for this pair:
- ingest: name = codewiki-ingest; description = Digest a raw source document into wiki pages; argument-hint = <source-file-path>; allowed-tools = [Read, Write, Edit, Glob, Grep, Bash]
- query: name = codewiki-query; description = Search wiki and synthesize an answer from wiki pages; argument-hint = <question>; allowed-tools = [Read, Glob, Grep, Bash]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical ingest skill source</name>
  <files>src/templates/skills/codewiki-ingest/SKILL.md</files>
  <read_first>
    - src/templates/claude/commands/codewiki/ingest.md
    - docs/skills/wiki.md
  </read_first>
  <action>
Create `src/templates/skills/codewiki-ingest/SKILL.md`.

Use this frontmatter exactly:
- `name: codewiki-ingest`
- `description: Digest a raw source document into wiki pages`
- `argument-hint: <source-file-path>`
- `allowed-tools: [Read, Write, Edit, Glob, Grep, Bash]`

Then migrate the existing ingest prompt body from `src/templates/claude/commands/codewiki/ingest.md` into the new file. Preserve the current `# CodeWiki Ingest` heading, the `purpose` and `process` XML blocks, the wiki-first loading sequence, the approval gate, and the guardrails.

Do not edit or delete the legacy command file in this sub-phase. Per D-01.4, this phase only creates the new canonical source file.
  </action>
  <verify>
    <automated>test -f src/templates/skills/codewiki-ingest/SKILL.md && grep -q '^name: codewiki-ingest$' src/templates/skills/codewiki-ingest/SKILL.md && grep -q '^argument-hint: <source-file-path>$' src/templates/skills/codewiki-ingest/SKILL.md && grep -q 'Wait for user approval before writing any file' src/templates/skills/codewiki-ingest/SKILL.md</automated>
  </verify>
  <done>`src/templates/skills/codewiki-ingest/SKILL.md` exists with skill metadata and the preserved ingest workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical query skill source</name>
  <files>src/templates/skills/codewiki-query/SKILL.md</files>
  <read_first>
    - src/templates/claude/commands/codewiki/query.md
    - docs/skills/wiki.md
  </read_first>
  <action>
Create `src/templates/skills/codewiki-query/SKILL.md`.

Use this frontmatter exactly:
- `name: codewiki-query`
- `description: Search wiki and synthesize an answer from wiki pages`
- `argument-hint: <question>`
- `allowed-tools: [Read, Glob, Grep, Bash]`

Then migrate the existing query prompt body from `src/templates/claude/commands/codewiki/query.md` into the new file. Preserve the current `# CodeWiki Query` heading, the `purpose` and `process` XML blocks, the instruction to read `wiki/index.md` first, and the explicit read-only boundary.

Do not edit or delete the legacy command file in this sub-phase.
  </action>
  <verify>
    <automated>test -f src/templates/skills/codewiki-query/SKILL.md && grep -q '^name: codewiki-query$' src/templates/skills/codewiki-query/SKILL.md && grep -q '^argument-hint: <question>$' src/templates/skills/codewiki-query/SKILL.md && grep -q 'Do not edit files from this command' src/templates/skills/codewiki-query/SKILL.md</automated>
  </verify>
  <done>`src/templates/skills/codewiki-query/SKILL.md` exists with skill metadata and the preserved query workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy command template -> canonical skill file | Prompt content is copied into a new source-of-truth file and must not gain unintended behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.1-01 | T | ingest skill migration | mitigate | Preserve the existing prompt body and tool list; only add skill metadata in frontmatter |
| T-04.1.1-02 | T | query skill migration | mitigate | Preserve the existing read-only query behavior and do not add write-capable instructions |
</threat_model>

<verification>
- `test -f src/templates/skills/codewiki-ingest/SKILL.md`
- `test -f src/templates/skills/codewiki-query/SKILL.md`
- `grep -q '^name: codewiki-ingest$' src/templates/skills/codewiki-ingest/SKILL.md`
- `grep -q '^name: codewiki-query$' src/templates/skills/codewiki-query/SKILL.md`
- `grep -q 'Wait for user approval before writing any file' src/templates/skills/codewiki-ingest/SKILL.md`
- `grep -q 'Do not edit files from this command' src/templates/skills/codewiki-query/SKILL.md`
</verification>

<success_criteria>
The ingest and query skills exist under `src/templates/skills/` with the required skill metadata and no behavioral regression from the command templates.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-01-SUMMARY.md`
</output>
