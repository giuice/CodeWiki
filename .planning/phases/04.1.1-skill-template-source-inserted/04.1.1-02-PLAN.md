---
phase: 04.1.1-skill-template-source-inserted
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/skills/codewiki-lint/SKILL.md
  - src/templates/skills/codewiki-absorb/SKILL.md
autonomous: true
requirements:
  - SM-01
must_haves:
  truths:
    - "The canonical lint skill exists at src/templates/skills/codewiki-lint/SKILL.md with skill metadata and preserved read-only lint behavior"
    - "The canonical absorb skill exists at src/templates/skills/codewiki-absorb/SKILL.md with skill metadata and preserved git-diff-driven approval-gated behavior"
    - "Commands without an explicit required argument omit argument-hint instead of inventing invocation semantics"
  artifacts:
    - path: "src/templates/skills/codewiki-lint/SKILL.md"
      provides: "Canonical lint skill source"
      contains: "This is a read-only lint pass"
    - path: "src/templates/skills/codewiki-absorb/SKILL.md"
      provides: "Canonical absorb skill source"
      contains: "anti-cramming"
  key_links:
    - from: "src/templates/skills/codewiki-lint/SKILL.md"
      to: "src/templates/claude/commands/codewiki/lint.md"
      via: "frontmatter upgrade while preserving the lint prompt body"
      pattern: "This is a read-only lint pass"
    - from: "src/templates/skills/codewiki-absorb/SKILL.md"
      to: "src/templates/claude/commands/codewiki/absorb.md"
      via: "frontmatter upgrade while preserving git-diff and approval-gated absorb behavior"
      pattern: "anti-cramming"
---

<objective>
Create the new canonical skill-source files for `codewiki-lint` and `codewiki-absorb`.

Purpose: Migrate the wiki health-check and code-change absorption workflows into skill-native template locations without widening their behavior.
Output: Two new `SKILL.md` files under `src/templates/skills/` for lint and absorb.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-CONTEXT.md
@docs/codewiki-project-v2.md
@docs/skills/wiki.md
@src/templates/claude/commands/codewiki/lint.md
@src/templates/claude/commands/codewiki/absorb.md

<interfaces>
Commands without required arguments should omit `argument-hint` in this sub-phase.

Keep the existing tool allowances intact:
```yaml
allowed-tools: [Read, Glob, Grep, Bash]
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical lint skill source</name>
  <files>src/templates/skills/codewiki-lint/SKILL.md</files>
  <read_first>
    - src/templates/claude/commands/codewiki/lint.md
    - docs/skills/wiki.md
  </read_first>
  <action>
Create `src/templates/skills/codewiki-lint/SKILL.md`.

Use this frontmatter exactly:
- `name: codewiki-lint`
- `description: Check wiki for contradictions, orphans, stale content, and drift`
- `allowed-tools: [Read, Glob, Grep, Bash]`

Then migrate the existing lint prompt body from `src/templates/claude/commands/codewiki/lint.md` into the new file. Preserve the `# CodeWiki Lint` heading, the `purpose` and `process` XML blocks, the severity-grouped report structure, and the explicit read-only boundary.

Do not add optional scope arguments or any write behavior in this sub-phase.
  </action>
  <verify>
    <automated>test -f src/templates/skills/codewiki-lint/SKILL.md && grep -q '^name: codewiki-lint$' src/templates/skills/codewiki-lint/SKILL.md && ! grep -q '^argument-hint:' src/templates/skills/codewiki-lint/SKILL.md && grep -q 'This is a read-only lint pass' src/templates/skills/codewiki-lint/SKILL.md</automated>
  </verify>
  <done>`src/templates/skills/codewiki-lint/SKILL.md` exists with skill metadata and the preserved lint workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical absorb skill source</name>
  <files>src/templates/skills/codewiki-absorb/SKILL.md</files>
  <read_first>
    - src/templates/claude/commands/codewiki/absorb.md
    - docs/skills/wiki.md
  </read_first>
  <action>
Create `src/templates/skills/codewiki-absorb/SKILL.md`.

Use this frontmatter exactly:
- `name: codewiki-absorb`
- `description: Extract knowledge from recent code changes into wiki`
- `allowed-tools: [Read, Write, Edit, Glob, Grep, Bash]`

Then migrate the existing absorb prompt body from `src/templates/claude/commands/codewiki/absorb.md` into the new file. Preserve the `# CodeWiki Absorb` heading, the `purpose` and `process` XML blocks, the `git diff HEAD~1` workflow, the `anti-cramming` and `anti-thinning` rules, the backlinks update step, and the approval boundary.

Do not invent new absorb arguments in this sub-phase.
  </action>
  <verify>
    <automated>test -f src/templates/skills/codewiki-absorb/SKILL.md && grep -q '^name: codewiki-absorb$' src/templates/skills/codewiki-absorb/SKILL.md && ! grep -q '^argument-hint:' src/templates/skills/codewiki-absorb/SKILL.md && grep -q 'anti-cramming' src/templates/skills/codewiki-absorb/SKILL.md && grep -q 'anti-thinning' src/templates/skills/codewiki-absorb/SKILL.md</automated>
  </verify>
  <done>`src/templates/skills/codewiki-absorb/SKILL.md` exists with skill metadata and the preserved absorb workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy command template -> canonical skill file | Prompt content is copied into a new source-of-truth file and must not gain extra permissions or semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.1-03 | T | lint skill migration | mitigate | Preserve the existing read-only lint behavior and keep the non-writing tool list intact |
| T-04.1.1-04 | T | absorb skill migration | mitigate | Preserve the existing approval gate and git-diff-driven workflow; do not add autonomous write behavior |
</threat_model>

<verification>
- `test -f src/templates/skills/codewiki-lint/SKILL.md`
- `test -f src/templates/skills/codewiki-absorb/SKILL.md`
- `grep -q '^name: codewiki-lint$' src/templates/skills/codewiki-lint/SKILL.md`
- `grep -q '^name: codewiki-absorb$' src/templates/skills/codewiki-absorb/SKILL.md`
- `grep -q 'This is a read-only lint pass' src/templates/skills/codewiki-lint/SKILL.md`
- `grep -q 'anti-cramming' src/templates/skills/codewiki-absorb/SKILL.md`
- `grep -q 'anti-thinning' src/templates/skills/codewiki-absorb/SKILL.md`
</verification>

<success_criteria>
The lint and absorb skills exist under `src/templates/skills/` with required skill metadata, no invented argument hints for no-input skills, and no behavioral regression from the command templates.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-02-SUMMARY.md`
</output>
