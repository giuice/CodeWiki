---
phase: 04.1.3-skills-regression-coverage-inserted
plan: 02
subsystem: integration-tests
tags: [codewiki, skills, init, packaging, npm-pack]
requires:
  - phase: 04.1.2-adapter-skill-install-paths-inserted
    provides: Claude and shared-skill install paths plus combined init reporting
provides:
  - Compiled init regression coverage for the full eight-skill install surface
  - Tarball verification pinned to dist/templates/skills/codewiki-ingest/SKILL.md
affects: [04.1.4-planning-docs-canon-refresh, 04.1.5-product-docs-canon-refresh, packaging-verification]
tech-stack:
  added: []
  patterns:
    - Compiled init integration tests use shared eight-skill tree assertions for Claude-only and mixed-tool installs
    - npm pack coverage asserts canonical skill assets from the dist/templates/skills tree while keeping hook asset checks unchanged
key-files:
  created: []
  modified:
    - test/init.test.ts
    - test/pack.test.ts
key-decisions:
  - Claude-only init runs must explicitly prove .agents/skills stays absent, not just that .claude/skills exists.
  - Pack coverage should assert only the canonical skill tarball path, while documenting any remaining legacy prose mismatch in comments rather than broadening the matcher.
patterns-established:
  - Shared helper assertions in compiled node:test files are the right place to enforce full install-surface coverage across repeated init scenarios.
requirements-completed: [SM-04]
duration: -
completed: 2026-04-12
---

# Phase 04.1.3 Plan 02: Compiled Skill Install and Pack Coverage Summary

**The built integration suite now proves the full eight-skill install surface for Claude and mixed-tool runs, and the tarball regression points at the canonical packaged skill asset.**

## Performance

- **Duration:** -
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Strengthened `test/init.test.ts` so Claude-only, auto-detected Claude, rerun, and `--force` scenarios all assert the full `.claude/skills` tree and explicit absence of `.agents/skills`.
- Strengthened mixed-tool init coverage so combined selections prove both the `.claude/skills` and `.agents/skills` eight-skill trees.
- Repointed `test/pack.test.ts` from the retired command asset to `dist/templates/skills/codewiki-ingest/SKILL.md` while keeping the established two-stage dry-run strategy and hook asset checks.

## Task Commits

Each task was committed atomically:

1. **Task 1: Expand compiled init regression coverage for the dual skill trees without weakening existing guarantees** - `5d42154` (test)
2. **Task 2: Repoint npm pack tarball coverage from legacy command assets to canonical skills** - `03051af` (test)

## Files Created/Modified
- `test/init.test.ts` - adds reusable eight-skill tree assertions and explicit `.agents/skills` absence checks for Claude-only scenarios.
- `test/pack.test.ts` - asserts the canonical packaged ingest skill asset under `dist/templates/skills/` and preserves hook asset coverage.

## Verification

- `repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/init.test.js`
- `repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/pack.test.js`
- `rtk npm test`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None.

## Next Phase Readiness

- Phase 04.1.3 now has regression coverage that follows the canonical skill install surface from source templates through packaged tarball assets.
- The documentation refresh phases can now update planning and product prose against verified install and packaging behavior instead of legacy command paths.

## Self-Check: PASSED

---
*Phase: 04.1.3-skills-regression-coverage-inserted*
*Completed: 2026-04-12*
