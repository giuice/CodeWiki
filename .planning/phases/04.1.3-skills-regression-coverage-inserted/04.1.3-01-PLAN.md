---
phase: 04.1.3-skills-regression-coverage-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/__tests__/commands.test.ts
  - src/templates/__tests__/absorb.test.ts
  - src/templates/__tests__/breakdown.test.ts
  - src/templates/__tests__/lint-query-backlinks.test.ts
  - src/templates/__tests__/ingest-backlinks.test.ts
autonomous: true
requirements:
  - SM-04
must_haves:
  truths:
    - "Template-spec regressions read the canonical `src/templates/skills/codewiki-<name>/SKILL.md` files instead of the legacy command tree"
    - "Template coverage reflects the full eight-skill canon, including the exact skills that require `argument-hint`"
    - "Backlink-aware skill regressions stay pinned to the migrated `SKILL.md` source files"
  artifacts:
    - path: "src/templates/__tests__/commands.test.ts"
      provides: "frontmatter and workflow assertions for the eight canonical skill source files"
      contains: "src/templates/skills"
    - path: "src/templates/__tests__/absorb.test.ts"
      provides: "absorb skill regression coverage against the canonical source tree"
      contains: "codewiki-absorb"
    - path: "src/templates/__tests__/ingest-backlinks.test.ts"
      provides: "ingest skill backlink regression coverage against `SKILL.md`"
      contains: "codewiki-ingest"
  key_links:
    - from: "src/templates/__tests__/commands.test.ts"
      to: "src/templates/skills/codewiki-ingest/SKILL.md"
      via: "path-based test helpers resolve per-skill directories directly"
      pattern: "src/templates/skills"
    - from: "src/templates/__tests__/lint-query-backlinks.test.ts"
      to: "src/templates/skills/codewiki-query/SKILL.md"
      via: "direct readFile path assertions"
      pattern: "codewiki-query/SKILL\\.md"
---

<objective>
Repoint the template-source regression suite from legacy command markdown files to the canonical skill directories.

Purpose: Deliver the source-template portion of SM-04 so the unit-level template regressions follow the skill canon established in Phase 04.1.1 and no longer depend on the temporary legacy command copies kept for migration safety.
Output: The five `src/templates/__tests__` files read `src/templates/skills/codewiki-<name>/SKILL.md` directly and cover the eight-skill frontmatter plus backlink behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-01-SUMMARY.md
@.planning/phases/04.1.2-adapter-skill-install-paths-inserted/04.1.2-VERIFICATION.md
@docs/skills-migration-handoff.md
@docs/skills/wiki.md
@src/templates/__tests__/commands.test.ts
@src/templates/__tests__/absorb.test.ts
@src/templates/__tests__/breakdown.test.ts
@src/templates/__tests__/lint-query-backlinks.test.ts
@src/templates/__tests__/ingest-backlinks.test.ts
@src/templates/skills/codewiki-ingest/SKILL.md
@src/templates/skills/codewiki-query/SKILL.md
@src/templates/skills/codewiki-prd/SKILL.md

<interfaces>
Current helper pattern in `src/templates/__tests__/commands.test.ts`:
```typescript
const COMMANDS_DIR = path.resolve("src/templates/claude/commands/codewiki");

async function readCommand(name: string): Promise<string> {
  return readFile(path.join(COMMANDS_DIR, name), "utf8");
}
```

Canonical skill source layout to test directly per D-01, D-03, and D-05:
```text
src/templates/skills/codewiki-absorb/SKILL.md
src/templates/skills/codewiki-breakdown/SKILL.md
src/templates/skills/codewiki-ingest/SKILL.md
src/templates/skills/codewiki-lint/SKILL.md
src/templates/skills/codewiki-prd/SKILL.md
src/templates/skills/codewiki-process/SKILL.md
src/templates/skills/codewiki-query/SKILL.md
src/templates/skills/codewiki-tasks/SKILL.md
```

Observed `argument-hint` set from the canonical skill files:
```text
codewiki-ingest   -> <source-file-path>
codewiki-query    -> <question>
codewiki-prd      -> <feature-description>
codewiki-process  -> <task-file-path>
codewiki-tasks    -> <prd-file-path>
```
Only these five skills currently declare `argument-hint`; the others should not gain it accidentally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the canonical skill frontmatter regression tests for the eight-skill source tree</name>
  <files>src/templates/__tests__/commands.test.ts, src/templates/__tests__/absorb.test.ts, src/templates/__tests__/breakdown.test.ts</files>
  <action>
Update these tests to read the hand-authored canonical skill files under `src/templates/skills/codewiki-<name>/SKILL.md` per D-01, D-03, and D-05 instead of `src/templates/claude/commands/codewiki/*.md`.

In `commands.test.ts`, replace the legacy six-command assumptions with the eight-skill canon used by the installer: `absorb`, `breakdown`, `ingest`, `lint`, `prd`, `process`, `query`, and `tasks`. Assert `name:` plus `description:` on every skill, assert `argument-hint:` only for `ingest`, `query`, `prd`, `process`, and `tasks`, and preserve the existing behavioral checks for approval gates, `Task` usage, the `Go` gate, and purpose/process XML blocks.

In `absorb.test.ts` and `breakdown.test.ts`, change only the file-resolution layer plus any frontmatter assertions needed to match `SKILL.md`; keep the semantic checks for approval boundaries, backlinks, git-diff behavior, ranking, and undocumented-gap detection intact. Do not weaken assertions to substring-only checks if the current expectations can still point at the canonical skill content.
  </action>
  <verify>
    <automated>rtk npm run test:unit -- src/templates/__tests__/commands.test.ts src/templates/__tests__/absorb.test.ts src/templates/__tests__/breakdown.test.ts</automated>
  </verify>
  <done>The three template tests read canonical skill files directly, cover the eight-skill canon, and keep the migrated workflow assertions intact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire backlink regression tests to the canonical skill source files</name>
  <files>src/templates/__tests__/lint-query-backlinks.test.ts, src/templates/__tests__/ingest-backlinks.test.ts</files>
  <action>
Update the direct path constants in these backlink-focused tests from the legacy command markdown files to the canonical `SKILL.md` files per D-01 and D-03.

Preserve the exact backlink-count and string-match expectations already established in Phase 03.1. These tests exist to prove the migrated skill files retained the backlink-aware load, prioritization, and write-back instructions; do not relax counts or rewrite the assertions into broad smoke tests.
  </action>
  <verify>
    <automated>rtk npm run test:unit -- src/templates/__tests__/lint-query-backlinks.test.ts src/templates/__tests__/ingest-backlinks.test.ts</automated>
  </verify>
  <done>The backlink regression tests pin the canonical skill sources and still prove the migrated prompts preserved the Phase 03.1 backlink behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical skill source -> regression tests | The tests must resolve the real `SKILL.md` files so future deletions of legacy command copies cannot silently hide broken coverage |
| migrated skill frontmatter -> install-time discovery behavior | Incorrect assertions here can miss regressions in `name`, `description`, or `argument-hint` that affect skill discoverability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.3-01 | T | `src/templates/__tests__` source paths | mitigate | Replace every legacy `src/templates/claude/commands/codewiki` constant in this plan with direct `src/templates/skills/codewiki-<name>/SKILL.md` resolution and verify those exact files in vitest |
| T-04.1.3-02 | I | skill frontmatter regression coverage | mitigate | Assert the exact eight-skill set plus the current five-skill `argument-hint` set so missing or extra frontmatter fields fail fast |
| T-04.1.3-03 | D | backlink behavior regressions hidden by broad smoke tests | mitigate | Keep the existing specific backlink count and sentence-level expectations instead of downgrading them to generic presence checks |
</threat_model>

<verification>
- `rtk npm run test:unit -- src/templates/__tests__/commands.test.ts src/templates/__tests__/absorb.test.ts src/templates/__tests__/breakdown.test.ts`
- `rtk npm run test:unit -- src/templates/__tests__/lint-query-backlinks.test.ts src/templates/__tests__/ingest-backlinks.test.ts`
- `rtk npm run test:unit -- src/templates/__tests__/commands.test.ts src/templates/__tests__/absorb.test.ts src/templates/__tests__/breakdown.test.ts src/templates/__tests__/lint-query-backlinks.test.ts src/templates/__tests__/ingest-backlinks.test.ts`
</verification>

<success_criteria>
All five template regression tests now follow the canonical skill source tree, the eight-skill frontmatter contract is explicitly covered, and the backlink-related skill behaviors remain pinned by specific assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.3-skills-regression-coverage-inserted/04.1.3-01-SUMMARY.md`
</output>
