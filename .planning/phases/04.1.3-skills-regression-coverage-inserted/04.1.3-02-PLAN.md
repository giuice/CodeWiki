---
phase: 04.1.3-skills-regression-coverage-inserted
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/init.test.ts
  - test/pack.test.ts
autonomous: true
requirements:
  - SM-04
must_haves:
  truths:
    - "Compiled init regression coverage asserts the canonical skill install surfaces for Claude-only and mixed-tool runs"
    - "Init regression coverage keeps rerun, force, agent, hook, permission, and settings/instruction merge guarantees intact while verifying no redundant skill tree appears"
    - "Pack tarball coverage asserts `dist/templates/skills/codewiki-ingest/SKILL.md` from the published asset tree"
  artifacts:
    - path: "test/init.test.ts"
      provides: "built CLI integration coverage for `.claude/skills` and `.agents/skills`"
      contains: ".claude/skills"
    - path: "test/pack.test.ts"
      provides: "npm pack tarball assertions for canonical skill assets and shared hook assets"
      contains: "dist/templates/skills/codewiki-ingest/SKILL.md"
  key_links:
    - from: "test/init.test.ts"
      to: "src/lib/adapters/claude.ts"
      via: "CLI integration asserts Claude installs `.claude/skills` and mixed runs install `.agents/skills`"
      pattern: "\\.claude/skills|\\.agents/skills"
    - from: "test/pack.test.ts"
      to: "dist/templates/skills/codewiki-ingest/SKILL.md"
      via: "`npm pack --dry-run --json` file-list assertion"
      pattern: "dist/templates/skills/codewiki-ingest/SKILL\\.md"
---

<objective>
Lock the compiled init and tarball integration coverage to the skill-based install surface delivered in Phase 04.1.2.

Purpose: Finish SM-04 by making the executable integration suite prove both the install paths and the published package assets now use the canonical skill layout instead of the retired command layout.
Output: `test/init.test.ts` asserts the Claude and shared non-Claude skill trees thoroughly, and `test/pack.test.ts` asserts the tarball includes `dist/templates/skills/codewiki-ingest/SKILL.md` plus the existing hook assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04.1.2-adapter-skill-install-paths-inserted/04.1.2-03-SUMMARY.md
@.planning/phases/04.1.2-adapter-skill-install-paths-inserted/04.1.2-VERIFICATION.md
@.planning/phases/05-test-suite/05-01-SUMMARY.md
@test/init.test.ts
@test/pack.test.ts
@src/lib/adapters/shared-skills.ts
@src/lib/adapters/claude.ts
@src/commands/init.ts

<interfaces>
Current compiled init assertions already present in `test/init.test.ts`:
```typescript
assert.equal(existsSync(path.join(cwd, ".claude/skills/codewiki-query/SKILL.md")), true);
assert.equal(existsSync(path.join(cwd, ".claude/skills/codewiki-ingest/SKILL.md")), true);
assert.equal(existsSync(path.join(cwd, ".agents/skills/codewiki-ingest/SKILL.md")), true);
```
These prove the migrated paths exist, but they do not yet force full eight-skill coverage for both trees or explicit absence of `.agents/skills` in Claude-only runs.

Current pack assertion in `test/pack.test.ts` still targets the retired command asset:
```typescript
assert.match(
  packOutput,
  /dist\/templates\/claude\/commands\/codewiki\/ingest\.md/,
  "tarball must include dist/templates/claude/commands/codewiki/ingest.md (BUILD-02)"
);
```
The canonical packaged skill asset after Phase 04.1.1 and 04.1.2 is:
```text
dist/templates/skills/codewiki-ingest/SKILL.md
```

Shared non-Claude install contract from `src/lib/adapters/shared-skills.ts`:
```typescript
const AGENT_SKILLS_DIR = ".agents/skills";

return copyTemplateDir(
  path.join(options.templateDir, "skills"),
  ensureInsideRoot(options.root, AGENT_SKILLS_DIR),
  options.force,
  options.root
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand compiled init regression coverage for the dual skill trees without weakening existing guarantees</name>
  <files>test/init.test.ts</files>
  <action>
Audit the existing compiled CLI integration cases and strengthen them where the Phase 04.1.2 path migration is still under-specified, per D-01, D-02, and D-04.

Use a shared canonical skill path list so Claude-only installs assert all eight `.claude/skills/codewiki-<name>/SKILL.md` files, mixed selections assert the shared `.agents/skills` tree for the same eight skills, and Claude-only cases explicitly assert `.agents/skills` is absent. Preserve the current rerun/idempotency, `--force`, hook permissions, agent install, settings merge, CLAUDE.md marker merge, and pending-integration-reporting assertions. Do not narrow or delete those older checks while adding the stronger skill-path coverage.
  </action>
  <verify>
    <automated>repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/init.test.js</automated>
  </verify>
  <done>The compiled init integration suite explicitly proves the full eight-skill install surface for Claude-only and mixed runs while retaining all existing idempotency and hook-related guarantees.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint npm pack tarball coverage from legacy command assets to canonical skills</name>
  <files>test/pack.test.ts</files>
  <action>
Update the pack assertions from `dist/templates/claude/commands/codewiki/ingest.md` to `dist/templates/skills/codewiki-ingest/SKILL.md` per D-01, D-05, and SM-04.

Keep the two-stage `npm pack --dry-run` plus `npm pack --dry-run --json` pattern established in Phase 05, and keep the hook asset assertions unchanged. If older planning prose still references the command path, document that mismatch inline in the test comment while asserting the real canonical tarball path; do not weaken the assertion to cover both old and new paths.
  </action>
  <verify>
    <automated>repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/pack.test.js</automated>
  </verify>
  <done>The pack integration test proves the published tarball includes the canonical ingest skill asset and still checks the hook scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter install behavior -> compiled integration tests | The regression suite must exercise the real built CLI outputs, not just source-level helpers, so installer path regressions are caught before publish |
| packaged dist asset tree -> tarball assertions | The tarball test must assert the real packaged skill asset path or it will give false confidence after the migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.3-04 | T | `test/init.test.ts` skill-surface assertions | mitigate | Use canonical eight-skill path lists for both `.claude/skills` and `.agents/skills`, plus explicit Claude-only absence checks for `.agents/skills` |
| T-04.1.3-05 | D | legacy idempotency or hook guarantees accidentally dropped while editing tests | mitigate | Keep the existing rerun, `--force`, hook permission, agent, and settings/instructions assertions in place and verify the compiled init suite end-to-end |
| T-04.1.3-06 | T | `test/pack.test.ts` tarball asset assertions | mitigate | Assert only `dist/templates/skills/codewiki-ingest/SKILL.md` via the `--json` file list while retaining hook asset checks from Phase 05 |
</threat_model>

<verification>
- `repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/init.test.js`
- `repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/pack.test.js`
- `rtk npm test`
</verification>

<success_criteria>
The built integration suite proves the installer writes the canonical skill trees for Claude-only and mixed selections, preserves the earlier non-skill guarantees, and the tarball test asserts the published ingest skill asset under `dist/templates/skills/`.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.3-skills-regression-coverage-inserted/04.1.3-02-SUMMARY.md`
</output>
