---
phase: 04.1.3-skills-regression-coverage-inserted
verified: 2026-04-12T12:01:53Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 04.1.3: Skills Regression Coverage Verification Report

**Phase Goal:** Regression tests and packaging verification prove the new skill install paths and bundled template assets.
**Verified:** 2026-04-12T12:01:53Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Template-source regressions read the canonical `src/templates/skills/codewiki-<name>/SKILL.md` files instead of the legacy command tree | ✓ VERIFIED | `src/templates/__tests__/commands.test.ts`, `absorb.test.ts`, `breakdown.test.ts`, `lint-query-backlinks.test.ts`, and `ingest-backlinks.test.ts` now resolve `src/templates/skills/codewiki-<name>/SKILL.md` directly. The focused unit verification passed: 5 files, 43 tests, 0 failures. |
| 2 | Compiled init regression coverage asserts the full skill install surfaces for Claude-only and mixed-tool runs | ✓ VERIFIED | `test/init.test.ts` now uses a shared eight-skill tree assertion for `.claude/skills` and `.agents/skills`. The compiled verification command `repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/init.test.js` passed with 8/8 tests. |
| 3 | Claude-only init coverage explicitly proves `.agents/skills` stays absent while older rerun, force, agent, hook, and merge guarantees remain intact | ✓ VERIFIED | `test/init.test.ts` now asserts `.agents/skills` absence in the explicit Claude, auto-detected Claude, rerun, and `--force` scenarios. Full-suite verification via `rtk npm test` passed with 93 Vitest tests and 9 node:test tests, showing the stronger skill-surface checks did not break prior idempotency or hook coverage. |
| 4 | Tarball verification asserts the canonical packaged ingest skill asset under `dist/templates/skills/` | ✓ VERIFIED | `test/pack.test.ts` now matches only `dist/templates/skills/codewiki-ingest/SKILL.md` while preserving the hook asset checks. The compiled verification command `repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/pack.test.js` passed with 1/1 tests. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/templates/__tests__/commands.test.ts` | Eight-skill canonical frontmatter and workflow coverage | ✓ VERIFIED | Reads `src/templates/skills/`, asserts `name` and `description` for all eight skills, and asserts `argument-hint` only for `ingest`, `query`, `prd`, `process`, and `tasks`. |
| `src/templates/__tests__/lint-query-backlinks.test.ts` | Backlink regressions pinned to canonical lint and query skills | ✓ VERIFIED | Direct path constants now target `src/templates/skills/codewiki-lint/SKILL.md` and `src/templates/skills/codewiki-query/SKILL.md`; exact string and count assertions remain intact. |
| `src/templates/__tests__/ingest-backlinks.test.ts` | Backlink regression pinned to canonical ingest skill | ✓ VERIFIED | Direct path constant now targets `src/templates/skills/codewiki-ingest/SKILL.md`; both backlink references are asserted explicitly. |
| `test/init.test.ts` | Built CLI integration coverage for `.claude/skills` and `.agents/skills` | ✓ VERIFIED | Reusable skill-tree assertions cover Claude-only, mixed-tool, rerun, and `--force` scenarios, with explicit `.agents/skills` absence checks for Claude-only runs. |
| `test/pack.test.ts` | Tarball assertion for the canonical packaged ingest skill | ✓ VERIFIED | `npm pack --dry-run --json` output is matched against `dist/templates/skills/codewiki-ingest/SKILL.md`, while hook asset assertions remain unchanged. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `src/templates/__tests__/commands.test.ts` | `src/templates/skills/codewiki-ingest/SKILL.md` | per-skill path helper | ✓ VERIFIED | Canonical test helper resolves `src/templates/skills/codewiki-<name>/SKILL.md` instead of the retired command directory. |
| `src/templates/__tests__/lint-query-backlinks.test.ts` | `src/templates/skills/codewiki-query/SKILL.md` | direct path constants | ✓ VERIFIED | Query backlink coverage now points directly at the canonical query skill file. |
| `test/init.test.ts` | `src/lib/adapters/claude.ts` and `src/lib/adapters/shared-skills.ts` | compiled CLI install scenarios | ✓ VERIFIED | The compiled integration suite proves both `.claude/skills` and `.agents/skills` tree creation against the real built CLI behavior. |
| `test/pack.test.ts` | `dist/templates/skills/codewiki-ingest/SKILL.md` | `npm pack --dry-run --json` file-list assertion | ✓ VERIFIED | The tarball assertion is pinned to the canonical packaged skill asset and no longer accepts the retired command path. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Task 1 template regressions pass | `rtk npm run test:unit -- src/templates/__tests__/commands.test.ts src/templates/__tests__/absorb.test.ts src/templates/__tests__/breakdown.test.ts` | 40 tests passed | PASS |
| Task 2 backlink regressions pass | `rtk npm run test:unit -- src/templates/__tests__/lint-query-backlinks.test.ts src/templates/__tests__/ingest-backlinks.test.ts` | 3 tests passed | PASS |
| Plan 01 combined verification passes | `rtk npm run test:unit -- src/templates/__tests__/commands.test.ts src/templates/__tests__/absorb.test.ts src/templates/__tests__/breakdown.test.ts src/templates/__tests__/lint-query-backlinks.test.ts src/templates/__tests__/ingest-backlinks.test.ts` | 43 tests passed | PASS |
| Plan 02 init integration passes | `repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/init.test.js` | 8 tests passed | PASS |
| Plan 02 pack integration passes | `repo_root="$PWD" && rtk npm run build && rtk proxy node --test dist/test/pack.test.js` | 1 test passed | PASS |
| Cross-phase regression gate passes | `rtk npm test` | 93 Vitest tests passed, 9 node:test tests passed | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `SM-04` | `04.1.3-01-PLAN.md`, `04.1.3-02-PLAN.md` | Regression coverage asserts skill install paths in init tests and pack coverage for `dist/templates/skills/codewiki-ingest/SKILL.md` | ✓ SATISFIED | Source-template regressions now read canonical `SKILL.md` files, compiled init tests assert the skill trees and Claude-only absence rule, and pack coverage matches the canonical tarball asset path. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `(none)` | - | No weakened smoke-test conversions, TODO markers, or dual-path legacy assertions were introduced in the reviewed phase files | ℹ️ Info | The regression suite now fails on the canonical skill contract instead of tolerating both old and new paths |

### Gaps Summary

No blocking gaps were found for the Phase 04.1.3 goal. The regression suite, compiled init coverage, and tarball assertions all follow the canonical skill install surface, and the full automated test suite remains green.

---

_Verified: 2026-04-12T12:01:53Z_
_Verifier: GitHub Copilot (manual Phase 04.1.3 verification)_
