---
phase: 04.1-skills-migration
plan: 03
type: execute
wave: 3
depends_on: ["04.1-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/research/FEATURES.md
  - .planning/research/SUMMARY.md
  - .planning/research/ARCHITECTURE.md
  - docs/codewiki-project-v2.md
  - docs/implementation-plan-v2.md
  - README.md
  - .planning/phases/04-claude-code-adapter-init-command/04-VERIFICATION.md
  - .planning/phases/06-opencode-adapter/06-01-PLAN.md
autonomous: true
requirements:
  - CC-01
  - CODEX-01
  - COP-03
  - OC-01
  - BUILD-02
must_haves:
  truths:
    - "REQUIREMENTS.md CC-01 says 'Installs 8 Skills to .claude/skills/codewiki-<name>/SKILL.md' not 'Installs 8 slash commands to .claude/commands/codewiki/'"
    - "ROADMAP.md Phase 4 success criteria reference Skills layout, not slash commands"
    - "ROADMAP.md Phase 4.1 goal describes the skills migration accurately"
    - "PROJECT.md reflects the skills install surface in Key Decisions and Active requirements"
    - "All planning docs and research docs that reference .claude/commands/codewiki/ now reference .claude/skills/codewiki-<name>/SKILL.md"
    - "Phase 6 OpenCode plan references Skills layout, not slash commands"
    - "Phase 4 VERIFICATION.md reflects the updated CC-01 evidence"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated CC-01 requirement text and traceability"
      contains: "Skills"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 4 and 4.1 descriptions"
      contains: "Skills"
    - path: ".planning/PROJECT.md"
      provides: "Updated active requirements and Key Decisions"
      contains: "Skills"
    - path: "README.md"
      provides: "Updated feature description"
      contains: "Skills"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "CC-01"
      via: "requirement text update"
      pattern: "Skills.*codewiki-"
    - from: ".planning/ROADMAP.md"
      to: "Phase 4.1"
      via: "success criteria update"
      pattern: "Skills"
---

<objective>
Update all documentation and planning files to reflect the skills migration: replace every reference to "slash commands" and `.claude/commands/codewiki/` with "Skills" and `.claude/skills/codewiki-<name>/SKILL.md`. Delete stale handoff docs. Update requirement text, roadmap success criteria, research docs, and implementation plan references.

Purpose: Without this doc cascade, the codebase is correct but the docs still describe the old slash-command surface. This creates a drift where docs say "commands" but code installs "skills." The cascade ensures every planning artifact, requirement, and user-facing doc matches the new reality.

Output: All references to the old install surface are updated. REQUIREMENTS.md CC-01, BUILD-02, CODEX-01, COP-03, OC-01 are updated. ROADMAP Phase 4 success criteria and Phase 4.1 description are updated. Stale handoff docs are deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@docs/skills-migration-handoff.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md, ROADMAP.md, PROJECT.md, and Phase 4 VERIFICATION to reflect Skills surface</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/phases/04-claude-code-adapter-init-command/04-VERIFICATION.md
  </files>
  <action>
Per the migration handoff section 3.4 (Requirements update) and section 3.7 (Doc cascade):

**In `.planning/REQUIREMENTS.md`:**

1. Update CC-01 from:
   "Installs 8 slash commands to `.claude/commands/codewiki/`"
   to:
   "Installs 8 Skills to `.claude/skills/codewiki-<name>/SKILL.md` (+ dual-tree to `.agents/skills/` when non-Claude tools are also selected)"

2. Update the "Prompt Files (Skills)" section header note to confirm the skills canon is now implemented in code (not just a doc statement). The note already says the right thing but should reference that the installer now writes to the skills layout.

3. Update BUILD-02 from:
   "`npm pack --dry-run` lists `dist/templates/claude/commands/codewiki/ingest.md` (prompt files in tarball)"
   to:
   "`npm pack --dry-run` lists `dist/templates/skills/codewiki-ingest/SKILL.md` (skill files in tarball)"

4. Update CODEX-01 from:
   "Installs 6 slash commands to correct Codex command directory"
   to:
   "Installs 8 Skills to `.agents/skills/codewiki-<name>/SKILL.md` (Codex reads `.agents/skills/` only)"

5. Update COP-03 from:
   "Documents slash command limitation (no file-based slash command directory confirmed)"
   to:
   "Installs 8 Skills to `.agents/skills/codewiki-<name>/SKILL.md` for Copilot (Copilot reads `.agents/skills/` and `.claude/skills/`; canonical write target is `.agents/skills/` for Copilot-only installs)"

6. Update OC-01 from:
   "Installs 8 slash commands to `.opencode/commands/codewiki/`"
   to:
   "Installs 8 Skills to `.agents/skills/codewiki-<name>/SKILL.md` for OpenCode (dual-tree to `.claude/skills/` when Claude Code is also selected)"

7. Update the Traceability table: mark CC-01, BUILD-02 as needing re-verification in Phase 4.1. The Phase column for CC-01 should note "Phase 4.1 (Skills Migration)" in addition to "Phase 4".

**In `.planning/ROADMAP.md`:**

8. Update Phase 4 success criteria item 1 from:
   "Running `npx codewiki init` in a project with `.claude/` present creates all wiki directories, installs 8 slash commands to `.claude/commands/codewiki/`, installs 2 agents to `.claude/agents/`, and prints a structured install report"
   to:
   "Running `npx codewiki init` in a project with `.claude/` present creates all wiki directories, installs 8 Skills to `.claude/skills/codewiki-<name>/SKILL.md`, installs 2 agents to `.claude/agents/`, and prints a structured install report"

9. Update Phase 4.1 description to have concrete success criteria:
   "1. `src/templates/skills/codewiki-<name>/SKILL.md` exists for all 8 skills with `name:` and enriched `description:` frontmatter"
   "2. `npx codewiki init --tool claude-code` installs skills to `.claude/skills/codewiki-<name>/SKILL.md` (not `.claude/commands/codewiki/*.md`)"
   "3. `npm run build && npm run test:unit && npm test` all pass"
   "4. `npm pack --dry-run --json` lists `dist/templates/skills/codewiki-ingest/SKILL.md`"
   "5. No code or test references to `commands/codewiki` path remain in `src/` or `test/`"

10. Update Phase 4.1 plan count and plan list with the actual plan files created.

11. Update Phase 5 success criteria item 3 from:
   "A test runs `npm pack --dry-run` and asserts `dist/templates/claude/commands/codewiki/ingest.md` appears in the file list"
   to:
   "A test runs `npm pack --dry-run` and asserts `dist/templates/skills/codewiki-ingest/SKILL.md` appears in the file list"

12. Update Phase 6 description from "8 slash commands" to "8 Skills" and the path from `.opencode/commands/codewiki/` to `.agents/skills/codewiki-<name>/SKILL.md` (for OpenCode-only) and `.claude/skills/` (for dual-tree).

13. Update Phase 8 success criteria item 1 from:
   "`npm pack --dry-run` output includes `dist/templates/claude/commands/ingest.md`"
   to:
   "`npm pack --dry-run` output includes `dist/templates/skills/codewiki-ingest/SKILL.md`"

**In `.planning/PROJECT.md`:**

14. Update the Active requirements list to reflect the skills install surface:
    - Change "Slash commands installed as native markdown prompts: `/codewiki-ingest`..." to "Skills installed as per-command SKILL.md files: `/codewiki-ingest`..."

15. Update the Key Decisions table: change the "Prompt files bundled in `dist/prompts/`" decision to "Skills bundled in `dist/templates/skills/`" and update rationale to reference the skills model.

**In `.planning/phases/04-claude-code-adapter-init-command/04-VERIFICATION.md`:**

16. Update CC-01 evidence from "Claude adapter installs 8 command markdown files into `.claude/commands/codewiki/`" to "Claude adapter installs 8 Skills into `.claude/skills/codewiki-<name>/SKILL.md`". Add a note that this was updated during Phase 4.1 Skills Migration.
  </action>
  <verify>
    <automated>cd /home/giuice/Desenv/CodeWiki && grep -rn "commands/codewiki" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/PROJECT.md .planning/phases/04-claude-code-adapter-init-command/04-VERIFICATION.md | head -5; echo "---"; grep -c "\.claude/skills/codewiki" .planning/REQUIREMENTS.md .planning/ROADMAP.md</automated>
  </verify>
  <done>REQUIREMENTS.md CC-01, BUILD-02, CODEX-01, COP-03, OC-01 updated to reference Skills layout. ROADMAP Phase 4, 4.1, 5, 6, 8 success criteria updated. PROJECT.md active requirements and Key Decisions updated. Phase 4 VERIFICATION CC-01 evidence updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update research docs, implementation plan, README, Phase 6 plan, and delete stale handoff docs</name>
  <files>
    .planning/research/FEATURES.md
    .planning/research/SUMMARY.md
    .planning/research/ARCHITECTURE.md
    docs/implementation-plan-v2.md
    README.md
    .planning/phases/06-opencode-adapter/06-01-PLAN.md
  </files>
  <action>
Per the migration handoff section 3.7 (Doc cascade):

**In `.planning/research/FEATURES.md`:**

1. Update all references to "8 slash commands" to "8 Skills (one per command, SKILL.md format)".
2. Update the per-tool install path table from `.claude/commands/codewiki/<name>.md` to `.claude/skills/codewiki-<name>/SKILL.md` for Claude Code.
3. Update MVP bullets that reference "slash command" language to use "Skills" language.
4. Update the skills-directory research gap: it is now CLOSED (verified 2026-04-11, dual-tree install confirmed).

**In `.planning/research/SUMMARY.md`:**

5. Update "8 slash command .md files" language to "8 Skills (SKILL.md files, one per command)".
6. Update directory tree references from `.claude/commands/codewiki/*.md` to `.claude/skills/codewiki-<name>/SKILL.md`.
7. Update "Codex per-project command path" language to reference skills directory (`.agents/skills/`) and note it is now confirmed.

**In `.planning/research/ARCHITECTURE.md`:**

8. Update the architecture diagram tree from `.claude/commands/codewiki/*.md (slash commands)` to `.claude/skills/codewiki-<name>/SKILL.md (8 Skills)`.
9. Update "Phase 2: Prompt files (slash commands)" to "Phase 2: Prompt files (Skills)".
10. Update the install matrix row for Claude Code from `skip with warning` / `overwrite` to reflect `.claude/skills/` as the install target.

**In `docs/implementation-plan-v2.md`:**

11. Search for all references to "slash command", "commands/codewiki", or `.claude/commands/` and update to Skills language and `.claude/skills/codewiki-<name>/SKILL.md` paths.
12. Update any "Available Commands" section to "Available Skills".

**In `README.md`:**

13. Find the Phase 3 table row or feature description that mentions "8 slash commands" or "commands" and update to "8 Skills" with the correct path.
14. Update any other references to the command install surface.

**In `.planning/phases/06-opencode-adapter/06-01-PLAN.md`:**

15. Search for references to `.opencode/commands/codewiki/` and update to `.agents/skills/codewiki-<name>/SKILL.md` (OpenCode reads `.agents/skills/` per the verified research). If the plan references "slash commands" for OpenCode, update to "Skills".

**Delete stale handoff docs:**

16. Delete `docs/docs-reconciliation-handoff.md` if it exists (it was built on the wrong canon per the migration handoff section 0).
17. Delete `docs/inconsistences.md` if it exists (superseded by the migration handoff).
18. Do NOT delete `docs/skills-migration-handoff.md` yet — it contains useful context for the Phase 4.1 execution. It can be deleted after verification.
  </action>
  <verify>
    <automated>cd /home/giuice/Desenv/CodeWiki && grep -rn "commands/codewiki" .planning/research/ docs/implementation-plan-v2.md README.md .planning/phases/06-opencode-adapter/ | grep -v "node_modules" | grep -v ".git" | head -5; echo "--- stale docs check ---"; test ! -f docs/docs-reconciliation-handoff.md && echo "stale handoff deleted" || echo "WARNING: stale handoff still exists"</automated>
  </verify>
  <done>All research docs, implementation plan, README, and Phase 6 plan reference Skills layout (not slash commands). Stale handoff docs are deleted. No remaining references to commands/codewiki path in planning docs or README.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs → Code | Documentation must match the actual installed surface; drift creates confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-05 | Tampering | Requirement text | accept | Requirements are developer-maintained docs, not runtime inputs |
| T-04.1-06 | Repudiation | Doc-code drift | mitigate | Integration tests in Plan 02 verify actual install surface matches doc claims |
</threat_model>

<verification>
1. `grep -rn "commands/codewiki" .planning/ src/ test/ docs/ README.md` returns zero results (excluding node_modules and .git)
2. `grep -rn "slash command" .planning/ docs/ README.md` returns zero results (or only historical/quoted references)
3. REQUIREMENTS.md CC-01 contains "Skills" and ".claude/skills/"
4. ROADMAP.md Phase 4 success criteria contain "Skills" and ".claude/skills/"
5. No `docs/docs-reconciliation-handoff.md` file exists
6. No `docs/inconsistences.md` file exists
</verification>

<success_criteria>
- REQUIREMENTS.md CC-01, BUILD-02, CODEX-01, COP-03, OC-01 reference Skills layout
- ROADMAP.md success criteria for Phases 4, 4.1, 5, 6, 8 reference Skills
- PROJECT.md active requirements and Key Decisions reference Skills
- All research docs (FEATURES, SUMMARY, ARCHITECTURE) reference Skills
- README.md references Skills
- Phase 6 plan references Skills layout for OpenCode
- Phase 4 VERIFICATION.md CC-01 evidence updated
- Stale handoff docs deleted
- Zero remaining references to `commands/codewiki` path in docs or planning files
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-skills-migration/04.1-03-SUMMARY.md`
</output>