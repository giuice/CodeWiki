---
phase: 07-codex-and-copilot-adapters
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/adapters/codex.ts
  - src/lib/adapters/index.ts
  - src/commands/init.ts
autonomous: true
requirements:
  - CODEX-01
  - CODEX-02
  - CODEX-03
must_haves:
  truths:
    - "D-01: Codex adapter scope is complete in Phase 7, not reported as pending after this plan"
    - "D-05: `.codex/config.toml` is merged so `[features] codex_hooks = true` is present without clobbering existing config"
    - "D-06/D-07/D-08/D-09: `.codex/hooks.json` installs UserPromptSubmit, PreToolUse, PostToolUse, and Stop entries from the templates"
    - "D-10: Installed hook commands resolve repository root via `git rev-parse --show-toplevel`"
    - "D-11: Adapter installs `.codex/agents/codewiki-wiki-updater.toml` and `.codex/agents/codewiki-verifier.toml`"
    - "D-19/D-20/D-22: Adapter merges CodeWiki guidance into `AGENTS.md` using marker comments and preserves existing user text"
    - "CODEX-01: Shared `.agents/skills` remains the only Codex skill tree"
  artifacts:
    - path: "src/lib/adapters/codex.ts"
      provides: "Real Codex adapter layered on shared skills"
      exports: ["CodexAdapter"]
    - path: "src/lib/adapters/index.ts"
      provides: "Adapter resolution that runs shared-skills plus Codex adapter for Codex selections"
      contains: "CodexAdapter"
    - path: "src/commands/init.ts"
      provides: "Pending-report logic no longer labels Codex as shared-skills-only"
      contains: "SHARED_SKILL_ONLY_TOOLS"
---

<objective>
Implement the real Codex adapter and route Codex selections through it while preserving shared `.agents/skills` behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-codex-and-copilot-adapters/07-CONTEXT.md
@.planning/phases/07-codex-and-copilot-adapters/07-RESEARCH.md
@.planning/phases/07-codex-and-copilot-adapters/07-PATTERNS.md
@.planning/phases/07-codex-and-copilot-adapters/07-01-PLAN.md
@src/lib/adapters/claude.ts
@src/lib/adapters/opencode.ts
@src/lib/adapters/shared-skills.ts
@src/lib/adapters/base.ts
@src/lib/adapters/index.ts
@src/lib/merge.ts
@src/commands/init.ts
@src/templates/codex/hooks.json
@src/templates/codex/config.toml
@src/templates/codex/instructions.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement Codex adapter copy, chmod, hook JSON merge, TOML feature merge, and AGENTS merge</name>
  <files>src/lib/adapters/codex.ts</files>
  <read_first>
    - src/lib/adapters/claude.ts
    - src/lib/adapters/opencode.ts
    - src/lib/adapters/base.ts
    - src/lib/merge.ts
    - src/templates/codex/hooks.json
    - src/templates/codex/config.toml
    - src/templates/codex/instructions.md
  </read_first>
  <action>
Create `src/lib/adapters/codex.ts` exporting `CodexAdapter implements ToolAdapter` with `tool: SupportedTool = "codex"`.

Implement `install(options)` to:
- ensure `.codex`, `.codex/hooks`, and `.codex/agents`
- copy `src/templates/codex/hooks/` into `.codex/hooks/`
- copy `src/templates/codex/agents/` into `.codex/agents/`
- chmod copied `.sh` wrapper scripts executable using `chmodExecutable()`
- merge `.codex/hooks.json` from `src/templates/codex/hooks.json` into any existing file using `deepMerge()` plus `deduplicateHookEntries()` per hook event
- merge `.codex/config.toml` so `[features]` exists and `codex_hooks = true` is present without removing other user lines
- merge `src/templates/codex/instructions.md` into `AGENTS.md` via `mergeMarkerSection(existingText, instructionContent, options.force)`
- return `ReportEntry[]` with created/replaced/skipped/failed entries like the Claude adapter

For TOML merge, implement a narrow helper:
- if no file exists, write the template exactly
- if `[features]` exists but no `codex_hooks`, insert `codex_hooks = true` immediately after `[features]`
- if `codex_hooks = false` exists under features, replace it with `codex_hooks = true`
- if `codex_hooks = true` already exists, leave the file unchanged
- preserve all unrelated text and comments
  </action>
  <acceptance_criteria>
    - `src/lib/adapters/codex.ts` contains `export class CodexAdapter`
    - `src/lib/adapters/codex.ts` contains `.codex/hooks`, `.codex/agents`, `.codex/hooks.json`, `.codex/config.toml`, and `AGENTS.md`
    - `src/lib/adapters/codex.ts` imports or uses `deepMerge`, `deduplicateHookEntries`, `mergeMarkerSection`, `copyTemplateDir`, and `chmodExecutable`
    - `src/lib/adapters/codex.ts` contains `codex_hooks = true`
  </acceptance_criteria>
  <verify>
    <automated>test -f src/lib/adapters/codex.ts && rg -n "export class CodexAdapter|\\.codex/hooks|\\.codex/agents|\\.codex/hooks\\.json|\\.codex/config\\.toml|AGENTS\\.md|deepMerge|deduplicateHookEntries|mergeMarkerSection|copyTemplateDir|chmodExecutable|codex_hooks = true" src/lib/adapters/codex.ts</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Resolve Codex adapter alongside shared skills</name>
  <files>src/lib/adapters/index.ts</files>
  <read_first>
    - src/lib/adapters/index.ts
    - src/lib/adapters/shared-skills.ts
    - src/lib/adapters/opencode.ts
    - src/lib/adapters/codex.ts
  </read_first>
  <action>
Update `src/lib/adapters/index.ts` so `adapterFactories` has a `codex` factory that imports `./codex.js` and returns `new CodexAdapter()`.

Do not remove `codex` from `SHARED_SKILLS_TOOLS`. Codex selections must still run `SharedSkillsAdapter` first so `.agents/skills/codewiki-<name>/SKILL.md` remains the canonical skill tree.
  </action>
  <acceptance_criteria>
    - `src/lib/adapters/index.ts` contains `codex: async () =>`
    - `src/lib/adapters/index.ts` contains `./codex.js`
    - `src/lib/adapters/index.ts` contains `CodexAdapter`
    - `src/lib/adapters/index.ts` still contains `["codex", "copilot", "opencode"]` in `SHARED_SKILLS_TOOLS`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "codex: async|\\.\\/codex\\.js|CodexAdapter|\\[\"codex\", \"copilot\", \"opencode\"\\]" src/lib/adapters/index.ts</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 3: Stop reporting Codex as pending once real adapter exists</name>
  <files>src/commands/init.ts</files>
  <read_first>
    - src/commands/init.ts
    - test/init.test.ts
    - .planning/phases/07-codex-and-copilot-adapters/07-CONTEXT.md
  </read_first>
  <action>
Update `src/commands/init.ts` so `codex` is no longer part of `SHARED_SKILL_ONLY_TOOLS`.

Before this plan, the set is `new Set<SupportedTool>(["codex", "copilot"])`. After this plan, it must be `new Set<SupportedTool>(["copilot"])`.

Keep the pending-integration section for Copilot until Plan 07-05 implements the real Copilot adapter. Do not change report formatting otherwise.
  </action>
  <acceptance_criteria>
    - `src/commands/init.ts` contains `new Set<SupportedTool>(["copilot"])`
    - `src/commands/init.ts` does not contain `["codex", "copilot"]`
    - `getPendingIntegrationEntries` remains present for Copilot
  </acceptance_criteria>
  <verify>
    <automated>rg -n "new Set<SupportedTool>\\(\\[\"copilot\"\\]\\)|getPendingIntegrationEntries" src/commands/init.ts && ! rg -n "\\[\"codex\", \"copilot\"\\]" src/commands/init.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex adapter -> user `.codex/config.toml` | Feature flag merge must preserve unrelated user config |
| Codex adapter -> user `.codex/hooks.json` | Hook merge must preserve unrelated hook entries and dedupe CodeWiki entries |
| Codex adapter -> `AGENTS.md` | Marker merge must preserve user-authored instructions |
| Shared skills -> Codex adapter | Real Codex adapter must complement, not replace, `.agents/skills` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T | `.codex/config.toml` merge | mitigate | Use a narrow text merge that changes only `codex_hooks` in `[features]` |
| T-07-02-02 | T | `.codex/hooks.json` merge | mitigate | Reuse JSON merge and hook-entry deduplication instead of overwriting |
| T-07-02-03 | R | `AGENTS.md` merge | mitigate | Use `mergeMarkerSection()` and later test repeated installs |
| T-07-02-04 | I | install report | mitigate | Remove Codex from pending report so users are not misled about active hook behavior |
</threat_model>

<verification>
- `test -f src/lib/adapters/codex.ts`
- `rg -n "export class CodexAdapter|\\.codex/hooks|\\.codex/agents|\\.codex/hooks\\.json|\\.codex/config\\.toml|AGENTS\\.md" src/lib/adapters/codex.ts`
- `rg -n "codex: async|CodexAdapter" src/lib/adapters/index.ts`
- `rg -n "new Set<SupportedTool>\\(\\[\"copilot\"\\]\\)" src/commands/init.ts`
- `npm run build`
</verification>

<success_criteria>
- Codex selections resolve a real Codex adapter plus the existing shared-skills adapter.
- Codex install code can preserve and merge existing `.codex` and `AGENTS.md` content.
- Codex is no longer reported as a pending shared-skills-only integration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-codex-and-copilot-adapters/07-02-SUMMARY.md`.
</output>
