---
phase: 07-codex-and-copilot-adapters
plan: 05
type: execute
wave: 2
depends_on: ["07-04"]
files_modified:
  - src/lib/adapters/copilot.ts
  - src/lib/adapters/index.ts
  - src/commands/init.ts
autonomous: true
requirements:
  - COP-01
  - COP-02
  - COP-03
must_haves:
  truths:
    - "D-01: Copilot adapter scope is complete in Phase 7, not reported as pending after this plan"
    - "D-14/D-15/D-16/D-17/D-18: Adapter installs Copilot hook JSON and loop-safe wrappers with `agentStop` as smart follow-up and `sessionEnd` cleanup-only"
    - "D-19/D-20/D-23: Adapter merges CodeWiki guidance into `.github/copilot-instructions.md` using marker comments and preserves existing user text"
    - "COP-03: Shared `.agents/skills` remains the only CodeWiki skill tree for Copilot"
  artifacts:
    - path: "src/lib/adapters/copilot.ts"
      provides: "Real Copilot adapter layered on shared skills"
      exports: ["CopilotAdapter"]
    - path: "src/lib/adapters/index.ts"
      provides: "Adapter resolution that runs shared-skills plus Copilot adapter for Copilot selections"
      contains: "CopilotAdapter"
    - path: "src/commands/init.ts"
      provides: "Pending-report logic no longer labels Copilot as shared-skills-only"
      contains: "SHARED_SKILL_ONLY_TOOLS"
---

<objective>
Implement the real Copilot adapter and route Copilot selections through it while preserving shared `.agents/skills` behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-codex-and-copilot-adapters/07-CONTEXT.md
@.planning/phases/07-codex-and-copilot-adapters/07-RESEARCH.md
@.planning/phases/07-codex-and-copilot-adapters/07-PATTERNS.md
@.planning/phases/07-codex-and-copilot-adapters/07-04-PLAN.md
@src/lib/adapters/opencode.ts
@src/lib/adapters/claude.ts
@src/lib/adapters/shared-skills.ts
@src/lib/adapters/base.ts
@src/lib/adapters/index.ts
@src/lib/merge.ts
@src/commands/init.ts
@src/templates/copilot/hooks/codewiki-hooks.json
@src/templates/copilot/instructions.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement Copilot adapter copy, chmod, hook JSON install, and instruction merge</name>
  <files>src/lib/adapters/copilot.ts</files>
  <read_first>
    - src/lib/adapters/opencode.ts
    - src/lib/adapters/claude.ts
    - src/lib/adapters/base.ts
    - src/lib/merge.ts
    - src/templates/copilot/hooks/codewiki-hooks.json
    - src/templates/copilot/instructions.md
  </read_first>
  <action>
Create `src/lib/adapters/copilot.ts` exporting `CopilotAdapter implements ToolAdapter` with `tool: SupportedTool = "copilot"`.

Implement `install(options)` to:
- ensure `.github/hooks` and `.github/hooks/codewiki`
- copy `src/templates/copilot/hooks/*.sh` into `.github/hooks/codewiki/`
- chmod copied `.sh` wrapper scripts executable using `chmodExecutable()`
- install `src/templates/copilot/hooks/codewiki-hooks.json` to `.github/hooks/codewiki-hooks.json`
- write the CodeWiki-owned hook JSON idempotently: created if absent, replaced if different or `--force`, skipped if identical
- do not modify unrelated `.github/hooks/*.json` files
- merge `src/templates/copilot/instructions.md` into `.github/copilot-instructions.md` via `mergeMarkerSection(existingText, instructionContent, options.force)`
- return `ReportEntry[]` with created/replaced/skipped/failed entries like existing adapters
  </action>
  <acceptance_criteria>
    - `src/lib/adapters/copilot.ts` contains `export class CopilotAdapter`
    - `src/lib/adapters/copilot.ts` contains `.github/hooks`, `.github/hooks/codewiki`, `.github/hooks/codewiki-hooks.json`, and `.github/copilot-instructions.md`
    - `src/lib/adapters/copilot.ts` imports or uses `copyTemplateDir`, `chmodExecutable`, and `mergeMarkerSection`
    - `src/lib/adapters/copilot.ts` does not contain `.github/skills`
  </acceptance_criteria>
  <verify>
    <automated>test -f src/lib/adapters/copilot.ts && rg -n "export class CopilotAdapter|\\.github/hooks|\\.github/hooks/codewiki|\\.github/hooks/codewiki-hooks\\.json|\\.github/copilot-instructions\\.md|copyTemplateDir|chmodExecutable|mergeMarkerSection" src/lib/adapters/copilot.ts && ! rg -n "\\.github/skills" src/lib/adapters/copilot.ts</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Resolve Copilot adapter alongside shared skills</name>
  <files>src/lib/adapters/index.ts</files>
  <read_first>
    - src/lib/adapters/index.ts
    - src/lib/adapters/shared-skills.ts
    - src/lib/adapters/copilot.ts
  </read_first>
  <action>
Update `src/lib/adapters/index.ts` so `adapterFactories` has a `copilot` factory that imports `./copilot.js` and returns `new CopilotAdapter()`.

Do not remove `copilot` from `SHARED_SKILLS_TOOLS`. Copilot selections must still run `SharedSkillsAdapter` first so `.agents/skills/codewiki-<name>/SKILL.md` remains the canonical skill tree.
  </action>
  <acceptance_criteria>
    - `src/lib/adapters/index.ts` contains `copilot: async () =>`
    - `src/lib/adapters/index.ts` contains `./copilot.js`
    - `src/lib/adapters/index.ts` contains `CopilotAdapter`
    - `src/lib/adapters/index.ts` still contains `copilot` in `SHARED_SKILLS_TOOLS`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "copilot: async|\\.\\/copilot\\.js|CopilotAdapter|SHARED_SKILLS_TOOLS" src/lib/adapters/index.ts</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 3: Remove shared-skills-only pending report after both adapters are real</name>
  <files>src/commands/init.ts</files>
  <read_first>
    - src/commands/init.ts
    - test/init.test.ts
    - .planning/phases/07-codex-and-copilot-adapters/07-CONTEXT.md
  </read_first>
  <action>
Update `src/commands/init.ts` so Copilot is no longer reported as "shared skills installed; hooks and instructions remain pending".

After this plan, both Codex and Copilot are real adapters. Either:
- set `SHARED_SKILL_ONLY_TOOLS` to an empty set and leave `getPendingIntegrationEntries()` in place, or
- remove the pending-integration helper/section entirely if no supported tools need it anymore.

Do not change report formatting for real adapter sections.
  </action>
  <acceptance_criteria>
    - `src/commands/init.ts` no longer contains the string `shared skills installed; hooks and instructions remain pending`, or no supported tool can produce that entry
    - `src/commands/init.ts` does not contain `["copilot"]`
    - `src/commands/init.ts` does not contain `["codex", "copilot"]`
  </acceptance_criteria>
  <verify>
    <automated>! rg -n "\\[\"copilot\"\\]|\\[\"codex\", \"copilot\"\\]" src/commands/init.ts</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Copilot adapter -> `.github/hooks/` | Adapter must install only CodeWiki-owned hook file/wrappers and avoid unrelated hook files |
| Copilot adapter -> `.github/copilot-instructions.md` | Marker merge must preserve user-authored instructions |
| Shared skills -> Copilot adapter | Real Copilot adapter must complement, not replace, `.agents/skills` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | T | `.github/hooks` install | mitigate | Use deterministic CodeWiki-owned file paths and leave unrelated hook JSON files alone |
| T-07-05-02 | R | instruction merge | mitigate | Use `mergeMarkerSection()` and later test repeated installs |
| T-07-05-03 | I | install report | mitigate | Remove pending-report language once Copilot adapter is real |
</threat_model>

<verification>
- `test -f src/lib/adapters/copilot.ts`
- `rg -n "export class CopilotAdapter|\\.github/hooks|\\.github/copilot-instructions\\.md" src/lib/adapters/copilot.ts`
- `rg -n "copilot: async|CopilotAdapter" src/lib/adapters/index.ts`
- `npm run build`
</verification>

<success_criteria>
- Copilot selections resolve a real Copilot adapter plus the existing shared-skills adapter.
- Copilot install code writes only CodeWiki-owned hook surfaces and marker-managed instructions.
- No Phase 7 tool remains labeled as a pending shared-skills-only integration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-codex-and-copilot-adapters/07-05-SUMMARY.md`.
</output>
