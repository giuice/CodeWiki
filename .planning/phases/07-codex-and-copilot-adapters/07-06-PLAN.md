---
phase: 07-codex-and-copilot-adapters
plan: 06
type: execute
wave: 3
depends_on: ["07-04", "07-05"]
files_modified:
  - src/templates/__tests__/copilot-adapter.test.ts
  - test/init.test.ts
autonomous: true
requirements:
  - CODEX-01
  - CODEX-02
  - CODEX-03
  - COP-01
  - COP-02
  - COP-03
must_haves:
  truths:
    - "D-01: Tests prove both Codex and Copilot adapters are complete"
    - "D-14 through D-18: Tests lock Copilot `agentStop` and cleanup-only `sessionEnd` behavior"
    - "D-19/D-20/D-23: Tests prove `.github/copilot-instructions.md` merge preserves user text and avoids duplicate marker blocks"
    - "COP-03: Tests prove Copilot uses `.agents/skills` and mixed Claude+Copilot installs write both skill trees"
    - "Phase 7 success criteria: Mixed `claude-code,codex` and `claude-code,copilot` selections write both `.claude/skills` and `.agents/skills` exactly once"
  artifacts:
    - path: "src/templates/__tests__/copilot-adapter.test.ts"
      provides: "Template contract coverage for Copilot assets"
      contains: "agentStop"
    - path: "test/init.test.ts"
      provides: "Compiled CLI coverage for Copilot install plus final mixed-selection coverage"
      contains: "init --tool copilot"
---

<objective>
Add Copilot and final Phase 7 regression coverage, including explicit Copilot bootstrap, no-clobber/idempotency, mixed Claude+Copilot skill trees, and no stale pending report.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-codex-and-copilot-adapters/07-CONTEXT.md
@.planning/phases/07-codex-and-copilot-adapters/07-RESEARCH.md
@.planning/phases/07-codex-and-copilot-adapters/07-VALIDATION.md
@.planning/phases/07-codex-and-copilot-adapters/07-04-PLAN.md
@.planning/phases/07-codex-and-copilot-adapters/07-05-PLAN.md
@test/init.test.ts
@test/helpers.ts
@src/templates/__tests__/opencode-adapter.test.ts
@src/templates/copilot/hooks/codewiki-hooks.json
@src/templates/copilot/instructions.md
@src/lib/adapters/copilot.ts
@src/commands/init.ts
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add Copilot template contract tests</name>
  <files>src/templates/__tests__/copilot-adapter.test.ts</files>
  <read_first>
    - src/templates/__tests__/opencode-adapter.test.ts
    - src/templates/copilot/hooks/codewiki-hooks.json
    - src/templates/copilot/hooks/pre-tool-use.sh
    - src/templates/copilot/hooks/post-tool-use.sh
    - src/templates/copilot/hooks/agent-stop.sh
    - src/templates/copilot/instructions.md
  </read_first>
  <action>
Create `src/templates/__tests__/copilot-adapter.test.ts` using Vitest and the same local `readFile` pattern as `opencode-adapter.test.ts`.

Add tests that assert:
- `hooks/codewiki-hooks.json` contains `"version": 1`, `preToolUse`, `postToolUse`, `agentStop`, `sessionEnd`, `.github/hooks/codewiki/pre-tool-use.sh`, `.github/hooks/codewiki/post-tool-use.sh`, `.github/hooks/codewiki/agent-stop.sh`, and `session-end.sh >/dev/null 2>&1 || true`
- wrappers contain required shared hook filenames and JSON-contract markers:
  - `pre-tool-use.sh` contains `pre-wiki-context.sh`
  - `post-tool-use.sh` contains `post-verify.sh` and `additionalContext`
  - `agent-stop.sh` contains `session-end.sh`, `agentStop`, `sessionEnd`, `"decision":"block"`, and `"decision":"allow"`
- instructions contain `.agents/skills/codewiki-<name>/SKILL.md`, `.github/hooks/codewiki-hooks.json`, `agentStop`, `sessionEnd`, `cleanup-only`, `wiki/_backlinks.json`, and `.codewiki/config.yml`
- instructions do not contain `.github/skills`
  </action>
  <acceptance_criteria>
    - `src/templates/__tests__/copilot-adapter.test.ts` contains `COP-01`
    - The test file contains `"version": 1`, `agentStop`, `sessionEnd`, `cleanup-only`, and `.agents/skills/codewiki-<name>/SKILL.md`
    - `npx vitest run src/templates/__tests__/copilot-adapter.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f src/templates/__tests__/copilot-adapter.test.ts && rg -n "COP-01|\\\"version\\\": 1|agentStop|sessionEnd|cleanup-only|\\.agents/skills/codewiki-<name>/SKILL\\.md" src/templates/__tests__/copilot-adapter.test.ts && npx vitest run src/templates/__tests__/copilot-adapter.test.ts</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Add Copilot compiled CLI install, no-clobber, and idempotency tests</name>
  <files>test/init.test.ts</files>
  <read_first>
    - test/init.test.ts
    - test/helpers.ts
    - src/lib/adapters/copilot.ts
    - src/commands/init.ts
  </read_first>
  <action>
Extend `test/init.test.ts` with a Copilot integration test.

The test must:
- create a temp project
- pre-create `.github/copilot-instructions.md` with `# Existing Copilot Instructions`
- optionally pre-create `.github/hooks/existing.json` to prove unrelated hook files are not removed
- run `init --tool copilot`
- assert stdout contains `shared-skills adapter:` and `copilot adapter:`
- assert stdout does not contain `Tool-specific integrations pending:` or `copilot (shared skills installed; hooks and instructions remain pending)`
- assert `.agents/skills/codewiki-ingest/SKILL.md` exists and `.claude/skills` does not exist
- assert `.github/hooks/codewiki-hooks.json`, `.github/hooks/codewiki/pre-tool-use.sh`, `.github/hooks/codewiki/post-tool-use.sh`, `.github/hooks/codewiki/agent-stop.sh`, and `.github/copilot-instructions.md` exist
- parse `.github/hooks/codewiki-hooks.json` and assert `version === 1`, plus `preToolUse`, `postToolUse`, `agentStop`, and `sessionEnd` keys exist
- assert `.github/copilot-instructions.md` still contains existing text and exactly one `<!-- codewiki:start -->`
- rerun `init --tool copilot` and assert marker count remains one
  </action>
  <acceptance_criteria>
    - `test/init.test.ts` contains `init --tool copilot`
    - `test/init.test.ts` contains `.github/hooks/codewiki-hooks.json` and `.github/copilot-instructions.md`
    - `test/init.test.ts` asserts no Copilot pending message
    - `npm run build && node --test dist/test/init.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg -n "init --tool copilot|\\.github/hooks/codewiki-hooks\\.json|\\.github/copilot-instructions\\.md|copilot \\(shared skills installed" test/init.test.ts && npm run build && node --test dist/test/init.test.js</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 3: Add final mixed-selection and stale pending-report regressions</name>
  <files>test/init.test.ts</files>
  <read_first>
    - test/init.test.ts
    - src/lib/adapters/index.ts
    - src/commands/init.ts
  </read_first>
  <action>
Update or add mixed-selection tests so:

`init --tool claude-code,copilot` asserts:
- stdout contains `claude-code adapter:`
- stdout contains `shared-skills adapter:`
- stdout contains `copilot adapter:`
- stdout does not contain `copilot (shared skills installed; hooks and instructions remain pending)`
- `.claude/skills/codewiki-ingest/SKILL.md` exists
- `.agents/skills/codewiki-ingest/SKILL.md` exists
- `.github/hooks/codewiki-hooks.json` exists
- `.github/copilot-instructions.md` exists
- `CLAUDE.md` exists

Add or update a final combined smoke assertion so `init --tool claude-code,codex,copilot,opencode` reports all real adapters and never prints `Tool-specific integrations pending:`.
  </action>
  <acceptance_criteria>
    - `test/init.test.ts` contains `claude-code,copilot`
    - `test/init.test.ts` contains `claude-code,codex,copilot,opencode`
    - Tests assert `copilot adapter:` and no `Tool-specific integrations pending:`
    - Tests assert both `.claude/skills` and `.agents/skills`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "claude-code,copilot|claude-code,codex,copilot,opencode|copilot adapter:|Tool-specific integrations pending|\\.claude/skills|\\.agents/skills" test/init.test.ts && npm run build && node --test dist/test/init.test.js</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests -> Copilot hook install | Tests must prove CodeWiki-owned hook JSON and wrappers land without deleting unrelated hooks |
| Tests -> Copilot instructions | Tests must prove user instruction text survives marker merge |
| Mixed adapters -> shared skill tree | Tests must prove shared-skills adapter runs once and both skill trees exist when Claude crosses with non-Claude tools |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | R | regression tests | mitigate | Assert exact installed files and report text for Copilot explicit and mixed selections |
| T-07-06-02 | T | no-clobber behavior | mitigate | Pre-seed Copilot instructions and unrelated hook file, then assert they survive |
| T-07-06-03 | D | idempotency | mitigate | Rerun install and assert marker counts remain stable |
| T-07-06-04 | T | all-tool selection | mitigate | Add smoke test proving all real adapters can install together without stale pending report |
</threat_model>

<verification>
- `npx vitest run src/templates/__tests__/copilot-adapter.test.ts`
- `npm run build && node --test dist/test/init.test.js`
- `npm run build && node --test dist/test/init.test.js && npx vitest run src/templates/__tests__/codex-adapter.test.ts src/templates/__tests__/copilot-adapter.test.ts`
- `npm test`
</verification>

<success_criteria>
- Copilot template contracts are locked by Vitest.
- Copilot installer behavior is locked by compiled CLI tests.
- Mixed Claude+Copilot selections install both skill trees exactly once.
- All supported tools can install together without stale pending-integration output.
</success_criteria>

<output>
After completion, create `.planning/phases/07-codex-and-copilot-adapters/07-06-SUMMARY.md`.
</output>
