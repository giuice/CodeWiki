---
phase: 07-codex-and-copilot-adapters
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/templates/__tests__/codex-adapter.test.ts
  - test/init.test.ts
autonomous: true
requirements:
  - CODEX-01
  - CODEX-02
  - CODEX-03
must_haves:
  truths:
    - "D-01: Tests prove Codex adapter scope is complete, not instruction-only"
    - "D-05 through D-10: Tests lock Codex hook event names, matcher aliases, feature flag, and root resolution"
    - "D-11 through D-13: Tests lock the two Codex TOML agents and their role responsibilities"
    - "D-19/D-20/D-22: Tests prove `AGENTS.md` merge preserves user text and avoids duplicate marker blocks"
    - "CODEX-01: Tests prove Codex uses `.agents/skills` and mixed Claude+Codex installs write both skill trees"
  artifacts:
    - path: "src/templates/__tests__/codex-adapter.test.ts"
      provides: "Template contract coverage for Codex assets"
      contains: "UserPromptSubmit"
    - path: "test/init.test.ts"
      provides: "Compiled CLI coverage for Codex install, no-clobber, idempotency, and mixed selections"
      contains: "init --tool codex"
---

<objective>
Add Codex regression coverage for template contracts, explicit bootstrap, no-clobber behavior, rerun idempotency, and mixed Claude+Codex skill-tree installs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-codex-and-copilot-adapters/07-CONTEXT.md
@.planning/phases/07-codex-and-copilot-adapters/07-RESEARCH.md
@.planning/phases/07-codex-and-copilot-adapters/07-VALIDATION.md
@.planning/phases/07-codex-and-copilot-adapters/07-01-PLAN.md
@.planning/phases/07-codex-and-copilot-adapters/07-02-PLAN.md
@test/init.test.ts
@test/helpers.ts
@src/templates/__tests__/opencode-adapter.test.ts
@src/templates/codex/hooks.json
@src/templates/codex/config.toml
@src/templates/codex/instructions.md
@src/templates/codex/agents/codewiki-wiki-updater.toml
@src/templates/codex/agents/codewiki-verifier.toml
</context>

<tasks>

<task type="execute">
  <name>Task 1: Add Codex template contract tests</name>
  <files>src/templates/__tests__/codex-adapter.test.ts</files>
  <read_first>
    - src/templates/__tests__/opencode-adapter.test.ts
    - src/templates/codex/hooks.json
    - src/templates/codex/config.toml
    - src/templates/codex/instructions.md
    - src/templates/codex/agents/codewiki-wiki-updater.toml
    - src/templates/codex/agents/codewiki-verifier.toml
  </read_first>
  <action>
Create `src/templates/__tests__/codex-adapter.test.ts` using Vitest and the same local `readFile` pattern as `opencode-adapter.test.ts`.

Add tests that assert:
- `hooks.json` contains `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`, `Edit|Write|apply_patch`, `user-prompt-submit.sh`, `pre-tool-use.sh`, `post-tool-use.sh`, `stop.sh`, and `git rev-parse --show-toplevel`
- `config.toml` contains `[features]` and `codex_hooks = true`
- wrapper scripts contain the required shared hook filenames and JSON-contract markers:
  - `user-prompt-submit.sh` contains `pre-wiki-context.sh`
  - `pre-tool-use.sh` contains `plain stdout` and `PreToolUse`
  - `post-tool-use.sh` contains `post-verify.sh`, `hookEventName`, and `PostToolUse`
  - `stop.sh` contains `session-end.sh`, `stop_hook_active`, and `"decision":"block"`
- TOML agents contain `name =`, `description =`, `developer_instructions =`, updater approval language, verifier read-only language, `wiki/index.md`, and `wiki/log.md`
- instructions contain `.agents/skills/codewiki-<name>/SKILL.md`, `UserPromptSubmit`, `PostToolUse`, `loop-safe Stop`, `wiki/_backlinks.json`, and `.codewiki/config.yml`
  </action>
  <acceptance_criteria>
    - `src/templates/__tests__/codex-adapter.test.ts` contains `describe("CODEX-02`
    - The test file contains `UserPromptSubmit`, `Edit|Write|apply_patch`, `codex_hooks = true`, `stop_hook_active`, and `developer_instructions`
    - `npx vitest run src/templates/__tests__/codex-adapter.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f src/templates/__tests__/codex-adapter.test.ts && rg -n "CODEX-02|UserPromptSubmit|Edit\\|Write\\|apply_patch|codex_hooks = true|stop_hook_active|developer_instructions" src/templates/__tests__/codex-adapter.test.ts && npx vitest run src/templates/__tests__/codex-adapter.test.ts</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 2: Add Codex compiled CLI install, no-clobber, and idempotency tests</name>
  <files>test/init.test.ts</files>
  <read_first>
    - test/init.test.ts
    - test/helpers.ts
    - src/lib/adapters/codex.ts
    - src/commands/init.ts
  </read_first>
  <action>
Extend `test/init.test.ts` with a Codex integration test.

The test must:
- create a temp project
- pre-create `.codex/hooks.json` containing a user `PreToolUse` hook entry
- pre-create `.codex/config.toml` containing `[features]\nother_feature = true\ncodex_hooks = false\n`
- pre-create `AGENTS.md` with `# Existing Instructions`
- run `init --tool codex`
- assert stdout contains `shared-skills adapter:` and `codex adapter:`
- assert stdout does not contain `Tool-specific integrations pending:` or `codex (shared skills installed; hooks and instructions remain pending)`
- assert `.agents/skills/codewiki-ingest/SKILL.md` exists and `.claude/skills` does not exist
- assert `.codex/hooks.json`, `.codex/config.toml`, `.codex/hooks/user-prompt-submit.sh`, `.codex/hooks/post-tool-use.sh`, `.codex/hooks/stop.sh`, `.codex/agents/codewiki-wiki-updater.toml`, `.codex/agents/codewiki-verifier.toml`, and `AGENTS.md` exist
- parse `.codex/hooks.json` and assert user hook plus CodeWiki hook entries remain
- assert `.codex/config.toml` contains `other_feature = true` and `codex_hooks = true`, and no longer contains `codex_hooks = false`
- assert `AGENTS.md` still contains `# Existing Instructions` and exactly one `<!-- codewiki:start -->`
- rerun `init --tool codex` and assert marker count remains one and hook entries are not duplicated
  </action>
  <acceptance_criteria>
    - `test/init.test.ts` contains `init --tool codex`
    - `test/init.test.ts` contains `.codex/hooks.json`, `.codex/config.toml`, and `.codex/agents/codewiki-wiki-updater.toml`
    - `test/init.test.ts` asserts no Codex pending message
    - `npm run build && node --test dist/test/init.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rg -n "init --tool codex|\\.codex/hooks\\.json|\\.codex/config\\.toml|\\.codex/agents/codewiki-wiki-updater\\.toml|codex \\(shared skills installed" test/init.test.ts && npm run build && node --test dist/test/init.test.js</automated>
  </verify>
</task>

<task type="execute">
  <name>Task 3: Add mixed Claude+Codex skill tree regression</name>
  <files>test/init.test.ts</files>
  <read_first>
    - test/init.test.ts
    - src/lib/adapters/index.ts
    - src/lib/adapters/shared-skills.ts
  </read_first>
  <action>
Update the existing mixed-tool pending integration test or add a new focused test so `init --tool claude-code,codex` now asserts:
- stdout contains `claude-code adapter:`
- stdout contains `shared-skills adapter:`
- stdout contains `codex adapter:`
- stdout does not contain `codex (shared skills installed; hooks and instructions remain pending)`
- `.claude/skills/codewiki-ingest/SKILL.md` exists
- `.agents/skills/codewiki-ingest/SKILL.md` exists
- `.codex/hooks.json` exists
- `AGENTS.md` exists
- `CLAUDE.md` exists

If an older assertion expected Codex to be pending, replace it with the new real-adapter assertions.
  </action>
  <acceptance_criteria>
    - `test/init.test.ts` contains `claude-code,codex`
    - The mixed-selection test asserts `codex adapter:`
    - The mixed-selection test asserts both `.claude/skills` and `.agents/skills`
    - The test does not expect Codex pending text
  </acceptance_criteria>
  <verify>
    <automated>rg -n "claude-code,codex|codex adapter:|\\.claude/skills|\\.agents/skills" test/init.test.ts && npm run build && node --test dist/test/init.test.js</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests -> user config merge | Tests must prove user hooks, TOML values, and instruction text survive installation |
| Tests -> report semantics | Tests must catch contradictory pending reports after real adapter implementation |
| Mixed adapters -> shared skill tree | Tests must prove shared-skills adapter is deduped but still runs for Codex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | R | regression tests | mitigate | Assert exact installed files and report text for Codex explicit and mixed selections |
| T-07-03-02 | T | no-clobber behavior | mitigate | Pre-seed user `.codex` and `AGENTS.md` content, then assert it survives |
| T-07-03-03 | D | idempotency | mitigate | Rerun install and assert marker/hook counts remain stable |
</threat_model>

<verification>
- `npx vitest run src/templates/__tests__/codex-adapter.test.ts`
- `npm run build && node --test dist/test/init.test.js`
- `npm test`
</verification>

<success_criteria>
- Codex template contracts are locked by Vitest.
- Codex installer behavior is locked by compiled CLI tests.
- Mixed Claude+Codex selections install both skill trees exactly once and no longer report Codex as pending.
</success_criteria>

<output>
After completion, create `.planning/phases/07-codex-and-copilot-adapters/07-03-SUMMARY.md`.
</output>
