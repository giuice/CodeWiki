---
phase: 02-shared-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - package.json
  - src/lib/index.ts
autonomous: true
requirements: [WIKI-01, WIKI-02, WIKI-03, WIKI-04, WIKI-05, MERGE-01, MERGE-02, MERGE-03, MERGE-04]

must_haves:
  truths:
    - "npm run build compiles all src/lib/ modules without errors"
    - "All vitest tests pass in a single run"
    - "src/lib/index.ts re-exports all public APIs from merge, scaffold, detect, reporter"
  artifacts:
    - path: "src/lib/index.ts"
      provides: "Barrel export for all lib modules"
      exports: ["deepMerge", "mergeMarkerSection", "deduplicateHookArray", "scaffoldProject", "detectTools", "formatReport"]
  key_links:
    - from: "src/lib/index.ts"
      to: "src/lib/merge.ts"
      via: "export * from"
      pattern: "export.*merge"
---

<objective>
Create barrel export, configure postbuild copy step, and run full integration verification across all Phase 2 modules.

Purpose: Confirms all modules work together, build pipeline copies templates to dist/, and public API is cleanly exported for Phase 4 consumers.
Output: Clean build with all tests passing and postbuild copy verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-infrastructure/02-01-SUMMARY.md
@.planning/phases/02-shared-infrastructure/02-02-SUMMARY.md

@src/lib/merge.ts
@src/lib/scaffold.ts
@src/lib/detect.ts
@src/lib/reporter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create barrel export and postbuild copy step</name>
  <files>src/lib/index.ts, package.json</files>
  <read_first>
    - package.json (current scripts)
    - src/lib/merge.ts (exported functions)
    - src/lib/scaffold.ts (exported functions)
    - src/lib/detect.ts (exported functions)
    - src/lib/reporter.ts (exported functions and types)
  </read_first>
  <action>
1. Create `src/lib/index.ts`:
```typescript
export { deepMerge, deduplicateHookArray, mergeMarkerSection } from "./merge.js";
export { scaffoldProject } from "./scaffold.js";
export type { ScaffoldOptions } from "./scaffold.js";
export { detectTools } from "./detect.js";
export { formatReport } from "./reporter.js";
export type { ReportAction, ReportEntry } from "./reporter.js";
```

2. Update package.json scripts — add postbuild to copy templates:
   - Add `"postbuild": "cp -r src/templates dist/templates"` (POSIX, no extra deps)
   - This ensures `dist/templates/` contains .ts template files after build (they get compiled by tsc AND the source .ts files are copied for reference; the actual template content functions are in the compiled .js files)

3. Run `npm run build` — must compile cleanly and `dist/templates/` must exist.
4. Run `npx vitest run` — all tests must pass.
  </action>
  <verify>
    <automated>npm run build && npx vitest run && ls dist/lib/index.js dist/templates/scaffold.js</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/index.ts contains `export { deepMerge` and `export { scaffoldProject` and `export { detectTools` and `export { formatReport`
    - package.json scripts contains "postbuild"
    - `npm run build` exits 0
    - `dist/lib/index.js` exists after build
    - `dist/templates/` directory exists after build
    - `npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All Phase 2 modules build, test, and export cleanly; postbuild copies templates to dist/</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan wires existing modules; no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | postbuild script | accept | cp -r of known source directory; no user input involved |
</threat_model>

<verification>
- `npm run build` exits 0
- `ls dist/lib/index.js` confirms barrel export compiled
- `ls dist/templates/` confirms postbuild copy worked
- `npx vitest run` all tests pass
</verification>

<success_criteria>
- All 4 lib modules compile and export through index.ts
- postbuild step copies src/templates/ to dist/templates/
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-infrastructure/02-03-SUMMARY.md`
</output>
