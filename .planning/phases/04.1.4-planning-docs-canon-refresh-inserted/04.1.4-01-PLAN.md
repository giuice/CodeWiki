---
phase: 04.1.4-planning-docs-canon-refresh-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/CONVENTIONS.md
autonomous: true
requirements: [SM-05]
must_haves:
  truths:
    - "ROADMAP.md success criteria and descriptions reference skills (not slash commands) as the install surface"
    - "REQUIREMENTS.md traceability marks SM-01 through SM-04 as Complete with correct phase assignments"
    - "STATE.md accumulated decisions reflect the skills canon and parser-safe numbering consistently"
    - "CONVENTIONS.md uses parser-safe decimal examples (4.1.1, 4.1.2) not letter suffixes (4.1a, 4.1b)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap with skills-canon language and correct SM traceability"
      contains: "Skills Migration"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirements with SM-01 through SM-04 completion status"
      contains: "SM-05"
    - path: ".planning/STATE.md"
      provides: "Updated state reflecting skills canon decisions"
      contains: "skills"
    - path: ".planning/CONVENTIONS.md"
      provides: "Updated conventions with parser-safe examples"
      contains: "4.1.1"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Requirement IDs referenced in success criteria and traceability"
      pattern: "SM-0[1-6]"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/STATE.md"
      via: "Decision alignment on skills canon"
      pattern: "skill"
---

<objective>
Refresh ROADMAP.md, REQUIREMENTS.md, STATE.md, and CONVENTIONS.md to reflect the skills canon and parser-safe sub-phase numbering, and fix stale slash-command references and incomplete traceability.

Purpose: Planning artifacts have drifted from the actual implemented state. Phases 04.1.1–04.1.3 completed the skills migration, but ROADMAP.md still references slash commands, REQUIREMENTS.md shows SM-02/SM-03 as "Planned" when they're complete, STATE.md decisions mention old patterns, and CONVENTIONS.md uses `4.1a`-style numbering that GSD tooling cannot parse. This plan brings all four files into canonical alignment.

Output: Updated ROADMAP.md, REQUIREMENTS.md, STATE.md, and CONVENTIONS.md with consistent skills-canon language, correct completion statuses, and parser-safe numbering.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/04.1-skills-migration/04.1-CONTEXT.md
@.planning/phases/04.1.1-skill-template-source-inserted/04.1.1-01-SUMMARY.md
@.planning/phases/04.1.2-adapter-skill-install-paths-inserted/04.1.2-03-SUMMARY.md
@.planning/phases/04.1.3-skills-regression-coverage-inserted/04.1.3-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update ROADMAP.md and REQUIREMENTS.md with skills-canon language and correct traceability</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>
In ROADMAP.md, make the following precise updates (do NOT rewrite sections — only change the specific stale references):

1. Phase 3 success criterion line about "slash command markdown files" — update to "six skill template files" or "six prompt markdown files" (the phase created both command files AND later they became skills, so the historical description of what Phase 3 created should reference "prompt markdown files" neutrally or "skill source files").
2. Phase 3.1 success criteria lines 1 and 2 reference `src/templates/claude/commands/codewiki/absorb.md` and `breakdown.md` — update paths to `src/templates/skills/codewiki-absorb/SKILL.md` and `src/templates/skills/codewiki-breakdown/SKILL.md` to reflect the canonical skill locations. Also update the plan title "Create absorb.md and breakdown.md slash commands" to "Create absorb and breakdown skill sources".
3. Phase 4 success criterion line about "8 slash commands to `.claude/commands/codewiki/`" — update to "8 skills to `.claude/skills/codewiki-<name>/`" to reflect the canonical skill install surface.
4. Phase 4.1.1 success criterion line 4 "The legacy files in `src/templates/claude/commands/codewiki/` are no longer the source of truth" — keep this as-is (it's a true statement about the migration result, not a future action).
5. Phase 5 success criterion line about `dist/templates/claude/commands/codewiki/ingest.md` — update to `dist/templates/skills/codewiki-ingest/SKILL.md` to reflect the canonical packaged path.
6. Phase 6 success criterion line about "8 slash commands" — update to "8 skills" throughout.
7. Phase 7 success criteria — update "6 slash commands" references to "6 skills" where applicable (keep the "per-project command directory" language since Codex commands are a different concept from CodeWiki skills).
8. Phase 4.1 Planning Note line — the note already correctly says "parser-safe" and "4.1a" is only mentioned as something NOT supported, so this can stay as-is.

In REQUIREMENTS.md, make these precise updates:

1. SM-02 traceability: Change status from "Planned" to "Complete (2026-04-12)" and phase from "Phase 4.1.2 (Adapter Skill Install Paths)" keeping the same.
2. SM-03 traceability: Change status from "Planned" to "Complete (2026-04-12)".
3. BUILD-02 success criterion: Update `dist/templates/claude/commands/codewiki/ingest.md` to `dist/templates/skills/codewiki-ingest/SKILL.md` to reflect the canonical packaged asset path (this was also updated in Phase 4.1.3 tests).
4. CC-01: Update "Installs 8 slash commands to `.claude/commands/codewiki/`" to "Installs 8 skills to `.claude/skills/codewiki-<name>/`" — this is the canonical Claude install surface.
5. OC-01: Update "Installs 8 slash commands to `.opencode/commands/codewiki/`" to "Installs 8 skills to `.opencode/skills/codewiki-<name>/`" for consistency.
6. CODEX-01: Update "Installs 6 slash commands" to "Installs 6 skills" for consistency.
7. Traceability table: Verify SM-01 has Complete, SM-04 has Complete, SM-05 has "Phase 4.1.4 (Planning Docs Canon Refresh) — Planned", and SM-06 has "Phase 4.1.5 (Product Docs Canon Refresh) — Planned".
8. Update the "Last updated" line to the current date with the note "Phase 4.1.4 — planning docs canon refresh and SM traceability update".
  </action>
  <verify>
    <automated>cd /home/giuice/Desenv/CodeWiki && grep -c "slash command" .planning/ROADMAP.md && echo "---" && grep -c "slash command" .planning/REQUIREMENTS.md && echo "---" && grep "SM-02" .planning/REQUIREMENTS.md | grep -c "Complete" && echo "---" && grep "SM-03" .planning/REQUIREMENTS.md | grep -c "Complete" && echo "---" && grep "codewiki-ingest/SKILL.md" .planning/ROADMAP.md | wc -l && echo "---" && grep "codewiki-ingest/SKILL.md" .planning/REQUIREMENTS.md | wc -l</automated>
  </verify>
  <done>
    - ROADMAP.md no longer references "slash command" install surfaces in success criteria for Phases 4, 5, 6
    - REQUIREMENTS.md traceability marks SM-02 and SM-03 as Complete
    - REQUIREMENTS.md BUILD-02 references canonical `dist/templates/skills/codewiki-ingest/SKILL.md` path
    - CC-01 and OC-01 reference `.claude/skills/` and `.opencode/skills/` respectively
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update STATE.md and CONVENTIONS.md to reflect skills canon and parser-safe numbering</name>
  <files>.planning/STATE.md, .planning/CONVENTIONS.md</files>
  <read_first>.planning/STATE.md, .planning/CONVENTIONS.md</read_first>
  <action>
In STATE.md, make the following updates to the Accumulated Context section:

1. Decisions section — Add a new entry: "Phase 04.1.4: Planning artifacts refreshed to reflect the skills canon — ROADMAP.md, REQUIREMENTS.md, STATE.md, and CONVENTIONS.md now describe the eight-skill install surface and parser-safe decimal sub-phases consistently."
2. Decisions section — Update the existing entry "Phase 04.1 planning uses parser-safe chained decimals (4.1.1-4.1.5) because local GSD tooling does not accept 4.1a-style suffixes" — this is already correct and can stay as-is.
3. Decisions section — Add: "Phase 04.1.2: skills canon install surface is `.claude/skills/codewiki-<name>/SKILL.md` for Claude and `.agents/skills/codewiki-<name>/SKILL.md` for non-Claude tools, replacing the legacy `.claude/commands/codewiki/` directory."
4. Pending Todos section — Confirm it says "None yet" or remove if already empty.
5. Update `last_activity` to current date with note "Phase 04.1.4 — planning docs canon refresh".
6. Update `stopped_at` to "Phase 04.1.4 complete; Phase 04.1.5 ready to plan".

In CONVENTIONS.md, make this precise update:

1. In the "Phase decomposition rules" bullet list, change the example from "(e.g., 4.1a, 4.1b)" to "(e.g., 4.1.1, 4.1.2)" to show parser-safe decimal numbering instead of letter suffixes that GSD tooling cannot parse.

This is the ONLY change in CONVENTIONS.md — do not modify any other content.
  </action>
  <verify>
    <automated>cd /home/giuice/Desenv/CodeWiki && grep "4\.1\.1, 4\.1\.2" .planning/CONVENTIONS.md | wc -l && echo "---" && grep "4\.1a" .planning/CONVENTIONS.md | wc -l && echo "---" && grep "skills" .planning/STATE.md | grep -ci "canon\|install surface" && echo "---" && grep "last_activity" .planning/STATE.md</automated>
  </verify>
  <done>
    - CONVENTIONS.md uses parser-safe decimal examples (4.1.1, 4.1.2) instead of letter suffixes (4.1a, 4.1b)
    - STATE.md includes Phase 04.1.4 completion decision reflecting the skills canon refresh
    - STATE.md includes the Phase 04.1.2 skills canon install surface decision
    - STATE.md last_activity updated to the current date
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase with no code changes, no external inputs, no runtime boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.4-01 | Spoofing | N/A | accept | Documentation-only — no identity boundaries |
| T-04.1.4-02 | Tampering | Planning docs | mitigate | Changes reviewed via git diff before commit; no automated deployment |
| T-04.1.4-03 | Information Disclosure | N/A | accept | No sensitive data in planning docs |

Note: Documentation-only phase. No code execution, no network calls, no secrets. Threat model is trivially satisfied.
</threat_model>

<verification>
1. `grep -c "slash command" .planning/ROADMAP.md` — should be 0 or only appear in Phase 3's historical description (which created commands that were later migrated to skills)
2. `grep "SM-02.*Complete" .planning/REQUIREMENTS.md` — should match
3. `grep "SM-03.*Complete" .planning/REQUIREMENTS.md` — should match
4. `grep "codewiki-ingest/SKILL.md" .planning/ROADMAP.md` — canonical path present
5. `grep "4\.1\.1, 4\.1\.2" .planning/CONVENTIONS.md` — parser-safe examples
6. `grep "4\.1a" .planning/CONVENTIONS.md` — should return 0 matches
7. `grep "skills" .planning/STATE.md | grep -i "canon\|install"` — skills canon decision present
</verification>

<success_criteria>
1. ROADMAP.md, REQUIREMENTS.md, and STATE.md describe the split phase structure and skill canon consistently — verified by absence of stale "slash command" install surface language and presence of canonical skill paths
2. Traceability exists for SM-01 through SM-06 — SM-01 through SM-04 marked Complete, SM-05 in progress (this phase), SM-06 Planned
3. CONVENTIONS.md uses parser-safe decimal sub-phase examples, not `4.1a` letter suffixes
4. STATE.md accumulated decisions include the skills canon install surface decision
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.4-planning-docs-canon-refresh-inserted/04.1.4-01-SUMMARY.md`
</output>