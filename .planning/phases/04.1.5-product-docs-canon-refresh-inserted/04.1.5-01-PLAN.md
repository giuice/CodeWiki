---
phase: 04.1.5-product-docs-canon-refresh-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/codewiki-project-v2.md
  - docs/implementation-plan-v2.md
  - docs/skills-migration-handoff.md
autonomous: true
requirements:
  - SM-06
must_haves:
  truths:
    - "README presents CodeWiki as eight Skills installed into the canonical skill trees, not as slash-command files"
    - "Readers can tell when installs write `.claude/skills/codewiki-<name>/SKILL.md`, when they write `.agents/skills/codewiki-<name>/SKILL.md`, and when both trees are required"
    - "Implementation and handoff docs no longer describe `.claude/commands/codewiki/` or similar command directories as the current source of truth"
  artifacts:
    - path: "README.md"
      provides: "User-facing setup, workflow, and multi-tool install guidance aligned to the skills canon"
      contains: ".claude/skills"
    - path: "docs/codewiki-project-v2.md"
      provides: "Canonical v2 architecture and install-surface rules aligned to shipped behavior"
      contains: ".agents/skills"
    - path: "docs/implementation-plan-v2.md"
      provides: "Implementation/backfill guidance that uses skill-tree paths and current template locations"
      contains: "src/templates/skills"
    - path: "docs/skills-migration-handoff.md"
      provides: "Current-state migration handoff preserving dual-tree rationale without stale next-step instructions"
      contains: "SM-06"
  key_links:
    - from: "README.md"
      to: "docs/codewiki-project-v2.md"
      via: "shared install-surface language and tool matrix"
      pattern: "\\.claude/skills|\\.agents/skills|eight Skills"
    - from: "docs/implementation-plan-v2.md"
      to: "docs/codewiki-project-v2.md"
      via: "reference PRD and matching install paths"
      pattern: "Reference PRD|src/templates/skills|SKILL\\.md"
    - from: "docs/skills-migration-handoff.md"
      to: "docs/codewiki-project-v2.md"
      via: "Decision 8 and dual-tree migration rationale"
      pattern: "Decision 8|dual tree|SM-06"
---

<objective>
Refresh the product-facing documentation set so it matches the shipped skills-canon install surface and the dual-tree adapter rules.

Purpose: Close SM-06 using the already-verified outcomes from Phases 04.1.2-04.1.4 instead of leaving README and implementation docs on the retired command-directory model.
Output: Updated README, v2 PRD, implementation plan, and migration handoff that all describe skills as the install surface and keep only clearly labeled historical references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1.2-adapter-skill-install-paths-inserted/04.1.2-03-SUMMARY.md
@.planning/phases/04.1.3-skills-regression-coverage-inserted/04.1.3-02-SUMMARY.md
@.planning/phases/04.1.4-planning-docs-canon-refresh-inserted/04.1.4-01-SUMMARY.md
@README.md
@docs/codewiki-project-v2.md
@docs/implementation-plan-v2.md
@docs/skills-migration-handoff.md
@docs/skills/wiki.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reframe README around skills and the dual-tree install surface</name>
  <files>README.md</files>
  <action>
Rewrite the user-facing overview, workflow sections, architecture diagram labels, generated project layout, quick start, CLI description, skills table, and multi-tool support matrix so README describes CodeWiki as installing eight Skills rather than slash-command markdown files. Keep the logical command names (`codewiki-ingest`, etc.) available as skill names/examples, but do not present `.claude/commands/codewiki/` or flat command directories as the live install surface. Reflect the verified tree split from Phases 04.1.2 and 04.1.3: Claude-only selections write `.claude/skills/codewiki-<name>/SKILL.md`, non-Claude selections write `.agents/skills/codewiki-<name>/SKILL.md`, and mixed selections write both trees.
  </action>
  <verify>
    <automated>rtk grep -nE "\.claude/skills|\.agents/skills|eight Skills|SKILL\.md" README.md &amp;&amp; if rtk grep -nE "\.claude/commands|commands/codewiki|installs slash commands|## Slash Commands" README.md; then exit 1; fi</automated>
  </verify>
  <done>README gives a reader the correct install surface, current tool matrix, and canonical skill-tree paths without any unqualified legacy command-directory guidance.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align the v2 PRD and implementation plan to shipped skill paths</name>
  <files>docs/codewiki-project-v2.md, docs/implementation-plan-v2.md</files>
  <action>
Update the canonical product and implementation docs so their current-state prose matches the shipped post-migration reality. In `docs/codewiki-project-v2.md`, remove stale implementation-status language that still claims the installer writes `.claude/commands/codewiki/*.md`, and make sure the current install model consistently references `src/templates/skills/codewiki-<name>/SKILL.md`, `.claude/skills/...`, and `.agents/skills/...` as appropriate. In `docs/implementation-plan-v2.md`, rewrite the remaining command-directory source-of-truth notes, file maps, adapter copy examples, smoke-test paths, and README follow-up text so the document clearly reads as a superseded implementation plan updated to the skills canon instead of a live command-directory guide. Preserve historical context only when explicitly labeled as superseded background.
  </action>
  <verify>
    <automated>rtk grep -nE "\.claude/skills|\.agents/skills|src/templates/skills|SKILL\.md" docs/codewiki-project-v2.md docs/implementation-plan-v2.md &amp;&amp; if rtk grep -nE "\.claude/commands|commands/codewiki|src/templates/claude/commands|src/prompts/commands" docs/codewiki-project-v2.md docs/implementation-plan-v2.md; then exit 1; fi</automated>
  </verify>
  <done>Both docs describe the current skill-tree install surface and no longer leave readers with obsolete command-path or flat-command-source guidance.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert the migration handoff into a current-state reference</name>
  <files>docs/skills-migration-handoff.md</files>
  <action>
Refresh the handoff so it reflects what 04.1.1 through 04.1.4 already accomplished and positions 04.1.5 as the remaining docs-canon cleanup, not an unfinished adapter migration. Keep the historical commit trail and dual-tree rationale, but remove or rewrite sections that still instruct the next session to migrate from `.claude/commands/codewiki/` as if that were the current product surface. The handoff should remain useful as a reference for why dual-tree skills exist and what SM-06 still needed, while no longer pointing future readers at stale next-step instructions.
  </action>
  <verify>
    <automated>rtk grep -nE "\.claude/skills|\.agents/skills|dual tree|SM-06" docs/skills-migration-handoff.md &amp;&amp; if rtk grep -nE "real problem — this is a code migration|Recommended next phase: `04\.1|what Phase 4 actually installs|Next session inherits a cleaner problem statement: migrate Phase 4 output" docs/skills-migration-handoff.md; then exit 1; fi</automated>
  </verify>
  <done>The handoff preserves migration rationale and historical context but no longer claims the code migration is still pending or that command directories are the live target state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> developer/operator | Users rely on these docs to choose paths, flags, and install expectations for CodeWiki integrations |
| historical notes -> current guidance | Legacy migration notes can be mistaken for live instructions if not clearly framed as superseded context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.5-01 | T | README.md + implementation docs | mitigate | Replace obsolete command-directory install guidance with the verified skills canon and dual-tree rules, then grep the touched docs for legacy path patterns as part of task verification |
| T-04.1.5-02 | R | docs/skills-migration-handoff.md | mitigate | Preserve historical rationale and commits, but rewrite next-step sections so readers can distinguish completed migration history from the remaining SM-06 docs cleanup |
</threat_model>

<verification>
Run the task-level grep checks, then do one final cross-doc audit:

`rtk grep -nE "\.claude/skills|\.agents/skills|src/templates/skills|SKILL\.md" README.md docs/codewiki-project-v2.md docs/implementation-plan-v2.md docs/skills-migration-handoff.md`

Manually confirm any remaining `slash command` phrasing is clearly historical or refers only to user invocation syntax, not the installed file layout.
</verification>

<success_criteria>
- README explains CodeWiki as an eight-skill install surface and documents the `.claude/skills` / `.agents/skills` split accurately.
- `docs/codewiki-project-v2.md` and `docs/implementation-plan-v2.md` no longer describe command directories as the current source of truth.
- `docs/skills-migration-handoff.md` records dual-tree migration history without telling future readers the adapter migration is still pending.
- A grep across the four touched docs shows the canonical skill-tree paths and no unqualified obsolete command-directory path claims.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1.5-product-docs-canon-refresh-inserted/04.1.5-01-SUMMARY.md`
</output>
